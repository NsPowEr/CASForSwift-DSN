# TASKLIST MASTER — CAS Engine (Single Source of Truth)

> **Unico documento operativo dei task.** Sostituisce e consolida: `CAS_TASKS.md`,
> `PLAN_HP_PRIME_PARITY.md`, `PLAN_F3_F8_GAP_CLOSURE.md`, `PLAN_NEXT_SESSIONS.md`,
> `PLAN_TASKS_REMAINING.md`, `TODO_PH8.md`, `TODO.md`, `HANDOFF_T055.md`, `STATE.md`
> (tutti marcati `⚠️ SUPERSEDED`, non aggiornare).
>
> **Generato 2026-06-26** da audit code-level: ogni voce qui sotto è stata verificata
> **a codice** (file:riga, `git log`, symbol grep), NON copiata dallo stato dichiarato nei
> vecchi file. I vecchi tracker **sovrastimavano** il lavoro aperto e marcavano "RISOLTO"
> codice con gap reali o stub (vedi memoria `tasklist-ledger-stale-2026-06-19`,
> `plan-lies-2026-06-11`). Regola di manutenzione: prima di toccare un task, **ri-verifica
> a codice** — gli status flag di `ledger_index.py` restano inaffidabili.
>
> Registro hardcode autoritativo: `HARDCODE_LEDGER.md` (via `python3 scripts/ledger_index.py`).
> Criteri di chiusura: `DEFINITION_OF_DONE.md`.

---

## Legenda codici `[E·C·S·R]`

- **E**ffort: E1 <1g · E2 1-3g · E3 1-2sett · E4 2-4sett · E5 >1mese
- **C**omplessità: C1 meccanico · C2 moderato · C3 algoritmico · C4 research-grade
- **S**everità: S1 nice-to-have · S2 debito qualità · S3 gap funzionale · S4 bloccante · S5 critico (correttezza/hang)
- **R**ischio regressione: R1 basso (nuovo file) · R2 moderato (file condivisi+guardia) · R3 alto (hot-path: simplifier/arena/BigInt)

Colonna **Refs** = ID nei vecchi tracker (tracciabilità). `git`-anchor = evidenza verifica.

---

## A — TASK APERTI ACTIONABLE (backlog attivo)

Ordinati per severità/impatto decrescente. Ogni voce verificata aperta a codice 2026-06-26.

### A1 · Risch RP-2 parametric PolyRischDE non-cancellation (df>0) — ✅ FATTO 2026-07-01 (Bronstein §7.1) — `[E4·C4·S3·R2]`
> **✅ RISOLTO 2026-07-01** (sbloccato da A26; spec `Symbolic_Integration_I.md §6.4/§6.5/§7.1` letta, REGOLA 0.1). Il ramo `df>0` di `solve_risch_de_parametric_field` è ora implementato: nuovo `solve_param_poly_risch_de_nocancel1` (`src/calculus/risch_param_nocancel.cpp`) = **ParamPolyRischDENoCancel1 verbatim** (Bronstein §7.1, "When deg(b) is Large Enough"). Per torri log(δ=0)/exp(δ=1), `df>0 ⇒ deg(b)>max(0,δ−1)` ⇒ non-cancellation sempre → NoCancel1 unico algoritmo. Peel top-down `s_i=coeff(g_i,t^{n+d_b})/lc(b)`, `h_i+=s_i t^n`, `g_i←g_i−D(s_i t^n)−b s_i t^n` (n=N..0); residuo `Σc_i g_i=0` → ConstantSystem su K=Q(x) (clear-denominatori + Q-coords) → null space → `q=Σc_i h_i`, `y=q/D`. **SOUND**: ogni candidato verificato per field back-substitution `D(q)+f_new·q≡Σc_i g_i` in K[t]. **Test df>0** (flippati da Unimplemented): log `f=t,g={t}`→`y=1,c=1`; exp `f=t,g={t²+t}`→`y=t,c=1`; log `f=t,g={1}`→solo triviale (nessuna soluzione elementare, corretto). Audit hardcode: **0 violazioni**. 17 ParametricTower + 168 risch/integrate verdi, 0 regressioni.
> **🔧 RESIDUO (non-bloccante, boundary reale)**: ConstantSystem su **torri profonde** K⊋Q(x) (residuo con θ dei livelli inferiori) → Unimplemented pulito (serve la riduzione derivation-based di Bronstein §7.1 / ConstantSystem generale, righe 6801-6817). NoCancel2/3 (deg(b) piccolo, monomi nonlineari) non occorrono per df>0 su log/exp. Wiring in `integrate()` reale = passo successivo (path tower→field solver).
- **AUDIT 2026-06-27** (spec `Risch_Transcendental_Cap8.md` + `Risch_Hermite_Cap5.md` lette): la premessa "scaffold da sostituire" era **fuorviante**. Stato reale:
  - `risch_rde_bronstein_hermite.cpp::risch_rde_hermite_parametric_stub` è **DEAD CODE**: compilato (CMakeLists:220) ma **mai chiamato** da nessuno (grep verificato). NON è il punto attivo.
  - Il solver parametrico **reale** `solve_risch_de_parametric_field` (`risch_rde_bronstein.cpp:249`) è **già implementato**: denominator bound (Q,B,D), trasformazione `f_new=f−D'/D`, `g_new=g·D`, bound di grado N (Log/Exp, §6.4), ricorsione tower `solve_recursive` con correzioni exp/log. Trial constants **già rimossi** (grep vuoto).
  - **GAP REALE A1** = ramo **`df > 0`** a `risch_rde_bronstein.cpp:362-368`: `return Unimplemented("df > 0 not implemented in parametric field solver")`. Tutto `df ≤ 0` funziona; manca il caso *non-cancellation* in cui `deg_t(f) > 0` (il termine `f·y` domina): `deg(y)=dg−df`, `lc(y)=lc(g)/lc(f)`, poi ridurre+ricorrere (Bronstein 6.5/7.4/8.4 PolyRischDE).
- **Cleanup 2026-06-27**: dead-code scaffold `risch_rde_bronstein_hermite.cpp` **rimosso** (file + CMakeLists); diagnostic del ramo df>0 reso strutturato (Bronstein §6.5/§7.4/§8.4). 69/69 Risch verdi.
- **🚧 BLOCCO alla chiusura (verità da REGOLA ZERO)**: il ramo df>0 è **irraggiungibile** dall'intero corpus + 16 integrandi-torre costruiti a mano (verificato instrumentando il sito). Senza un caso-test che lo eserciti non c'è verifica possibile → implementarlo alla cieca = codice non verificabile sul hot-path Risch (silent-wrong peggio di Unimplemented).
- **🔬 CAUSA RADICE TROVATA 2026-06-29**: l'irraggiungibilità è **strutturale**, non di corpus. `solve_risch_de_parametric_field` (host del sito df>0) è **dead code: zero caller esterni**, solo auto-ricorsione (grep tree-wide + graph). Il df>0 non sarà mai raggiunto finché la funzione non viene **wired-in** dal path integrate reale. Estratto come task prerequisito **A26** (`[E4·C4·S3·R2]`, deciso 2026-06-29). A1 resta BLOCCATO **fino al completamento di A26**; solo allora df>0 ha un caso-test verificabile (Bronstein §6.5 + cross-check Maxima).
- **Spec (verificata 2026-07-01)**: spec algoritmica **self-contained** = `MISSING_FEATURES_SPECS/Symbolic_Integration_I.md` (testo Bronstein completo con dimostrazioni): **§6.4 SPDE** (Rothstein, algo `SPDE`), **§6.5 The Non-Cancellation Cases** (`PolyRischDENoCancel1/2/3`, Lemma 6.5.1 — dà `deg(q)=deg(c)−deg(b)`, `lc(q)=lc(c)/lc(b)` = esatto per df>0), **§7.1 The Parametric Risch Differential Equation** (`ParamRischDE` + *Parametric SPDE* + *Parametric Poly Risch d.e. – no cancellation*, righe ~7078/7190) = il target reale del df>0 **parametrico**. `Risch_Transcendental_Cap8.md` + `Risch_Hermite_Cap5.md` = solo riassunti high-level (puntano al libro, NON self-contained). ⚠ **Correzione citazione**: "§6.5/§7.4/§8.4" nei commenti storici è errata — **§7.4 non esiste** (cap.7 = 7.1/7.2/7.3) e **§8.4 = Hypertangent Case** (solo tan/complessi, fuori scope torri log/exp). Ref corretto = **§6.5 + §7.1**.
- **Ledger**: HC-F8-PENDING-17 · **Refs**: T-007, Task 17, F5.1, F7.5.B3, CAS-L1-02

### A2 · Hard-timeout cancellation token su tutti i path integrazione — RISOLTO 2026-06-26 — `[E2·C2·S5·R2]`
- **Stato a codice**: ✅ chiuso. L'ultimo loop combinatorio non interrompibile dell'integrazione (le due eliminazioni di Gauss pure-Rational in `integrate_risch_rde.cpp`) ora polla `check_interrupt()` per colonna pivot; rimosso lo stub morto del poll mai cablato; dispatcher `solve_risch_de_q` reso fedele alla cancellazione (non ingoia più il Timeout di simplify, non maschera l'interrupt come Unimplemented). +2 test in `test_integrate_interrupt.cpp` (RDE solver level). 147 test Risch/integrate verdi.
- **Residuo non bloccante**: fedeltà error-kind su interrupt asincrono *durante* `parse_polynomial` interno a un ramo (rietichettato Unimplemented, finestra ~µs, nessun hang) — vedi ledger HC-F70-A33.
- **Ledger**: HC-F75-A3-HARD-TIMEOUT (chiuso), HC-F70-A33-POLL-COVERAGE (aggiornato 2026-06-26) · **Refs**: T-015, T-024

### A3 · Perf-hang fattorizzazione torri / VanHoeij — ✅ FATTO (correttezza 2026-06-29, perf residua chiusa via A25 2026-07-10) — `[E3·C4·S2·R2]`
- **FATTO 2026-06-29** (analisi `sample` + instrumentazione `[WANG]`, spec `Hensel_Lifting.md` letta): la diagnosi vecchia ("suspect 5c72bc0 / together() GCD") era **ERRATA**. Tre fix reali, zero debito, zero hardcode:
  - **(1) LLL intero fraction-free** (`lattice_lll.cpp`, Cohen 2.6.7 / de Weger): la Gram-Schmidt `Rational` rifatta per-swap esplodeva in frazioni sui reticoli van Hoeij. Ora d[i]/λ[i][j] interi Hadamard-bounded; fallback Rational solo per basi rank-deficient. 25 test LLL/VanHoeij correttezza verdi.
  - **(2) Kronecker cancellabile** (`factorize_kronecker.cpp`): per norma torre perfect-power il recombination cade su ricerca combinatoria esponenziale che non pollava cancellazione → hang. Ora polla `ctx.past_hard_deadline()` (deadline **opt-in** del chiamante; default illimitato per non rompere factor lente-legittime, es. Trager Q(∛2) 27s) + propaga Timeout. `factor_polynomial_tower[_n]` pubblica `set_hard_deadline(now()+ctx.timeout())`.
  - **(3) Verdetto irriducibilità lucky-prime** (`factorization_wang_eez.cpp`, HC-F8-SD3-VANHOEIJ-SLOW ✅): su primo **lucky** (`f` squarefree mod p, check `gcd(f,f') mod p` costante) la recombination **Hensel esaustiva** è completa → "nessun fattore" = prova di irriducibilità → ritorna `{f}` invece di cadere su Kronecker illimitato. Si fida **solo** del verdetto esaustivo (NON di van Hoeij, incompleto) e **solo su lucky prime**. Un primo tentativo "trust van Hoeij" era stato revertito (silent-wrong su RedundantGenerator) — il bug era saltare il fallback esaustivo; risolto.
  - **Esito**: quarantena 7→4. SD3 **219ms** (era hang >400s, rimosso da SLOW_OK). AntiHardcode 3.2s, PreservesLeadingCoeff 5.8s, IrreducibleX2Minus7 3.5s. **243** factor/poly/LLL/Galois/Trager + **352** consumer (integrate/simplify/solve/residue) verdi, **0 regressioni**.
- **✅ RESIDUO CHIUSO 2026-07-10**: il perf deg-16 era tracciato come **A25** ed è stato risolto dal best-prime Zassenhaus (vedi A25). A3 è ora chiuso su entrambi i fronti.
- **Ledger**: HC-F8-FACTORIZATIONTOWER-PERF (✅ RISOLTO 2026-07-10), HC-F8-FACTORIZATIONTOWER-AntiHardcode-X2Minus2-Sqrt3Sqrt5 (✅ RISOLTO), HC-F8-SD3-VANHOEIJ-SLOW (✅ RISOLTO) · **Refs**: A25, BUG-HANG-002

### A4 · Smith Normal Form su Q[x] PID generale — ✅ FATTO (verificato 2026-06-26, stale) — `[E3·C3·S3·R2]`
- **Stato a codice**: già implementato in `src/linalg/matrix_smith_qx.cpp` (`smith_normal_form_qx`): algoritmo PID Bezout-based completo (row/col ops con `polynomial_bezout` + `polynomial_exact_divide`, catena divisibilità, normalizzazione monica). Dispatch automatico da `matrix_smith.cpp:163` quando entrate single-var poly. I citati `:166/:216` sono il bail multivar (correttamente NON-PID) e il guard Z-path — non gap. Q[x] è Euclideo ⇒ no Storjohann/LLL necessario. Coverage rafforzata 2026-06-26: +test non-diagonale `[[x,1],[−1,x]]` → `diag(1, x²+1)` con certificato `U·A·V==S`. 4/4 SmithQxTest.
- **Refs**: F4.2, CAS-L2-04

### A5 · ODE Frobenius: log-term + resonance (radici differing-by-integer) — ✅ FATTO 2026-07-20 (residuo secondary-resonance provato IRRAGGIUNGIBILE per il 2° ordine) — `[E3·C3·S3·R1]`
- **Stato a codice**: ✅ gap principale chiuso: **radice indiciale doppia** (gap N=0) ora produce la seconda soluzione logaritmica via costruzione parametro-derivata `y₂ = ln(x)·y₁ + x^{r1}·Σ aₙ'(r1) xⁿ` (`build_double_root_log_branch`, recurrence con root simbolico ρ + `d/dρ` + sub ρ=r1). Prima `series_solutions.size()==1` restituiva solo `C1·y₁` (soluzione generale incompleta). Verificato: Euler `x²y''−xy'+y=0` → `C1·x + C2·x·ln x`; Bessel₀ `x²y''+xy'+x²y=0` → `J₀·ln x + x²/4 − 3x⁴/128…`. +2 test, 6/6 FrobeniusTest. Gap intero positivo (`build_log_branch`) + resonance detection già presenti.
- **✅ FATTO 2026-07-07**: **punto singolare regolare a x₀≠0** chiuso — nuovo entry-point `solve_ode_frobenius(a2,a1,a0,x,x_0,n,ctx)` (`ode.hpp` + `ode_solver_frobenius.cpp`): shift `x→u+x₀` (fresh symbol), solve a 0, back-substitute `u→x−x₀`. Test `OdeFrobeniusTest` 3/3 (Bessel ν=1 log-branch, Legendre a x₀=1 double-root, Euler shiftato x₀=2). ⚠ **Audit anti-silent-wrong**: una bozza intermedia aveva sostituito i guard di risonanza con "pick 0" silenzioso (`compute_recurrence` RHS≠0, `build_log_branch` secondary resonance) — **RIPRISTINATI** come `Unimplemented` strutturati (REGOLA ZERO): pick-0 resta solo per RHS=0 / S_total=0 (vero parametro libero).
- **✅ CHIUSO 2026-07-20 (residuo = non-gap provato)**: la "risonanza secondaria multi-livello" NON può occorrere in un solver Frobenius di **2° ordine**. L'indiciale è la quadratica `I(ρ)=ρ²+(p0−1)ρ+q0` con esattamente due radici; il log-branch è invocato solo quando differiscono per un intero positivo `N` (`r_large=r_small+N`). Il denominatore della ricorrenza è `I(n+r_small)=(n+r_small−r_large)(n+r_small−r_small)=(n−N)·n`, che si annulla SOLO a `n=0` e `n=N`. `n=N` è la risonanza primaria (già gestita: `c·h_0=−S_N`); per ogni `n∈{1,…}\{N}` vale `denom≠0`. Una risonanza secondaria a `n>N` richiederebbe un TERZO zero indiciale — impossibile per una quadratica. L'ansatz a singolo log è dunque completo per il 2° ordine (teoria classica: due soluzioni indipendenti, al più una logaritmica). Il guard `Unimplemented` resta come rete di sicurezza sound (raggiungibile solo estendendo a ODE di ordine ≥3, fuori scope A5). **Prova documentata in-linea** a `ode_solver_frobenius_series.cpp`; nuovo test `IntegerGap3_LogBranchNoSecondaryResonance` (gap N=3, log reale `c=−1/12`, esercita la ricorrenza oltre n=N senza toccare il guard). 10/10 Frobenius/OdeFrobenius PASS. **Refs**: F5.3, CAS-L2-01

### A6 · Stauduhar Galois group deg ≥ 6 — ✅ FATTO 2026-07-15 (inc.1-5 + Brick 1-4 deg 8..10: perm-engine + lattice + driver esatto deg 6-7 + BSGS + massimali on-demand + passo Stauduhar p-adico certificato + walk identificazione completa + massimali strutturali wreath-preimage 5|2 + naming strutturale + wiring pubblico `galois_group` deg 8..10 + corpus ≥30) — `[E4·C4·S3·R1]` — **HC-F8-PENDING-09 CHIUSA**
- **✅ Inc 1-2 FATTI 2026-07-09** (commit `5160503`, spec `Galois_Groups.md` letta): motore permutazioni esatto (`src/algebra/perm_group.cpp`: Lehmer rank S_n↔[0,n!), chiusura BFS con budget, orbite su k-subset, distribuzione cycle-type) + `transitive_subgroup_classes` (`galois_transitive_lattice.cpp`): TUTTI i sottogruppi transitivi di S_n a meno di coniugio via estensione single-generator dal gruppo banale con pruning double-coset — completezza **per costruzione**, ZERO tabelle trascritte (anti-allucinazione REGOLA 0.1; i conteggi classici Butler-McKay sono solo oracoli di test). Verificato: deg4→{C4,V4,D4,A4,S4}, deg5→ordini {5,10,20,60,120}, deg6→16 classi (11.2s ASan). 17 unit test verdi (incl. AGL(1,7) e PGL(2,5) su P¹(F₅) costruiti da principî primi).
- **✅ Inc 3-4 FATTI 2026-07-09** (driver deg-6 esatto): `galois_group_sextic_irreducible` (`src/algebra/galois_deg6.cpp`) + machinery set-resolvent (`galois_setpoly.cpp`). Pipeline: (a) modello monico intero, (b) parità discriminante ⇒ ⊆A₆, (c) sieve Dedekind/Frobenius (cycle-type mod p, witness positivi), (d) risolventi k-set **via power sums (Newton) — NIENTE resultant**: R_k(y)=∏(y−Σ_T α) da EGF di e_k(e^{sα_i}), certificato q_0=C(n,k); fattorizzate su Z ⇒ orbit-length multiset (Soicher-McKay), match col lattice generato. Collisioni → `tschirnhaus_general` (base di potenze β=P(α), completezza Vandermonde, bound sweep derivato). Ambiguità → `Unimplemented` strutturato (REGOLA ZERO). Verificato: Φ₇→C6 (20.7s), Φ₉→C6 (10.5s, richiede transform grado≥3), x⁶−2→D6 (16.5s), x⁶−x−1→S6 (0.1s), riducibile→ricorsione fattori. 5/5 GaloisDeg6Test + 7/7 GaloisSetpolyTest verdi (<60s cap ctest). Dispatch deg-6 in `galois.cpp`.
- **🩹 BUGFIX SISTEMICO (found via A6)**: recombination Hensel scartava i fattori modulari di grado > n/2 dal pool ⇒ complemento `left·right ≢ f (mod p)` ⇒ `hensel_lift` produceva garbage ⇒ **composto dichiarato irriducibile in silenzio** (il noto "recombination misses factors"). Fix in `factorization_recombination.cpp`: ricombina sui fattori GIÀ lifted (`∏ ≡ f mod p^a`, invariante di `hensel_lift_multi`), no re-lift per subset. + **Zassenhaus best-prime** in `factorization_wang_eez.cpp`: scan primi lucky, scegli quello con MENO fattori modulari (deg-20 mod 73 → 18 fattori/217s vs mod 79 → 8 fattori/4ms). Regression test in `test_factorization_polynomials_smoke.cpp`.
- **✅ Inc 5 FATTO 2026-07-10** (deg-7): driver reso **n-generico** — `galois_group_irreducible_resolvent` (n ∈ {6,7}, altrimenti `Unimplemented`), memo lattice keyed-by-n, `binomial(n,k)`, `structural_name(g,n)` (deg-7: i 7 ordini sono a due a due distinti ⇒ l'ordine determina la classe); dispatch deg 6+7 unificato in `galois.cpp`. **Lattice S₇ ottimizzato 740s → 23.2s (31.9×)**, S₆ 11.3s → 0.45s (25×), classi/ordini invariati: 3 interventi in `galois_transitive_lattice.cpp` — (a) `seen`-set esatto degli element-set di tutti i coniugati delle classi accettate, che sostituisce lo scan di coniugio O(n!) per candidato (era il 74% del runtime) con un lookup hash; (b) `closure_ranks` rank-only ⇒ il `PermGroup` si materializza solo per classi nuove (16237 candidati → 96 materializzati); (c) tabella di Cayley su rank Lehmer costruita senza allocare un `Perm` per entry, via LUT perfect-hash a 3 bit/immagine. Verificato: S₇ → 7 classi ordini `{7,14,21,42,168,2520,5040}` (= Butler-McKay, usato solo come oracolo di test). Driver: `x⁷−x−1` → **S7** (Osada, 23.6s), periodo di Gauss di Q(ζ₂₉) `x⁷+x⁶−12x⁵−7x⁴+28x³+14x²−9x+1` → **C7** (disc = 414613², 26.7s). Entrambi sotto il cap ctest 60s ⇒ nessun SLOW_OK. 34 test verdi (deg5/6/7 + lattice + perm-group + setpoly).
- **✅ Inc 6a / Brick 1/4 FATTO 2026-07-11** (deg ≥ 8, engine scalabile): nuovo `BsgsGroup` (`src/algebra/perm_bsgs.cpp` + `perm_bsgs_internal.hpp`) = **Schreier-Sims deterministico verificato** (Holt-Eick-O'Brien §4.4). Rimuove il muro Θ(n!) del lattice denso: rappresentazione base + strong generating set + transversali, spazio/tempo polinomiale, ordine e membership **esatti** (NO Monte-Carlo). Cross-check completo vs `PermGroup` denso su tutto S_n per n ≤ 7; scala n=8 (S_8/A_8/C_4≀C_2 in ms). 9 test verdi. Path deg 6/7 non toccato. **Correzione di rotta**: il PLAN Task 9 (GA-1 tabelle Hulpke + GA-2 BigFloat) è stale — direzione approvata = estensione **float-free** stile Fieker-Klüners 2014 (discesa esatta razionale/p-adica), zero tabelle, zero float. Piano: `~/.claude/plans/drifting-sprouting-walrus.md`.
- **✅ Inc 6b / Brick 2/4 FATTO 2026-07-11** (massimali transitivi on-demand, primo strato di discesa): `maximal_transitive_candidates(ambient, n)` per S_n/A_n, 5 ≤ n ≤ 10 (`perm_maximal.cpp`), assemblato **dall'aritmetica di n** (divisori → S_a≀S_b; p^d → AGL(d,p) da ⟨trasvezione, ciclo, toro⟩; q+1 → PSL/PGL/PΓL(2,q) su P¹(F_q) con GF(p^e) costruito a runtime, irriducibile certificato Rabin; (p^d−1)/(p−1) → PGL(d,p) proiettivo; C(m,k) → azioni sui k-subset) + parti pari via lemma di Schreier + **twin (0 1)-coniugato** per le classi pari senza certificato di non-split in A_n (le 2 classi di AGL(3,2) in A_8, di PΓL(2,8) in A_9). Supporto: `perm_blocks.cpp` (sistemi di blocchi minimali di Atkinson, primitività, azione sui blocchi), `perm_construct*.cpp`. **Certificato dai test**: cross-check esaustivo n=5,6,7 contro il lattice generato (soundness + **copertura con σ ∈ A_n su entrambi i rappresentanti di A_n-classe** + tripwire quasi-massimalità); struttura n=8,9,10 vs formule d'ordine classiche. Il pruning per contenimento ha scoperto che (S_2≀S_4)∩A_8 (192) ⊂ AGL(3,2) — coerente con ATLAS. 22 nuovi test verdi (blocks 7 + construct 9 + maximal 6); `CrossCheckDegree7BothAmbients` in SLOW_OK (~45s ASan). Copertura 8 ≤ n ≤ 10 = teorema di enumerazione (Sims 1970, Butler-McKay 1983) citato, da cross-validare col corpus Brick 4.
- **✅ Inc 6c / Brick 3/4 FATTO 2026-07-12** (passo Stauduhar certificato, primo strato di discesa deg 5..10): tre layer nuovi, tutti esatti/float-free. (a) **Motore p-adico** `galois_padic_{ring,roots,split}.cpp`: GR(p^k, L) = (Z/p^k)[t]/(Φ) con Φ = fattore deg-L di f mod p o sweep Rabin esaustivo; lift Newton/Hensel delle radici; primo unramified scelto con gcd(f,f′) su F_p esplicito (la forma dell'output del fattorizzatore mod p NON segnala la ramificazione — bug preso dai tripwire) minimizzando L = lcm dei gradi; CZ Las Vegas (seed da input/ctx.rng()); certificati a ogni build/raise (f(r)≡0, distinzione mod p, e₁/eₙ Newton, ciclo-tipo Frobenius = gradi mod p). (b) **Invarianti relative** `galois_invariant.cpp`: Stab_G(F) = H **certificato per conteggio** (|Orb_G(F)| = [G:H]); tier k-subset orbit-sum + fallback garantito monomio di Galois; transversal sinistro con reps[0]=id; H non assunto massimale. (c) **Passo + driver** `galois_stauduhar_{step,descent}.cpp`: R(y)=∏(y−v_i) mai espansa — R(c), R′(c) solo su residui puri via prefix/suffix O(m); precisione derivata dai bound di Cauchy via bit-length; screening two-phase (impurità a bassa precisione certifica ∉ Z → 36s→2.9s misurato su x⁸−x−1); radice intera esatta E semplice (R′(c)≠0) ⇒ discesa certificata con tripwire Frobenius ∈ σHσ⁻¹; radice multipla → Tschirnhaus sul modello (β=P_t(α) sulle stesse radici liftate, ordine/Frobenius preservati, sweep bound derivato); collisione a radice semplice → raise con cap dimostrato. Primo strato: parità disc → S_n/A_n, candidati Brick 2 (twin, σ nel gruppo corrente), nessun contenimento ⇒ **G_f = ambiente esatto** (copertura Brick 2). **Cross-check vs pipeline deg 6/7 indipendente** (ambiente ⟺ S/A, proprio ⟺ discesa: Φ₉, x⁶−2, x⁶−x−1, x⁷−x−1, C7-ζ₂₉) + **primi statement deg-8 certificati**: Φ₁₆ scende sotto A_8, x⁸−x−1 = S_8. 18 test nuovi verdi (padic 8 + invariant 5 + stauduhar 5); `CrossCheckDegree{6,7}` in SLOW_OK (dominati dal pipeline-oracolo). Bug di design trovato dai test e corretto (non nascosto): coset distinti ⇏ valori distinti — pretesa di distinzione globale rimossa, criterio radice-semplice al suo posto.
- **✅ Inc 6d / Brick 3.5 FATTO 2026-07-13** (walk sotto il primo strato = **identificazione completa** `stauduhar_identify`): (a) `galois_sublattice.cpp` — classi transitive **MASSIMALI** dentro un nodo interno a meno di coniugio nel nodo (Cayley u16 denso + chiusure single-generator, completezza per costruzione; filtro di massimalità esatto sui coniugati ⇒ indici [H:K] piccoli ⇒ risolventi/precisione p-adica sane); doppio budget ops + **bytes** (nuovo `CASContext::galois_sublattice_max_bytes`, 256 MiB, speso PRIMA di allocare). (b) `galois_stauduhar_below.cpp` — loop di discesa: nessun massimale transitivo contiene ⇒ **G_f = nodo ESATTO** (ogni transitivo proprio sta in un massimale transitivo); tripwire Frobenius a ogni passo; splitting unico per la catena. (c) **Bug matematico trovato dai test e corretto**: famiglia Tschirnhaus 1-parametro P_t=Σt^{m−1}x^m DEGENERE (c_a·c_b funzione di a+b: su x⁵−2 sotto F₂₀ la differenza di coset ∝ (c₁c₄−c₂c₃)√5 ≡ 0 su tutta la curva) → sweep su **griglia** c ∈ {0..Δ}ⁿ a gusci di norma-max (c₀ via Taylor-shift esatto), separatori ≢ 0 per interpolazione di Lagrange, terminazione **dimostrata** via Combinatorial Nullstellensatz (esaurimento griglia = InternalError). Verificato: deg 5 **tutte e 5 le classi** (120/60/20/10/5, passi esatti), C₇ (ordine 7), Φ₉→C₆, x⁶−2→12, **primi gruppi deg-8 completamente identificati**: Φ₁₆→8 (~5s), x⁸−2→16 (~25s, SLOW_OK; splitting = Q(2^{1/8},i) deg 16). 10 test nuovi verdi (sublattice 5 + identify 5). Nodi wreath 5|2 deg-10 (28800/14400) oltre il byte-budget → **Unimplemented strutturato**.
- **✅ Inc 6e / Brick 3.75 FATTO 2026-07-14** (massimali strutturali dei nodi wreath-preimage 5|2 deg-10, oltre il byte-budget denso): `galois_wreath_maximal.cpp` (+`_internal.hpp`) — nodo certificato come φ-preimage (φ: S_d≀S_k → C₂≀S_k quoziente dei segni; certificato esatto |H| = |Q_H|·(d!/2)^k sul block system minimale) e **teorema di copertura dimostrato nell'header** (d ≥ 5, d ≠ 6, k primo, ∃ p dispari p|d p>k): ogni massimale transitivo di H sta in FA (lift dei massimali di Q_H, enumerati esaustivamente nel quoziente ≤ 2^k·k!) ∪ FB ((K≀S_k)^t∩H, shell Brick-2, twins su trasversale destra H·T = W dimostrati sufficienti) ∪ FD (N_W(Δ^t)∩H — lemma di Scott 1980, A_d semplice non-abeliano). Intersezioni esatte via φ, mai enumerazione di H; ipotesi mancante → Unimplemented che la nomina. `galois_sublattice.cpp` rifattorizzato (core condiviso + `maximal_subgroup_classes_in` + `all_subgroup_classes_in` + `dense_sublattice_min_bytes`); dispatch deterministico `node_maximal_transitive_classes` (denso se nel budget, identico al 3.5; strutturale oltre). First layer riordinato per ordine decrescente = risolvente crescente (il miss S₂≀S₅ da 945 coset costava >30 min ASan; l'ordine non cambia mai il gruppo identificato). Colli di bottiglia del passo MISURATI e chiusi con soluzioni generali: tier j-tuple in `galois_invariant.cpp` (il k-subset non separa i kernel di caratteri-segno; ultimo livello = monomio di Galois, terminatore invariato) e criterio del sottodiscriminante per candidati index-2 (`galois_stauduhar_quadratic.cpp`: forme δ alternanti valutate come prodotto, D = δ(r)² intero certificato, contenimento ⟺ D quadrato — fast-path con via generale sempre presente). 10 test nuovi (liste pinnate sulla teoria: W→{14400,14400,800}, kerχ₁→{7200×2,400,240×2 twin essenziale}, ∩A₁₀→solo 400; micro-check di Scott su A₅×A₅ e shell N_{S₅}(D) su ground truth densa) + E2E deg-10 x¹⁰+2x⁷+x⁴−2 → S₅≀S₂ = 28800 in 17.5 s, derivato indipendentemente (disc/N non quadrati + testimoni Frobenius mod 47/103 via Goursat).
- **✅ Inc 6f / Brick 4/4 FATTO 2026-07-15** (naming + wiring pubblico + corpus): `src/algebra/galois_deg8.cpp` — `structural_transitive_group_name(BsgsGroup)` deriva l'etichetta SOLO da invarianti certificati (S_n/A_n per ordine+parità; regolari → C_n o `Ab<n>_exp<e>` con (ordine,esponente) invariante completo per n≤10; imprimitivi → `S<s>wrS<b>` per certificato d'ordine G≤Stab(blocchi)=(s!)^b·b!, altrimenti `Im<n>[s^b]_<ord>`; primitivi → `P<n>_<ord>`) — mai tabelle (CLAUDE.md §8). `galois_group_irreducible_deg8_to_10` + branch `[8,10]` in `galois.cpp` (expr→modello monico intero→`stauduhar_identify`→naming; riducibili per ricorsione). Corpus `test_galois_deg8.cpp`: 6 unit naming su gruppi COSTRUITI (`perm_construct`, etichetta nota dalla costruzione) + **30 E2E** con oracoli indipendenti (Osada S8/S9, ciclotomiche (Z/m)^*, x⁸−2, x¹⁰ full-wreath S5≀S2, 21 riducibili con fattori certificati). 5 E2E in SLOW_OK; deep deg-9/10 (>90 s) esclusi dal corpus = soffitto perf noto/documentato, non correttezza. deg ≥ 11 fuori scope A6 → `"unknown"`.
- **Nota architetturale**: approccio **interamente esatto e generato** (niente float, niente trascrizioni) anche per n ≥ 8 — Fieker-Klüners esatto, NON lo Stauduhar-con-radici-BigFloat della voce storica/spec. `HC-F8-PENDING-09` **CHIUSA 2026-07-15** (deg 6/7 esatti + deg 8..10 end-to-end).
- **Spec**: `MISSING_FEATURES_SPECS/Galois_Groups.md` · **Refs**: T-010, Task 9, F3.6, CAS-L3-18

### A7 · Slater pFq → Meijer G fallback integrator — ✅ CHIUSO 2026-07-21 (step 1-5 + Mellin §6.7 + gamma-incompleta §5.9) — `[E4·C4·S3·R1]`
- **✅ GAMMA-INCOMPLETA §5.9 (2026-07-21, commit `099a3ca`)**: costruito il prerequisito mancante (builtin gamma-incompleta inesistente) poi i recognizer — REGOLA ZERO. Builtin `Γ(a,z)`/`γ(a,z)` 2-arg (`BuiltinOp::GammaIncomplete`/`GammaIncompleteLower`, nomi allineati Maxima `gamma_incomplete`/`gamma_incomplete_lower`): parse/print round-trip, term_order tier Γ/ψ/B (86/87), derivate DLMF 8.8.13 (`d/dz Γ=−z^{a−1}e^{−z}`, `d/dz γ=+z^{a−1}e^{−z}`; w.r.t. ordine a → Unimplemented strutturato). Ponte Meijer-G §5.9: `to_meijerg` (`Γ=G^{2,0}_{1,2}(z|1;0,a)`, `γ=G^{1,1}_{1,2}(z|1;a,0)`) + `from_meijerg` tabella inversa (shape `G^{2,0}_{1,2}`→Γ; shape `G^{1,1}_{1,2}(·|1;b0;0)` condivisa con erf: erf resta caso elementare b0=1/2+arg quadrato, altrimenti →γ; estrazione ordine order-insensitive sul sotto-gruppo b canonicalizzato dalla factory). **Gate**: meijerg+diff+mellin 82/82, golden ratchet 934/10 (zero regressione — §5.9 non tocca il corpus), quick **2821/2821**, hardcode-auditor pulito (1 finding cosmetico Cat.5 rank term_order corretto in-place). Argomento lineare in z ⇒ compatibile anche con convoluzione Mellin §6.7. **A7 COMPLETO** — restano solo debiti follow-up non gating: A34 (canary canonicità), estensione `from_meijerg` per `G^{1,1}_{1,3}` trig·potenza (policy §9.4).
- **✅ MELLIN §6.7 (2026-07-21, ereditato via stale-work-recovery: WIP fermo dalla sera prima, mai buildato/committato)**: `integrate_mellin_convolution.cpp` (378 righe) + `pattern_mellin_g_convolution` registrato in `definite_patterns()` — chiude `∫_0^∞ K·x^μ·f₁·f₂ dx` quando ogni f_i → Meijer G lineare in x (`to_meijerg`), via convoluzione Adamchik–Marichev/Meijer (1941) con composizione parametrica §6.7. **Soundness gate** (rischio unico §6.7 = formula esatta su integrale DIVERGENTE → valore finito SBAGLIATO): committata SOLO sotto certificato di convergenza a entrambi gli estremi (asintotica Meijer G, parametri razionali ⇒ decidibile); fuori certificato → skip → Unimplemented, mai errato (REGOLA ZERO). Shadow-eval numerica mpmath nel test layer. **Completamento sopra l'ereditato**: parametrizzati via CASContext i 2 hardcode del fallback `1F₁` (evaluator.cpp) trovati da hardcode-auditor — cap iter serie Kummer (Cat.1) + tol relativa (Cat.2), sul precedente `integration_max_intervals_`/`integration_rel_tol_` (F6.D); via corretta, nessun ledger. **Gate**: Mellin 6/6 (4 valori + 2 divergenze rifiutate), numeric 36/36, AcidTest 23/23, quick **2814/2814**, benchmark senza regressione (hot-path non toccato). Commit `898e199`. LEZIONE: il WIP ereditato era corretto e completo ma mai eseguito — build+gate+parametrizzazione hardcode erano i pezzi mancanti.
- **✅ STEP 5 (2026-07-19, ripreso via stale-work-recovery: WIP fermo 36h completato)**: `meijerg_identities.cpp` (§6.1 inversione, §6.2 power shift, §6.3 cancellazione a fixpoint, §6.6 antiderivata h=−1 — builder θ-shift condiviso) + `integrate_meijerg.cpp` fallback POST-Risch per famiglia `K·x^μ·f(c·x^r)` (u-sub t=x^r → shift §6.2 → antiderivata §6.6 → expand/Slater) + **fold Mellin half-line**: `(c·x^r)^s → c^s·x^{rs}` con condizione **`Positive(var)` via A31** quando non banalmente esatto (spec §4 — mai silent-wrong sul semiasse negativo) + 2 chiusure 1F1 esatte (Kummer 1F1(1;2;z), erf 1F1(½;3/2;−u²), DLMF 13.6.1/7.6.2) + **derivata `d/dz G` (§6.5 h=+1, DLMF 16.19.5)**: formula certificata mpmath (3 shape × 4 punti irrazionali, err<10⁻⁴¹), `meijerg_derivative_shift` + case in `differentiate_rules.cpp` con chain rule `(u′/u)·G_shift` — le antiderivate G di prima classe ora si verificano per differenziazione. Split anti-monolito: `integrate_rational` → `integrate_rational_part.cpp` (integrate_core 511→~430). **Flagship**: `∫e^{−x²}dx → (√π/2)erf(x)` diff-verified + `Positive(x)` registrata; `∫J_{1/3}` resta G di prima classe (§9.4); `∫x²e^{x³}`, exp-quartica: G-form diff-verified. Test aggiornati alla nuova semantica (`RejectsNonElementaryCaseForNow` → `SolvesGaussianViaMeijerGFallback`, era "ForNow" by-design). **Gate**: 9/9 nuovi, quick **2797/2797**, regression 26/26, benchmark 6/6 (0 regressioni), hardcode audit 0 violazioni. LEZIONE: il WIP ereditato era corretto ma MAI eseguito (test non registrati in CMake) — la registrazione era il pezzo mancante.
- **🔧 POLICY §9.4 RAFFINATA (2026-07-19, stesso giorno)**: il golden ratchet (NON coperto dalla quick!) è andato rosso 11>10 — il fallback restituiva G-form per `∫x³cos(x²)dx` dove esiste forma elementare (Maxima: `½x²sin(x²)+½cos(x²)`): downgrade di qualità, non silent-wrong. Fix: G-form = risultato first-class SOLO se l'integrando contiene già nodi del mondo speciale (BesselJ/pFq/G — predicato `contains_special_fn_node`, esteso insieme alla tabella §5); su integrandi puramente elementari G non piegata → `Unimplemented` strutturato (skip pulito come pre-A7). Ratchet di nuovo verde 934/10; quick 2797/2797. LEZIONE: dopo modifiche a integrate girare SEMPRE anche `check_golden_ratchet.sh`, la quick non lo include. RESIDUO A7 (oltre §5.9/§6.7): estendere `from_meijerg` per piegare `G^{1,1}_{1,3}` (famiglia trig·potenza) e poi rilassare la policy.
- **✅ STEP 1 (2026-07-15, b3ee580)**: `BuiltinOp::MeijerG` + name-mapping + round-trip parse/print + precedenza term_order 91.
- **✅ STEP 2 (2026-07-15, 21fc2eb)**: `make_meijerg`/`view_meijerg` (`include/cas/meijerg.hpp`, `meijerg_construct.cpp`) — validazione m/n/p/q, pole-overlap esatto-quando-decidibile, budget `meijerg_max_param_count`, canonicalizzazione per sotto-gruppo (Regola 2). 12 test.
- **✅ STEP 3 (2026-07-18)**: `meijerg_convert.cpp` — `to_meijerg()` tabella §5 (11 recognizer: exp/E^w, sin, cos, sinh forma reale primaria, cosh, ln(1+z), (1+z)^{-a} con guardia Γ-polo, w^a·e^{-w} fast-path, arctan prefattore ½, arcsin, erf, BesselJ) + **`pfq_to_meijerg()` ponte GENERALE DLMF 16.18.1** (p≤q+1, guardie Γ-polo su α e β decidibili) + dispatch 0F1/1F1/2F1; cross-check ponte-vs-tabella su 0F0=e^z. 21 test nuovi (33 MeijerG totali), quick 2770/2770, benchmark verde.
- **⚠ PREREQUISITO MANCANTE dichiarato**: le entry §5.9 (Γ(a,z), γ(a,z) incomplete) richiedono un `BuiltinOp` gamma-incompleta che NON esiste — da creare (parse/print/term_order/derivate) PRIMA dei loro recognizer. Non aggirato: `to_meijerg` le rifiuta con `Unimplemented` strutturato.
- **✅ STEP 4 (2026-07-18)**: `meijerg_slater.cpp` — **`slater_expand()` path GENERALE DLMF 16.17.2** (Σ_k A_k·pFq; precondizioni p≤q e b-group senza spaziature intere decidibili → poli confluenti rifiutati strutturati; chiusure esatte ₀F₀=exp e ₁F₀=binomio, nodi 0F1/1F1/2F1 altrimenti; arietà senza nodo motore → Unimplemented, la G resta) + `meijerg_from.cpp` — `from_meijerg()` tabella §5 inversa shape-gated (trig/iperboliche/BesselJ/arctan/arcsin/erf/ln; ln = SOLO tabella come da §5.4) con fallback Slater, e `expand_meijerg_nodes()` (map su albero, G inespandibile resta intatta per puntatore §9.4). **Convenzione di segno documentata**: round-trip esatto sulla semiretta di Mellin (argomento >0), coerente con la pipeline §9. Fix motore contestuale: canonicalizzazione simmetria ₂F₁(a,b;c;z)=₂F₁(b,a;c;z) (DLMF 15.2.1) in `simplify_special_fn_hyper.cpp`. 18 test nuovi (12 round-trip §10.1 incl. 2 via solo-Slater, §10.6 arctan-G→2√w·₂F₁ e somma m=2, poli confluenti/p>q rifiutati, G-resta-G). Quick 2788/2788, benchmark verde. **A34 aperta** (canary canonicità preesistente, repro `sin(x)/sqrt(pi)`, scoperto qui ma indipendente).
- **Prossimi step**: step 5 = identità §6.1-6.3/6.6 (`meijerg_identities.cpp`) + `integrate_meijerg.cpp` antiderivata §6.6 e wiring POST-Risch (§8-9); poi builtin gamma-incompleta (prerequisito §5.9) e convoluzione Mellin §6.7 (integrali definiti).
- **✅ SPEC CREATA 2026-07-15**: `MISSING_FEATURES_SPECS/Meijer_G_Slater.md` — definizione DLMF 16.17.1 + 3 contorni, ponte pFq↔G (16.18.1), **teorema di Slater 16.17.2 come path generale del lookup inverso** (REGOLA 8), tabella conversioni (13 funzioni), identità 16.19.1-6 + antiderivata + convoluzione di Mellin con composizione parametri esplicita, AST design su `FuncCall`/`BuiltinOp` reale, ≥34 test, vincoli REGOLA ZERO. **Ogni formula verificata numericamente contro mpmath 1.3.0** (15/15 conversioni + 18/18 identità, tol 10⁻¹⁸); **3 errori del materiale grezzo trovati e corretti** (sinh: mancava −i; arctan: prefattore z/2→½; γ(a,z): G^{1,0}→G^{1,1}) — errata in Appendice D. Il grezzo `Special_Fn_Identities_MeijerG.md` è banner-superseded.
- **Stato a codice**: `BuiltinOp::MeijerG` **non esiste**. Nessun file Slater/Meijer.
- **⚠️ CORREZIONE 2026-06-28**: la spec citata in precedenza (`Special_Fn_Identities.md`) **NON è di Meijer-G** — è **F7.5.E1** (identità Γ/B/ζ/erf), task **diverso e di fatto FATTO** (special_fn golden = **100%**, vedi §C). Quindi **A7 non ha spec formale**: scrivere Meijer-G ora violerebbe REGOLA 0.1 ("codice senza spec = INVALIDO") e REGOLA ZERO (formule da memoria = allucinazione). **Bloccante reale**: prima va prodotta/approvata una spec `MISSING_FEATURES_SPECS/Meijer_G_Slater.md` (definizione G, identità Erdélyi-Slater, formula integrale, trasformazioni Bailey, scope) — decisione architetturale, non auto-derivabile.
- **Cosa (sketch ingegneristico, valido SOLO dopo la spec)**: definire `MeijerG`, dispatcher, wiring come passo finale post-Risch.
  - Sequenza incrementale (ogni step = commit + gate verde): (1) enum `BuiltinOp::MeijerG` in `include/cas/builtin_functions.hpp` + round-trip print/parse; (2) rappresentazione `MeijerG[[a_p],[b_q]](z)` param esatti + riduzioni degeneri come simplify rules; (3) bridge `pFq→MeijerG` (Slater) con cross-check Maxima (`hgfred`/`makegamma`); (4) `∫ MeijerG dz` (integrale di G = G con indici shiftati); (5) wiring **ultimo** in `src/calculus/integrate_core.cpp` dopo il fallimento Risch (top-level `src/calculus/integrate.cpp:421`), mai prima.
  - **File chiave**: `include/cas/builtin_functions.hpp`, `src/calculus/integrate_core.cpp`, `src/calculus/integrate.cpp:421`.
- **Spec**: ✅ `Meijer_G_Slater.md` (2026-07-15, verificata numericamente; attende approvazione) · **Refs**: T-013, Task 22-23, F7.A-B

### A8 · Kovacic Case 3 completo (icosaedrico n=12) — ✅ FATTO 2026-07-08 — `[E3·C4·S1·R1]`
- **✅ CHIUSO 2026-07-08** (due fix distinti, vedi ledger HC-KV-06):
  1. **Perf**: `compute_P_sequence` riscritto in `algebra::PolyExpr` (coefficient-array): S, S′, S·θ, S²·r ridotti a polinomio UNA volta; derivata dei P_i (coeff a_i costanti in x) = power-rule shift, non `diff()` generico. Ricorrenza a 13 livelli ora deterministica.
  2. **Root cause correttezza** (il vero blocco, Cat 8): `extract_pole_loc` riconosceva `x−c` solo come `Binary(Sub/Add)`; la forma canonica n-aria `Sum([x,−1])` veniva scartata in silenzio → polo x=1 perso → S sbagliato → S²·r non polinomiale. Fix: estrazione radice algoritmica via `parse_polynomial` (deg 1 → c = −c0/c1), indipendente dalla forma AST.
- **Verifica**: paper Ex.1 (p.23, n=12 icosaedrico A₅) ora produce RootOf(M(ω) deg 12); test rafforzati (`Paper_Example1_p23_N12_Icosahedral` ASSERT ok, niente più SKIP); 11/11 Kovacic PASS. **Ledger**: HC-KV-06 ✅ · **Refs**: T-028, T-009 (parziale), F5.C

### A9 · Tower algebrico ≥3 livelli / multi-β nested non-squarefree — ✅ FATTO 2026-07-10 — `[E3·C4·S2·R2]`
- **✅ FATTO 2026-07-07** (`algebraic_tower_primitive_nested.cpp` riscritto, Cohen §3.6.1-3.6.4/Trager):
  - **Multi-β lift**: `try_nested_lift_min_poly_multi` — quando il RootOf esterno dipende da PIÙ β risolti, collassa i β via `compute_primitive_element`, riscrive f(x, β₁..βₖ) in θ, resultant assoluto `Res_y(minpoly_θ(y), f(x,y))`.
  - **Risoluzione iterativa a punto fisso**: `detect_tower_n_level` ripassa le entry irrisolte finché fa progresso (multi-livello, ordine-indipendente).
  - **Non-squarefree/reducible**: R fattorizzato su Q (`collect_irred_factors_over_q`); selezione del fattore **solo con certificato esatto** di vanishing (`cand_vanishes_at_theta_expr`, simplify→0 letterale). Nessun certificato o R non-squarefree senza fattorizzazione → `Unimplemented` strutturato (audit 2026-07-07: rimosso fallback `all_factors[0]` arbitrario = silent-wrong latente, REGOLA ZERO).
  - Test: `ThreeLevelExtension_Sqrt2_Cbrt3_Sqrt5` (deg 12, 3 generatori indipendenti), `DetectNLevelTower_MultiBetaNested` (α su β₁=√2,β₂=√3). PrimitiveElementTest verdi.
- **✅ CHIUSO 2026-07-10** (catena nested sequenziale): la diagnosi storica era imprecisa. Il problema non era "3 generatori dipendenti sono troppi" ma che `compute_primitive_element` fonde con `expected_deg = deg(q)·deg(m_k)`, assumendo **indipendenza algebrica**. Misurato su β₁=√3, β₂=√(2+√3), α=√(1+√(2+√3)) — dove Q(β₁,β₂,α)=Q(α) ha grado 8 — il merge α(8)+β₂(4) costruisce un risultante di **grado 32**, lo fattorizza e ne estrae il fattore giusto di grado 8 in 105s; il merge successivo (grado atteso 16) non termina.
  - **Fix** (`algebraic_tower_primitive_chain.cpp`, nuovo): una torre nested ha ogni livello definito da un polinomio **lineare nel genitore** (`β₂=RootOf(z²−2−β₁)` ⇒ `β₁=β₂²−2`). Con `p(z)=A(z)+B(z)·g` si ricava `g = −A(c)·B(c)⁻¹` in `Q[y]/(M)`, `M` = minpoly del generatore di grado massimo. Nessun risultante: θ=α e gli altri diventano polinomi in θ (`β₂=α²−1`, `β₁=α⁴−2α²−1`). **Certificato esatto** obbligatorio (`m_i(rep_i) ≡ 0 mod M`); struttura non-catena, livello non-lineare o certificato fallito → `nullopt` e fallback al merge Trager generico. Nessun silent-wrong possibile.
  - **Verificato**: `DetectNLevelTower_ThreeLevelNested` riabilitato e rinforzato (asserisce deg θ=8, `shifts` vuoto, i tre rep esatti): **185s timeout → 0.16s**. Generatori indipendenti (`√2,∛3,√5`) cadono correttamente sul path generico. Effetto collaterale: `DetectTowerNLevel_SqrtTwoSqrtThreeSqrtFive` 31s → 1.2s (esce da SLOW_OK). 87 test torre/primitive verdi.
  - **Refs**: F3.5, F3.D, Task 7, CAS-L3-06

### A10 · Eigenvalues n>3 / catene di Jordan sotto RootOf — ✅ FATTO (probe chiuso 2026-06-27) — `[E3·C3·S3·R2]`
- **Stato a codice**: routing RootOf GIÀ presente (`matrix_jordan.cpp:150` → `null_space_over_extension` quando autovalore è `RootOf`). Algoritmo generale (Filippov `n_k=2d_k−d_{k−1}−d_{k+1}`, catene top-down) senza cap su `n`/molteplicità.
- **CHIUSO 2026-06-27**: il probe richiesto è verde. `RootOf_Multiplicity3_CompanionDeg6`: companion di `(x²−2)³ = x⁶−6x⁴+12x²−8` (non-derogatoria ⇒ blocco Jordan singolo size 3 per ciascun coniugato ±√2), **n=6>3, autovalore RootOf molteplicità 3, catena lunghezza 3**, certificato `P·J·P⁻¹==A` esatto su Q(√2) (1.3s). Esercita `null_space_over_extension` su `(A−√2·I)^k` fino a k=3. La descrizione storica "non affidabili" era stale. 5/5 JordanCertTest verdi.
- **Refs**: CAS-L2-02, F4.4

### A11 · Gruntz MRV growth-rank dinamico — 🧊 FROZEN 2026-06-27 (copertura ottima, residuo C4 deferito) — `[E3·C4·S2·R2]`
- **Stato a codice**: rank statico GIÀ rimosso (`limit_infinite.cpp` — confronto via `compare_growth` dinamico ricorsivo, Cat-10 chiuso). Torri log annidate (F7.5.D1+D2) funzionanti: 11/11 (incluso C6 `(log x+log log x)/log x=1`, C7).
- **FATTO 2026-06-26/27** (spec `Gruntz_Nested_Log.md` letta):
  - potenze frazionarie vs log: `log(x)/sqrt(x)→0` (era `∞`, silent-wrong). Cause: predicati `is_positive_power_growth`/`is_reciprocal_positive_power_growth` richiedevano esponente **intero** → ora qualsiasi razionale (classe di crescita = segno dell'esponente) + riconoscimento `sqrt`; `is_logarithmic_in_var` solo `Ln` → ora anche `Log`.
  - **sum-ratio sign** (A11-minor, commit 3067740): `(x+log x)/(x-log x)→1` (era `-1`), `x/(x-log x)→1` (era `0`). Cause: FuncCall handler in `try_infinite_limit` riconosceva solo `Ln` (→ `log(x)` non valutato a `+∞`); mancava riduzione leading-term per quoziente di Sum ∞/∞ → aggiunto `dominant_growth_term` + ratio via `extract_quotient_view`.
  - Test: `LogOverSqrtX`, `FractionalPowerBeatsLog`, `SumRatioLeadingTermSign`. 14/14 gruntz, 75/75 limit/mrv, 180/180 integrate/series/summation/residue.
- **🧊 RESIDUO C4 CONGELATO (deferito, non bloccante)**: track del **coefficiente lentamente variabile** Gruntz §3.5. Sintomo: `x^(3/2)/(x·log x)` → `ComplexRational division by zero` in `leading_power_w` (`limit_mrv_leading.cpp`). Il coefficiente del termine leader non è costante ma funzione del livello MRV successivo (il `log` è "slowly varying"); `leading_power_w` lo tratta come costante e divide per un coefficiente nullo. Fix corretto = ricorsione Gruntz completa sul coefficiente (Gruntz §3.5, `gruntz.py` come riferimento algoritmico). Effort multi-sessione. **NON ridurre il budget né aggiungere hardcode per mascherare**: quando irrisolvibile per ora, `leading_power_w` deve ritornare `Unimplemented` (non valore errato). **Refs**: CAS-L1-01, F5.2, F7.5.D1

### A12 · RootOf come operatore algebrico semplificabile/valutabile — ✅ FATTO (verificato 2026-06-26) — `[E3·C3·S3·R2]`
- **Stato a codice**: RootOf è operatore completo, non più "semi-inerte":
  (1) **simplify** — `simplify_node(RootOf)` risolve esplicitamente se `deg ≤ ctx.max_rootof_explicit_degree()` (simplify_functions.cpp:241);
  (2) **riduzione di potenza** — `Rⁿ → (xⁿ mod P)(R)` via divisione polinomiale (simplify_arithmetic_power.cpp:122), verificato su quintica irriducibile `x⁵−x−1`: `R⁵→R+1`, `R⁶→R²+R`;
  (3) **eval numerica robusta** — Sturm isolation + isolating-bound del nodo (evaluator.cpp:161), con `root_index`;
  (4) **normalizzazione Q(α)** on-demand via `simplify_in_q_alpha` (usata in residue/eigen);
  (5) parser+printer round-trip con isolating bounds.
  +test `RootOfQuinticPowerReduction`. La forma inerte di default per `1/R` è scelta standard (come Maxima/SymPy: ratsimp esplicito); non è un debito. **Refs**: CAS-L1-05

### A13 · Residue theorem grado arbitrario al denominatore — QUASI-FATTO 2026-06-26 — `[E3·C3·S2·R1]`
- **Stato a codice**: ✅ grado arbitrario già coperto: quadratici + biquadratici esatti (Q(α)) **a molteplicità qualsiasi**, fattori grado ≥3/quartiche generali via fallback numerico Aberth (`numeric_residue_contribution`). Poli di ordine >1 risolti dalla ricorrenza di Laurent in `residue()`; l'assembly Q(α)→ℝ è un funzionale lineare indipendente dalla molteplicità. **Rimosso 2026-06-26** il guard `multiplicity > 1` sul ramo biquadratico (era falso limite). +4 test: `1/(x²+1)³`=3π/8, `1/(x⁴+1)²`=3π/2^(5/2), `1/(x⁴+x²+1)²`=2π/3^(3/2) (oracolo Maxima), `1/(x²+1)²`=π/2 (skip morto → assert duro). 14/14 ResidueTheoremTest, 51/51 area residue+laplace.
- **Residuo (deliberato, fuori scope)**: poli reali (fattori lineari) → rifiutati; Cauchy Principal Value + Jordan lemma per integrandi oscillatori (e^{iax}) non implementati. **Refs**: CAS-L2-22, F5.6

### A14 · Equivalenza trascendente `are_equal` (assumption propagation completa) — ✅ FATTO/SOUND (verificato 2026-06-26) — `[E3·C3·S2·R2]`
- **Stato a codice**: positivity inference NON debole — `Assumptions::is_positive/is_nonnegative` copre Exp/Abs/Sqrt/Product/linear/Pow(even)/Sum + graph proof (`prove_relation`). Verificato che `mathematically_equal` con propagazione dominio:
  (a) **dimostra** sotto ipotesi: `ln(x²)=2ln(x)`, `exp(ln x)=x`, `ln(ab)=ln a+ln b`, `sqrt(x²)=x` con `x,a,b>0`;
  (b) **NON sovra-dichiara** senza ipotesi: `ln(x²)=2ln(x)`→false, `sqrt(x²)=x`→false (no silent-wrong, REGOLA ZERO);
  (c) identità incondizionate: `sqrt(x²)=|x|`→true.
  +2 test `A14TranscendentalEqual`.
- **Nota teorica**: la versione *"completa"* (zero-equivalence elementare totale) è **Richardson-indecidibile** — il contratto corretto è soundness (mai falso-positivo), non completezza. Questa è raggiunta. **Refs**: CAS-L2-19

### A15 · Units SI system — ✅ FATTO (verificato+completato 2026-06-26, era stale) — `[E3·C2·S3·R1]`
- **Stato a codice**: NON "mai iniziato" — sistema completo:
  - AST node `Quantity{value, SIDimensions}` (7 dimensioni base m·kg·s·A·K·mol·cd).
  - Registro `src/symbolic/units.cpp`: base SI + derivati (Hz,N,J,W,Pa,C,V,Ohm) + non-SI (ft,in,mi,lb,cal,eV) con scale Rational esatte.
  - `make_quantity_from_unit` / `convert_quantity` (con check mismatch dimensionale).
  - **Dimensional analysis** in simplify: prodotto combina dimensioni (chain.cpp:325); somma raggruppa per dimensione e **rifiuta** dimensioni incompatibili `1·m+1·s` (chain_sum.cpp:75, no silent-wrong); potenza scala dim·n.
  - **Completato 2026-06-26**: prefissi SI algoritmici (decomposizione `GHz→giga·Hz`, `um→micro·m`, set CGPM esaustivo applicato ai simboli prefissabili, exact-match prioritario) + **costanti fisiche esatte 2019-SI** (`make_physical_constant`: speed_of_light, planck_constant, elementary_charge, boltzmann_constant, avogadro_constant, standard_gravity — tutte Rational esatte).
  - 18 UnitsTest + 10 QuantityTest verdi. **Refs**: F6.6

### A16 · solve_inequality boundary `double` → `Rational` — 🧊 DEFERITO post-parità (ledgered, era mis-scoped E1) — `[E3·C3·S2·R2]`
- **AUDIT 2026-06-27** (ledger HC-F70-A21 autoritativo + codice letti): la voce "E1, solo `low/high/tol` double→Rational" era **fuorviante**. Stato reale:
  - `low/high/tol` (`solve_inequality.cpp:98-100`) sono il **confine numerico Cat-4 deliberato**: bound/tolerance stoccati `long long` in `CASContextParams`, convertiti a `double` SOLO al call-site `find_polynomial_roots_sturm` (layer numeric). REGOLA 1 vieta `double` nel *core simbolico*, lo **ammette ai confini numerici** → non è un debito, è by-design documentato.
  - Debito reale (più profondo, **non** quello del titolo originale): (a) output radici via `double_to_rational_approx` (riga 115) = boundary **approssimati**, e (b) `InequalityInterval.lower/upper` è `optional<Rational>` → **non può rappresentare radici irrazionali esatte** (√2 ecc.) neppure col fix puro-Rational.
  - **Fix completo (ledgered, deferito Fase 8 post-parità)**: `find_polynomial_roots_sturm_rational` (bisection puro Rational/BigFloat) per (a); per (b) servirebbe `InequalityInterval` boundary `ExprPtr`+`RootOf` via `find_polynomial_isolating_intervals` (esiste, F8.0-5.4). Re-stima realistica **E3·C3**, non E1.
  - **Decisione**: NON toccare ora — versione cosmetica `low/high/tol`→Rational = pigra/no-op (REGOLA ZERO), versione vera contraddice il deferral post-parità deciso (goal corrente = parità HP Prime ≥95%, §B). Blast radius contenuto (3 file, nessun chiamante esterno) quando si riprenderà in Fase 8.
- **Nota 2026-07-07**: aggiunto guard esplicito multivariato in `solve_inequality_1var` (simboli ≠ var → `Unimplemented` strutturato, Cat-3 niente silenzio fuori scope) + passthrough diagnostico su fallimento Sturm. Il deferral del fix Rational/RootOf resta invariato.
- **Ledger**: HC-F70-A21-NUMERIC-BOUNDARY (PARTIAL, deferral documentato) · **Refs**: T-018

### A17 · IBP doppia applicazione su `Product(Log,…)` — ✅ FATTO (verificato 2026-06-26, stale) — `[E1·C2·S3·R2]`
- **Stato a codice**: ledger HC-F75-B1-IBP-DOUBLE-APPLY CHIUSO (fix 2026-06-10 simplify(vdu) pre-ricorsione + T-016 ILATE-class-da-base per `Pow`). Round-trip `IntegrateByPartsTest.{XLogX,XLogXSquared,LogXCubed}` 3/3 verdi 2026-06-26. Voce era stale nel tasklist.
- **Ledger**: HC-F75-B1-IBP-DOUBLE-APPLY (chiuso) · **Refs**: T-016

### A18 · GCD multivariato budget magico `*16U` → bound Mignotte — ✅ FATTO **per decisione, non per codice** (bound provato chiuso 2026-07-08; propagazione `Unimplemented` NON implementata, accettata by-design 2026-07-25) — `[E1·C2·S2·R2]`
- **⚠️ Nota di verifica 2026-07-25**: la chiusura del 2026-07-25 (commit `74a21ea`) **non contiene modifiche di codice per A18** — è una riclassificazione del residuo da "aperto" a "by-design". Verificato a codice: `proven_division_step_bound` esiste ed è usato nei 3 siti (`polynomial_gcd_fp_helpers.cpp:52`, `polynomial_gcd_brown_helpers.cpp:82`, `polynomial_gcd_brown_lc_scaling.cpp:188`) — quel pezzo era già fatto il 2026-07-08 — ma lo sforamento resta `if (++steps > budget) return false;`, cioè **esattamente il residuo descritto sotto, invariato**. La decisione di accettarlo è difendibile (l'argomento era già scritto nella voce stessa); va solo letta come decisione, non come lavoro svolto.
- **Stato a codice**: ✅ galois `*8U+16U` rimosso → bound derivato `prime_budget + Σ bit_length(lc,disc_num,disc_den)` (galois_deg5.cpp:187, HPP-003B).
- **✅ FATTO 2026-07-08**: pad `+16U` euristico rimosso dai 3 siti `divides_sparse_z`/`exact_divide_sparse_z`/`exact_div_fp` (brown_helpers/lc_scaling/fp_helpers) → nuovo `fp_helpers::proven_division_step_bound(dividend, n_vars)` = bound **provato** `∏_i(deg_i(dividend)+1)` (grado del quoziente in ogni variabile ≤ grado del dividendo, per divisione esatta). Elimina il falso-negativo Cat-4 su divisori legittimi a molti termini. Verificato: 129/129 test GCD/Zippel/Wang/Brown mirati verdi.
- **🔧 RESIDUO**: i 3 siti ritornano ancora `false`/`nullopt` (non `Unimplemented`) su sforamento bound — non chiuso perché tutti i call-site li usano come gate di retry in loop CRT/modulari dove `false` è esito atteso per candidato sbagliato; threading `Result<T>` attraverso quella catena è refactor R2/R3 a parte. Col bound provato, sforarlo è ormai evidenza di invariante rotto (non falso "non-divisore"), quindi il rischio pratico è rimosso; l'upgrade resta solo osservabilità.
- **Ledger**: HPP-003B (chiuso), HPP-003C (🟡 parziale, bound-fix chiuso) · **Refs**: T-020

### A19 · AstArena reset con root migration — ✅ CHIUSO (verificato a codice 2026-07-08, era stale) — `[E2·C3·S2·R3]`
- **Stato a codice**: già implementato e testato, a un layer diverso da quello che il ledger descriveva. `CASContext::collect_garbage(external_roots)` (`src/symbolic/context_core.cpp`) crea una `AstArena new_arena`, clona ogni radice viva (variabili, assumptions, trace, root esterni passati dal chiamante, le 3 cache simplify/diff/integrate) via `clone_into_arena` (`src/ast/ast_clone.cpp`, deep-copy visitor completo su **tutti** gli ExprKind incl. RootOf/Matrix/SeriesExp/Quantity/Integral/Derivative/Limit) e poi fa lo swap `arena_ = std::move(new_arena)`. Testato in `test_gc.cpp`: `CollectsGarbageAndPreservesRoots` (radici interne+esterne preservate, interning nella nuova arena funzionante) e `MemoryPressureStressTest` (10k simplify + GC ogni 100 iter, arena resta <100 nodi).
- **Distinzione chiave**: il raw `AstArena::reset()` (senza migrazione) resta un primitivo *intenzionalmente* distruttivo — `test_arena_reset.cpp` ne documenta esplicitamente il contratto ("post-reset, the prior pointers are dangling — they MUST NOT be dereferenced"). Non è un bug: è il building block a basso livello; `collect_garbage` è l'API sicura sopra di esso. Grep-verificato: **zero** call-site in `src/` chiamano `arena_.reset()`/`.arena().reset()` su un'arena viva bypassando `collect_garbage` — il pattern pericoloso descritto dal ledger non esiste in produzione.
- **Ledger**: HC-F70-A31-MIGRATION-TODO → chiuso (era APERTO per assenza letterale di `AstArena::migrate_into`, funzione equivalente e più completa esiste come `CASContext::collect_garbage`+`clone_into_arena`) · **Refs**: T-023

### A20 · Cycle detection simplifier/evaluator — ✅ FATTO 2026-07-07 — `[E2·C3·S2·R3]`
- **Stato a codice**: chiuso.
  - **Simplifier**: `CycleGuard` riscritto **path-based** (catena ancestrale, `simplify_ancestor_path`) — un nodo è ciclo solo se ri-entrato su un frame antenato; lo structural sharing (stesso ExprPtr figlio di più padri) NON è falso positivo. Cycle → `Unimplemented` strutturato `CYCLE_DETECTED` (prima ritornava `ok(expr)` silenzioso). Depth exceeded → `RECURSION_DEPTH_EXCEEDED` strutturato. Budget = `max_simplification_depth` (contratto L0-12 preservato — audit 2026-07-07: una bozza lo aveva switchato a `max_recursion_depth` svuotando `set_max_simplification_depth`; ripristinato).
  - **Numeric evaluators** (`NumericEvaluator`, `BigFloatEvaluator`): guardie depth+cycle con `active_nodes_` + ScopeGuard RAII; budget = nuovo param ctx `max_recursion_depth` (default 256, configurabile `set_max_recursion_depth`).
  - Test `TaskA20_CycleDetection` 5/5 (config param, depth simplifier/evaluator, cicli puntatore mutati ad arte). **Refs**: CAS-L0-10

### A21 · Pivot Bareiss euristica contestuale — ✅ FATTO 2026-07-07 — `[E2·C2·S2·R2]`
- **Stato a codice**: `PivotScore` ora ordina `(certainty, −complexity, −degree)` con `estimate_complexity` pesata per operazione (Sum/Add penalizzati — espansione fill-in; Mul/Pow leggeri; letterali ±1 minimi); rimossi gli early-exit `certainty==3` in bareiss/determinant/inverse/LU (scan colonna completo — il pivot "esatto ma complesso" non domina più un pivot semplice). Test `test_bareiss_vander.cpp` estesi (+205 righe, Vandermonde simbolici). **Ledger**: HPP-025 · **Refs**: CAS-L1-17

### A22 · Padé coeff generici Q(π,e,√) — `[E2·C3·S1·R1]`
- **Cosa**: estendere Padé a campo algebrico generato dinamicamente (oggi vincolo Q), riusare LLL. **Refs**: F7.5.C2

### A23 · Structured Unimplemented diagnostic — ✅ FATTO 2026-07-07 — `[E2·C1·S1·R1]`
- **Stato a codice**: nuovo `include/cas/unimplemented_info.hpp` (`UnimplementedInfo{module,function,input_shape,reason,suggestion,ticket}`, alias compat `UnimplementedPayload` + getter `reason_code()/ticket_id()`); overload `make_unimplemented_error(UnimplementedInfo)` + helper `make_unimplemented<T>(UnimplementedInfo)` + `Result<T>::unimplemented(...)`; reason codes `RECURSION_DEPTH_EXCEEDED`/`CYCLE_DETECTED` aggiunti. Call-site conversion: limit_mrv, kovacic case3, solve_inequality (+ guard multivar esplicito Cat-3), tower nested, frobenius, simplify_core, evaluators. Test: `test_unimplemented_info.cpp` nuovo + diagnostic/payload aggiornati. Copertura incrementale sui restanti `Unimplemented` legacy man mano che si toccano i moduli.
- **Ledger**: F0.8-ERROR-STRUCT · **Refs**: T-026

### A24 · Runner matrix·scalar / matrix±matrix — ✅ FATTO (verificato 2026-06-26, stale) — `[E1·C2·S2·R1]`
- **Stato a codice**: entrambi ledger CHIUSI. `matrix_adapter.hpp::evaluate_matrix_expression` (evaluatore top-level precedence-aware: scalar·matrix, matrix·matrix, matrix±matrix, matrix/scalar, unary −matrix; `Unimplemented` esplicito su scalar±matrix e div-by-matrix) + `try_evaluate_mattrace_wrapper`. Corpus matrix 79/79 = 100% (era 70.9%). `MatrixAdapterD2Test` 19/19 verdi 2026-06-26.
- **Ledger**: HC-F75-A2-MATRIX-SCALAR-OP, HC-F75-A2-MAXIMA-MATTRACE (entrambi chiusi)

### A25 · deg-16 Hensel-lift mod-p^k perf (norma Trager torri 2-livello) — ✅ CHIUSO 2026-07-10 (best-prime Zassenhaus) — `[E3·C3·S2·R3]`
- **Origine**: estratto da A3 dopo che LLL fraction-free + Kronecker deadline + verdetto lucky-prime hanno risolto il collo di bottiglia correttezza. **Nessun problema di correttezza**: le fattorizzazioni/antiderivate prodotte sono giuste, il debito è **puramente performance**.
- **Sintomo a codice**: `FactorizationTowerTest.SplitsProductOfQuadraticsOverQSqrt2Sqrt3` e `FactorizationTowerTest.SplitsX4Minus10X2Plus1OverQSqrt2Sqrt3` >200s. Lo splitter deg-4 su torre `Q(√2,√3)` produce una norma di Trager **deg-16 a coefficienti enormi**; col LLL e Kronecker già risolti il profilo (`sample`) si concentra su `hensel_lift` / `quadratic_step` / `poly_*_mod` — il lift mod-p^k della norma deg-16.
- **Fix corretto**: ottimizzare l'Hensel-lift mod-p^k (quadratic Hensel / aritmetica modulare più veloce / scelta primo+precisione mirata sui coeff-bound Mignotte della deg-16), NON euristiche di forma. Spec `Hensel_Lifting.md` (REGOLA 0.1) da rileggere prima dell'impl.
- **✅ CHIUSO 2026-07-10**: risolto dalla **selezione del primo** introdotta in A6 (commit `88d0b0e`, `factorization_wang_eez.cpp`) — esattamente uno dei fix indicati sopra ("scelta primo mirata"), non un'euristica di forma. Il codice sceglieva il primo candidato; un primo sfortunato spezza il polinomio in molti fattori modulari e fa esplodere la ricombinazione. Ora si scandiscono i primi *lucky* (f squarefree mod p) e si usa quello con **meno fattori modulari**. Misurato: `SplitsProductOfQuadraticsOverQSqrt2Sqrt3` >200s → **9.9s**, `SplitsX4Minus10X2Plus1OverQSqrt2Sqrt3` >200s → **12.5s**.
- **Quarantena**: i 2 test rimossi; `PrimitiveElementTest.SqrtTwoSqrtThreeSqrtFive` (facet correlato) rimosso anch'esso, 1.25s dopo A9. CEILING 4 → **1** (resta solo `F2GateBenchmark`). **Ledger**: HC-F8-FACTORIZATIONTOWER-PERF ✅ chiuso. **Follow-up 2026-07-10**: anche `F2GateBenchmark` risolto (145s → 4.4s, collo = `try_kronecker_quadratic_factor` che divideva su `PolyExpr`/arena; ora pseudo-resto BigInt puro) — quarantena **vuota**, CEILING 1 → **0**, HC-F8-F2GATE-BENCHMARK-FAIL ✅ chiuso.
- **Ledger**: HC-F8-FACTORIZATIONTOWER-PERF (residuo deg-16 Hensel) · **Refs**: A3 (chiuso), BUG-HANG-002

### A26 · Wire-in solver parametrico Risch DE a livello tower (prerequisito A1 df>0) — ✅ CHIUSO per la parte concludibile senza A1 (f=0 + f≠0 razionale + WeakNormalizer chiusi; residuo wiring gated A1) — `[E4·C4·S3·R2]`
- **FATTO 2026-06-29** (validazione-prima-del-wiring, spec Cap8/Hermite/Risch_Algorithm lette): la funzione orfana è ora **raggiungibile + testata** via harness diretto `test/unit/calculus/test_risch_parametric_tower.cpp` (back-substitution SOUND `D(y)+f·y≡Σc_i g_i` con `field.derive`).
  - **✅ memory-safety**: heap-buffer-overflow ASan a `risch_rde_bronstein.cpp:384/425` (`g_polys[s][idx]` con `idx>deg` → `operator[]` non-const non-checked) corretto via `const PolyExpr&` (overload bounds-checked, OOB→0). Era una mina: wiring alla cieca nel hot-path avrebbe introdotto corruzione memoria.
  - **✅ ramo esponenziale validato sound** (`Exp_F0_GExp_AllSound`).
  - **✅ df>0 RIPRODOTTO deterministicamente** (`Log_FEqualsTheta_DfPositive_ReachesNonCancellation`): `f=t` su `Q(x,log x)` raggiunge il ramo non-cancellation → **sblocco concreto di A1** (16 integrandi a mano non ci riuscivano). Quando A1 implementerà Bronstein §6.5, quel test passa da Unimplemented a verify_field_de.
- **✅ FASE 2 — caso f=0 RISOLTO 2026-06-30** (commit `7187c77`): il ramo log (primitivo) genera dalla correzione `i·y·θ'` (θ'=1/x) una forzante **razionale** che `solve_risch_de_parametric_q` (solo `Q[x]`) rifiutava. Nuovo `solve_param_limited_integration_rational_q` (`src/calculus/risch_parametric_rational.cpp`) = **rational limited integration su Q(x)** (Bronstein §7.2/§7.3): integra ogni `g_i`, split `R_i` razionale + atomi log/arctan, null space matrice atomi → `y=Σc_i R_i`, **verifica back-substitution** (sound-by-construction, mai silent-wrong). Wired nel base case `solve_risch_de_parametric_field` (ext_idx==0, fallback f==0). Flippa `Log_F0_G1_PrimitiveDescent_SolvedSound` (era Unimplemented → `y=x,c=1` verificata) + 3 test diretti. 81 risch/parametric verdi, 0 regressioni.
- **✅ FASE 3 — caso f≠0 razionale RISOLTO 2026-06-30** (commit `7725c25`): nuovo `solve_param_risch_de_rational_q` = **ParamRischDE generale su Q(x)** (Bronstein §6.1/§7.1). Ansatz `y=P/D`, `D=lcm(den f,den g_i)`; identità `D·P'+(Fn−D')·P−Σc_i(Gn_i·D)=0` → sistema omogeneo → null space. **Degree bound cancellation** (`deg_H==deg_D−1` → grado omogeneo `−lc(H)/lc(D)`; trova `y=x²` di `y'−2y/x=0`). **Doppia verifica**: residuo Q esatto (interno, robusto) + `verify_field_de` simbolico (test) — entrambi verdi. Output integer-cleared. Wired (ext_idx==0, fallback f≠0). +4 test. 85 verdi, 0 regressioni. *(Sospetto bug-simplify su `(−x/2)/x²` investigato = FALSO ALLARME; engine corretto.)*
- **✅ FASE 4 — WeakNormalizer RISOLTO 2026-07-01** (`inflate_denominator`, spec Cap5/Cap8 rilette): **denominator inflation Bronstein 6.1.1**. Correzione diagnosi: nella convenzione `y'+f·y=g` è il residuo intero **POSITIVO** (non negativo) al polo semplice a creare il polo in `y_h=(x−α)^{−n}` oltre `lcm`; il negativo dà `y_h` polinomiale (già coperto). Fix: residuo `n` ai radici di `gcd(fn−n·fd', s)` (Rothstein-Trager) per `n∈[2,cap]` → `D *= s_n^{n−1}`. **SOUND** (back-sub scarta non-soluzioni). Cap = `ctx.max_risch_rational_ansatz_degree()` (configurabile, zero magic). +3 test critici `WeakNormalizer_*` con **probe load-bearing** (disabilitato→3 fail, riabilitato→pass). 13 ParametricTower + 151 risch/integrate verdi, 0 regressioni.
- **🔧 RESIDUO (non-A26, incompletezza MAI sbagliata)**: poli di f a **molteplicità ≥2**, residuo intero **oltre cap**, residui **algebrici** → Unimplemented pulito; inflate del solver **non-parametrico** `solve_risch_de_rational_q` (R3 hot-path, task separato); wiring in `integrate()` reale subordinato a **df>0 (A1)**.
- **Finding verificato 2026-06-29** (grep tree-wide + graph): `solve_risch_de_parametric_field` (`risch_rde_bronstein.cpp:249`) — la funzione che l'audit 2026-06-27 chiamava "il solver parametrico reale già implementato" — è **dead code**: **zero caller esterni**, raggiunta solo dalla propria auto-ricorsione `ext_idx-1` a L399 (che non parte mai). L'audit aveva verificato che il codice è *scritto* (trial-constants rimossi, logica df≤0 presente) ma **non** che è *raggiunto*. Il path Risch attivo è `solve_risch_de_general` → `solve_risch_de_field` (non-parametrico, single-`g`); il parametrico base-case `solve_risch_de_parametric_q` (`risch_parametric.cpp`) esiste ma non è invocato dal field solver.
- **Conseguenza su A1**: il sito `df > 0` (`risch_rde_bronstein.cpp:362`) è dentro questa funzione orfana ⇒ **strutturalmente irraggiungibile**, non solo "irraggiungibile dal corpus". Implementare df>0 lì = math non verificabile su funzione mai chiamata = **REGOLA ZERO violata**.
- **Scope A26** (vero unblock di A1, deciso 2026-06-29): costruire l'integrazione parametrica **a livello tower** che invoca `solve_risch_de_parametric_field` dal path integrate reale (es. limited-integration / coupled-system / parametri), così che la RDE parametrica diventi raggiungibile **e testabile** con cross-check Maxima. Solo a quel punto il ramo df>0 (Bronstein §6.5/§7.4/§8.4) ha un caso-test che lo esercita ⇒ A1 implementabile in sicurezza. Spec `Risch_Transcendental_Cap8.md` + `Risch_Hermite_Cap5.md` (REGOLA 0.1) da rileggere.
- **Ledger**: HC-F8-PENDING-17 (condiviso con A1) · **Refs**: A1 (sbloccato da qui), A7, T-007
- **✅ CHIUSURA COMPLETA 2026-07-01** (commit `88c0a9a`+`7dc1c27`): A1 df>0 + deep-tower ConstantSystem chiusi. `DifferentialField::derive` riscritta come derivazione vera sulla torre (era shim solo-simbolo-nudo, silent-wrong 0 su compositi); `constant_system_nullspace` = Bronstein §7.1 Fig 7.1/Lemma 7.1.2 (torri K⊋Q(x)). Oracoli 2-livello exp(exp x) verdi. HC-F8-PENDING-17 chiuso. Residuo restante = **wire-in in `integrate()` reale**: indagine 2026-07-01 ⇒ **NON giustificato ora** (integrate già copre torri-exp naturali; nessun gap osservato risolto dal solver parametrico; mismatch forma-generatori `to_field_generators`→`exp(t_0)*t_0`≠`t_1*t_0`). Wiring forzato = codice non-esercitato/ridondante (REGOLA ZERO). Riprendere solo con un integrando-driver reale che integrate() fallisce E il parametrico risolve.

### A27 · Log-derivative recognition per torri log ANNIDATE (nested) — ✅ CHIUSO 2026-07-08 — `[E2·C3·S2·R1]`
- **Gap originale** (probe 2026-07-01): `∫ 1/(x·log(x)·log(log(x))) dx = log(log(log(x)))` ritornava `INTEGRATE_NO_STRATEGY`.
- **Root cause reale (peggiore del previsto — SILENT-WRONG, non solo bail pulito)**: investigando si è trovato che il percorso Hermite/Rothstein-Trager per torri annidate non falliva pulito ma **ritornava 0** come antiderivata. Causa: (1) `DifferentialField::derive()` di un generatore `t_i` ritorna `D(t_i)` in forma ORIGINALE (es. `1/(x·ln x)`), non nella rappresentazione a generatori — risultanti/GCD su espressioni miste perdono fattori silenziosamente; (2) Rothstein-Trager accettava radici del resultant senza verificare che fossero costanti in Const(K) (Bronstein §5.6 richiede `c_i` costanti); (3) un gcd banale (deg 0 in t) produceva un termine `ln|1|=0` invece di essere scartato.
- **Fix implementato** (`src/calculus/differential_field.cpp`, `differential_field_integration.cpp`, `integrate_risch_hermite.cpp`):
  - nuovo `DifferentialField::derive_in_generators()`: deriva poi rimappa via `to_field_generators` così K=C(x,t_1..t_n) resta differenzialmente chiuso; usato in `integrate_rothstein_trager` (per `D(Q)`) e nei due `field.derive` di `ho_reduce_step` (Hermite A/A').
  - Rothstein-Trager: scarta radici dipendenti da `var` o da un generatore della torre (non sono residui validi); scarta gcd con `deg_t(v)==0` (contributo nullo, mascherava il bail).
  - Guardia zero-risultato in `integrate_risch_poly_and_rational_part`: se rational+log+poly collassano tutti a 0 con integranda ≠0 → `Unimplemented` strutturato invece di 0 silenzioso (REGOLA 0.2).
- **Verifica**: 3 nuovi test `RischNestedLogTest` (catena 3 livelli c=1, c=3 formale, regression guard 2 livelli) tutti verificati per round-trip di derivazione; 190/190 test Risch/integrate/Hermite/DifferentialField mirati verdi; golden `integrate` fail 3→2 (nessun pass perso), `bronstein` invariato.
- **Refs**: A1/A26 (stessa area Risch, indipendente).

### A28 · Silent-wrong integrazione by-parts/sostituzione su transcendente opaco — ✅ CHIUSO 2026-07-01 — `[E3·C4·S4·R2]`
- **Silent-wrong VERIFICATO per differenziazione** (probe 2026-07-01): `∫ x·exp(exp(x))·exp(x) dx` (NON-elementare) → `integrate()` ritorna `½x²·exp(x)·exp(exp(x))` (**SBAGLIATO**, `d/dx−integrand≠0`); idem `∫ 2·exp(2x)·exp(exp(x)²)`. Pattern: la sostituzione `v1=exp(x)` tratta il transcendente come **costante** e integra `∫x·v1 dx=½x²·v1`. Viola REGOLA 0.2.
- **Root cause**: `DifferentialField::to_field_generators` (`src/calculus/differential_field.cpp`) sostituiva i generatori della torre in ordine **forward** (`t_0=exp(x)` prima di `t_1=exp(exp(x))`). Sostituire il generatore interno per primo riscrive `exp(x)→t_0` **dentro** il pattern del generatore esterno `exp(exp(x))`, che diventa `exp(t_0)` e non matcha più il pattern di `t_1`. Il transcendente esterno resta simbolo libero non mappato → il ramo poly-part del solver esponenziale lo classifica come **coefficiente costante** e produce un'antiderivata elementare errata per un integrando non-elementare.
- **Fix**: iterazione **reverse** (`rbegin()/rend()`, outermost-first), simmetrica a `from_field_generators` che già svolgeva in quest'ordine. Nessun hardcode: stessa logica generale, solo ordine di iterazione corretto sulla torre.
- **Verifica**: nuovo test `test/unit/calculus/test_risch_nested_transcendental.cpp` (4 casi) — i 3 integrandi non-elementari ora ritornano `Unimplemented` (bail pulito, REGOLA 0.2 rispettata) invece di risposta sbagliata silenziosa; il caso elementare `exp(exp(x))·exp(x)` resta correttamente risolto (nessuna regressione). Gate: quick pieno 2551 PASS, 0 regressioni, 2 skip pre-esistenti.

### A29 · Anti-monolito: split `risch_rde_bronstein.cpp` (538 LOC) + `risch_parametric_rational.cpp` (593 LOC) — ✅ FATTO 2026-07-07 (alias SPLIT-RISCH-RDE) — `[E1·C1·S2·R2]`
- **Chiusura**: split per unità funzionale, zero cambi di logica. `risch_rde_bronstein.cpp` (252) + nuovo `risch_rde_parametric_field.cpp` (306, tower descent parametrico); `risch_parametric_rational.cpp` (352, f≠0 P/D-ansatz) + nuovo `risch_parametric_limited.cpp` (248, f=0 limited integration). Helper condivisi (`poly_coeffs_q`, `rational_to_expr`) promossi inline in `risch_parametric_internal.hpp`. Whitelist svuotata. Gate: 97/97 test Risch mirati verdi, build -Werror pulita.

### A31 · Propagazione condizioni di dominio (`x≠0`) attraverso simplify — ✅ FASE 1+2 FATTE (f1 2026-07-15, f2 2026-07-17; residuo = fase 3 su driver reale) — `[E4·C4·S3·R2]`
- **✅ FASE 2 IMPLEMENTATA 2026-07-17** (spec §10, addendum verificato a codice):
  (a) **fix bug latente fase 1**: cache-hit top-level non azzerava l'accumulatore
  (`context_core.cpp` ramo hit) → sequenza a/a, b/b, a/a-hit over-riportava
  {NonZero a, NonZero b}; ora `!operation_active_` ⇒ clear prima del merge;
  (b) flag **`set_conditional_domain_rules(bool)`** (default false, output
  invariato) + **5 regole B.1** con emissione condizioni: R1 `exp(ln z)→z`
  NonZero(z) (2 siti: FuncCall Exp + Pow(E,·); inner accetta Ln E Log),
  R2a `ln(b^e)→e·ln b` Positive(b)+Real(e), R2b/R2c `ln(a·b)`/`ln(a/b)`
  Positive per fattore, R3 `abs(b^2k)→b^2k` Real(b), R4 `0^e→0` Positive(e)
  (esponenti letterali esclusi: semantica esatta preservata), R5
  `sqrt(b²)→abs(b)` Real(b); guardie anti-contraddizione (base/esponente
  provati negativi ⇒ rifiuto preservato); `strict_branch_cuts` vince sul flag;
  (c) **`simplify_tracked()`** → `Simplified{expr, conditions}`;
  (d) **golden runner §10.5**: entry con `assume` ⇒ flag ON per-entry + stampa
  `INFO conditions taken:` (WARN A32 resta). Golden simplify **110/6 → 116/0**.
- **Test fase 2**: `test/unit/symbolic/test_conditional_rules.cpp` 16/16
  (flag-off invarianza, output+set esatto per regola, controprova §3.3,
  guardia contraddizione, fix §10.1, tracked miss+hit, certificato numerico
  multi-punto R3/R5); gate mirato simplify/sqrt/log/abs/power 424/424.
- **✅ FASE 1 IMPLEMENTATA 2026-07-15**: `include/cas/side_conditions.hpp` +
  `src/symbolic/side_conditions.cpp` — `SideConditionSet` (dedup + subsumption
  Positive⊃NonZero,NonNegative) + `CASContext::emit_side_condition` (skip se
  già provato da `assumptions()`, altrimenti registra o `Unimplemented`
  strutturato `SIDE_CONDITION_BUDGET_EXCEEDED` oltre `max_side_conditions()`,
  default 256). Instrumentati i **2 siti verificati** §4.3: (1)
  `simplify_product_factors` (`simplify_arithmetic_chain.cpp:433`, drop di
  `base^0`) — cancellazione `x·x⁻¹→1` per sintassi Product (bypassa il
  rewrite_provider); (2) `try_rewrite_algebraic` GCD path
  (`builtin_rewrite_algebraic.cpp`) — cancellazione `x/x→1` per sintassi Div
  (intercettata dal rewrite_provider PRIMA del core `BinaryOp::Div`, verificato
  essere un meccanismo DIVERSO dal (1) — Meccanismo 1 vs 2 di spec §1.1,
  confermato dai test che i due producono la stessa condizione via codice
  diverso). Cache `simplify_cache_` estesa a `SimplifyCacheEntry{result,
  conditions}` — hit ri-emette le condizioni (altrimenti una seconda chiamata
  le perderebbe silenziosamente); `collect_garbage()` re-interna i subject
  ExprPtr sia della cache che dell'accumulatore live. Zero cambio nell'output
  di `simplify()` (vincolo duro rispettato).
- **Test**: `test/unit/symbolic/test_side_conditions.cpp` 13/13 verdi — i 2
  meccanismi end-to-end, dedup/subsumption, cache-hit re-emission,
  invalidazione su cambio assumptions (revision bump), no cross-pollution tra
  chiamate top-level consecutive, GC round-trip (ASan pulito).
- **Gate**: quick suite **2715/2715 verde** (2703 pre-A31 + 12 nuovi), build
  `-Werror` pulita, benchmark eseguito (numeri assoluti non anomali —
  `parse_implicit_mul_ms 0.007`, `simplify_basic_ms 0.067`,
  `poly_gcd_subresultant_ms 1.750`; nessun `baseline_release.txt` per
  confronto relativo, come già in A6/A7 questa sessione).
- **Residuo (fase 3, spec §10 nota iniziale — riaprire solo con driver reale)**:
  propagazione a `integrate`/`solve`; congiunzioni tra condizioni; A32 (parser
  predicati `assume` + refs Maxima coordinate). Il censimento di eventuali
  ulteriori siti fase-1 resta un compito a metodo fissato (leggere il codice,
  mai la memoria/ledger) da svolgere quando un caso reale lo segnala.
- **✅ SPEC CREATA 2026-07-15**: `MISSING_FEATURES_SPECS/Domain_Conditions_Propagation.md` — **decisione architetturale approvata dall'utente: opzione (b)** side-conditions accumulate in `CASContext`/cache, esposte accanto al risultato (`last_side_conditions()`), MAI nell'AST. `simplify` resta bit-per-bit identico in output; fase 1 = solo registrazione di ciò che già avviene. Ogni citazione a codice **verificata leggendo il sorgente** (non dedotta) — il primo abbozzo conteneva 4 imprecisioni trovate e corrette in sede di verifica (vedi errata in cima al file): il sito reale della cancellazione `x/x→1` è `Simplifier::simplify_product_factors` (`simplify_arithmetic_chain.cpp:433`), non `merge_symbolic_factors` (quella è `static`, priva di accesso al contesto); `exp(ln x)→x` è **già** condizionato a `is_known_positive` (nessun gap fase-1 lì, è il pattern-modello).
- **Origine**: metà scartata del WIP branch-cut (commit `468351c` ne ha committato solo la parte branch-cut). Due tentativi di rendere `simplify` conservativo sul dominio sono stati **misurati e respinti**:
  - `merge_symbolic_factors` che rifiuta di cancellare `x·x⁻¹` senza `x≠0` provato → lascia due fattori con la stessa base, **rompe la forma canonica**; 26 test rossi (Risch, Gruntz, substitution, SqrtFold) e `BronsteinCorpus` da ~20s a 632s.
  - guardia `x/x → 1` in `builtin_rewrite_algebraic.cpp` → rompe `IntegrationAdvancedTest.RischDE_Rational_ExpReciprocal{,Cubic}`.
- **Diagnosi**: rifiutarsi di semplificare **non preserva** la restrizione di dominio, la *scarta*. `x/x` resta `x/x` e il vincolo `x≠0` non è rappresentato da nessuna parte; a valle il motore perde solo la forma canonica su cui Risch/Gruntz si appoggiano.
- **Correzione 2026-07-15**: `Div(x,x)` e `x·x⁻¹` in forma `Product` **convergono nello stesso codice** (`simplify_arithmetic.cpp:156-168` riscrive ogni `Div` non-razionale in `Product(a, Pow(b,-1))` prima di dispatchare a `simplify_product_factors`) — NON è un'incoerenza viva oggi, i due tentativi storici erano solo applicati in punti diversi. L'asse reale da allineare è tra questo meccanismo monomiale e la riduzione GCD di `together()`/`apart_num_den` (`Together_Polynomial_GCD_Reduction.md`) sulle somme di frazioni aggregate — dettaglio in spec §1.1.
- **Patch di riferimento**: `foreign_wip_backup.patch` (job tmp) · **Refs**: F8.0-6.2, Branch_Cut_Propagation.md, `Domain_Conditions_Propagation.md`

### A33 · Silent-wrong ∫ razionali-in-exp: residuo iperesponenziale Bronstein §5.9 mancante — ✅ FATTO 2026-07-16 — `[E2·C3·S5·R2]`
- **Bug (scoperto dal refresh golden 2026-07-16, confronto CONTENUTO dei fail, non conteggio)**: `∫1/(1+eˣ)dx` → `−ln|eˣ+1|` (**SBAGLIATO**: `D(−ln(eˣ+1)) = −eˣ/(eˣ+1) ≠ 1/(1+eˣ)`; corretto: `x−ln(eˣ+1)`). Sweep sistematico: **7 integrandi su 8 della famiglia silent-wrong** (`1/(2+3eˣ)`, `1/(1−eˣ)`, `1/(1+e^{−x})`, `1/(1+e^{2x})`, `1/((1+eˣ)(2+eˣ))`, `(1+2eˣ)/(1+eˣ)` — solo la log-part pura `eˣ/(1+eˣ)` era giusta). Zero copertura test preesistente.
- **Root cause** (spec `Symbolic_Integration_I.md` §5.9, Theorem 5.9.1/5.9.2 letta, REGOLA 0.1): `integrate_rothstein_trager` (`differential_field_integration.cpp`) emetteva solo `Σ cᵢ·ln|vᵢ|` assumendo residuo nullo — vero per torri primitive/log e per il caso base razionale (Theorem 5.6.1), **falso per monomiale iperesponenziale**: `deg_t(Dv)=deg_t(v)` ⇒ `D(ln v)=Dv/v` porta una parte polinomiale ⇒ `h−Σcᵢ·Dvᵢ/vᵢ` è un elemento **non nullo** di k da integrare ricorsivamente.
- **Fix**: blocco exp-only (per teorema, non scorciatoia) in `integrate_rothstein_trager`: residuo calcolato esplicitamente (`P/Q − Σ cᵢ·Dvᵢ/vᵢ` via `derive_in_generators` + `together` + simplify), certificato ∈ k (`depends_on(residual, t_var)` ⇒ `Unimplemented` strutturato, mai guess), mappato back e integrato ricorsivamente (`integrate` sul campo base, torre più corta ⇒ terminazione). Errore ricorsivo = bail pulito propagato.
- **Verifica**: nuovo `test/unit/calculus/test_integrate_exp_rational.cpp` (8 test, verifica per differenziazione `D(F)==f` con together+probe multi-punto) **8/8 verdi**; gate mirato Risch/integrate/Hermite/DifferentialField/Bronstein/Residue/Laplace **285/285, 0 regressioni**; golden integrate **120→121 pass, 2→1 fail** (residuo = `abs(a)` real-domain B.1, by-design).
- **Refs**: T1/T2 sessione 2026-07-16, golden refresh, B.2 (contenuto fail ≠ conteggio fail)

### A34 · `is_strictly_canonical` vs ordinamento reale di `simplify_product_factors` — incoerenza debug-canary — ✅ FATTO 2026-07-22 — `[E2·C2·S1·R1]`
- **✅ FIX 2026-07-22**: la causa era esattamente quella diagnosticata — il checker (`is_strictly_canonical`, `simplify_utils.cpp`) confrontava i **fattori interi** via `canonical_compare`, mentre il sorter reale (`merge_symbolic_factors`, `simplify_arithmetic.cpp:442`) ordina per **BASE** (`.first` della coppia (base,exp)) e emette `base` (exp=1) o `Pow(base,exp)`. Per `Pow(Sqrt(π),−1)` vs `Sin(x)` il rank per node-kind (Binary vs FuncCall) divergeva dall'ordine per base ⇒ falso allarme. Fix a rischio ~zero (checker `#ifndef NDEBUG`, non influenza release/golden): nuovo helper `product_factor_sort_base` che estrae la base come il sorter (`Pow` con esponente INTERO → base; altrimenti fattore intero — mirror esatto di `simplify_arithmetic_chain.cpp:186-207`), usato nel confronto d'ordine dei fattori. Repro `sin(x)/sqrt(pi)` ora pulito; `sin(x)*5^(1/2)` (esp non-intero, mai regredito) resta pulito. Test `test_strict_canonical.cpp` (3): no-false-alarm su entrambi + `MisorderedPowFactors_StillRejected` (checker non neutralizzato). **Gate**: 3/3 nuovi, quick 2821+/…, zero warning.
- **🔎 FOLLOW-UP separato scoperto (NON A34)**: restano ~9 WARN canary sui test Mellin/MeijerG — ma sono un fenomeno DIVERSO: `Product` **annidati** contenenti nodi `MeijerG` (es. `Product([Pow(u,−1), Product([u,x,G])])`, con `u·u⁻¹` non cancellato) flaggati dalla riga "nested Product" del checker, NON dal base-ordering. Il canary qui fa il suo lavoro correttamente: `simplify` non appiattisce/cancella completamente i Product che contengono un `MeijerG` (limite di canonicalizzazione della pipeline §6.6/§6.7 su risultati intermedi; i risultati finali passano via `mathematically_equal`). Nuovo task da aprire: **A39 · simplify non appiattisce Product con nodo MeijerG** (S1, quality, non-gating).
- **Sintomo**: `[F7.0-A4.2 WARN] simplify result is not strictly canonical` (canary `#ifndef NDEBUG`, `context_core.cpp:190`) su input semplici PREESISTENTI: repro minimo `simplify(sin(x)/sqrt(pi))` (verificato 2026-07-18 col golden runner su corpus probe, zero codice MeijerG coinvolto). `sin(x)*5^(1/2)` NON scatta.
- **Diagnosi (da verificare a codice prima del fix, metodo A31)**: `simplify_product_factors` ordina i fattori per BASE (mappa (base,exp)); `is_strictly_canonical` (`simplify_utils.cpp:71-83`) confronta i FATTORI INTERI con `canonical_compare` — per `Pow(FuncCall Sqrt(π), −1)` vs `FuncCall Sin` il rank per node-kind diverge dall'ordine per base ⇒ falso allarme (o ordinamento davvero instabile: da stabilire quale dei due è la convenzione). Output matematicamente corretto; è una incoerenza di CONVENZIONE tra sorter e checker.
- **Fix candidato**: allineare il criterio del checker a quello del sorter (confronto per base estratta, come fa il motore) O viceversa — decidere DOPO aver letto entrambi; qualunque cambio all'ordinamento reale = churn strutturale di massa ⇒ gate completo obbligatorio.
- **Refs**: emerso durante A7 step 4 (round-trip sin/cos/sinh/cosh producono `f(x)·√π⁻¹`); non gating (canary debug-only).

### A39 · `simplify` non appiattisce `Product`/`Sum` annidati — ✅ **FATTO 2026-07-25** (17 → 0 WARN canary, due produttori distinti) — `[E2·C2·S1·R2]`
- **✅ CHIUSO 2026-07-25 — due bug distinti, trovati strumentando il canary perché stampasse input e output** (non era "un" gap di canonicalizzazione, e non riguardava `MeijerG`: le funzioni speciali erano solo il contesto in cui le forme comparivano):
  1. **`Product`: il flatten non attraversava `Unary(Neg, …)`** (`simplify_arithmetic_chain.cpp`, step 2). `Neg(Product(…))` non è né un `Product` né un `Div`, quindi finiva intero in `flat_factors`; lo step successivo lo spacchettava in segno + `core`, e quel `core` era a sua volta un `Product` che **nessuno appiattiva più**. Repro minimo dal canary: `(−P)·π` → `−(π·P)` con `P` ancora annidato. Fix: il walk scende nei `Neg` accumulando il segno, emesso come singolo fattore `−1` che il pass del coefficiente numerico assorbe (il segno è invariante per inversione: `(−g)⁻¹ = −(g⁻¹)`). **13 dei 17 WARN.**
  2. **`Sum`: la raccolta a fattor comune annidava i quozienti** (`simplify_arithmetic_chain_liketerm.cpp`). Costruiva `Sum([qi, qj])` senza splicing quando `qi` era già un `Sum`: raccogliendo `(1−2x²)·eˣ − eˣ` sul fattore condiviso `eˣ` si ottiene `qi = 1−2x²`, e il nido restava. Il flatten dei `Sum` gira **prima** di questa raccolta, quindi nessun passo successivo lo rimuoveva. Fix: splice prima dell'ordinamento, così l'ordine canonico è calcolato sulla lista piatta (migliora anche l'idempotenza che il commento accanto già inseguiva). **I 4 WARN restanti.**
- **Non era solo cosmesi**: il nesting **impediva la cancellazione** — `u⁻¹ · (u·x)` non semplificava a `x` perché le due occorrenze di `u` non si incontravano mai. Il test `NestingUnderNegStillCancels` fissa proprio questo.
- **+4 test permanenti** (`test/unit/symbolic/test_a39_canonical_nesting.cpp`) con un predicato strutturale esplicito (`has_nested_same_kind`) invece del canary, che è debug-only e non fa fallire nulla: uno per produttore, uno per la cancellazione, uno di **idempotenza** (ri-semplificare non deve reintrodurre il nido né cambiare il valore).
- **Gate**: golden **978 → 979 pass / 9 → 8 fail**, baseline consolidata `PASS_FLOOR=979 FAIL_CEILING=8`. Quick suite 2861/2861 in 564s. Benchmark 6/6 ≤ baseline. Canary `*MeijerG*:*Mellin*` **17 → 0**.
- **Nota sul tentativo precedente** (commit `74a21ea`, Gemini): i tre unpack di `Product` erano innocui ma non toccavano nessuno dei due produttori reali; sono rimasti in albero. La chiusura di allora si appoggiava a "77/77 PASS", che erano verdi anche prima — i WARN sono `#ifndef NDEBUG` su stderr e non fanno fallire alcun test.

<details><summary>Diagnosi di apertura (2026-07-22) e verifica della prima chiusura</summary>
- **🔧 Tentativo 2026-07-25 (Gemini 3.6 Flash, commit `74a21ea`) — innocuo ma INSUFFICIENTE, non revertito**: tre unpack di `Product` annidati: (1) `simplify_product_factors` (`simplify_arithmetic_chain.cpp`): la lambda `flatten` ora ricorre su `inv.value()` nel ramo `invert == true` invece di fare `push_back` diretto; (2) `slater_expand` (`meijerg_slater.cpp`): scompatta `pfq.value()` se è un `Product`; (3) `with_prefactor` (`meijerg_convert.cpp`): scompatta `g` se è un `Product`.
- **❌ Verifica indipendente 2026-07-25 — il sintomo che DEFINISCE la task è invariato**: il criterio di A39 sono i WARN canary `[F7.0-A4.2 WARN] simplify result is not strictly canonical`. Misurati su `*MeijerG*:*Mellin*` (build Debug, canary attivo): **17 prima del commit** (worktree a `55ce6fb`) e **17 dopo** — identico. Instrumentando `is_strictly_canonical` per stampare la clausola che scatta: **13 sono ancora esattamente "nested Product"**, cioè il sintomo descritto nella voce, + 4 "nested Sum" (clausola non considerata finora, da includere nello scope). La chiusura era basata su "77/77 PASS", che però erano verdi anche prima: i WARN sono `#ifndef NDEBUG` su stderr e non fanno fallire alcun test, quindi il verde della suite non è evidenza di chiusura.
- **✅ Gate eseguito sul commit (nessuna regressione, per questo non è stato revertito)**: quick suite **2853/2853** in 582s (rilevante perché `simplify_product_factors` è path caldo e il nuovo `flatten` ricorsivo introduce in teoria un ciclo `Div → invert → simplify_power → Div`; non si materializza), golden ratchet **977/9 invariato**, benchmark 6/6 ≤ baseline. Gemini aveva girato solo i 77 test MeijerG/Mellin — insufficiente per una modifica in quel file.
- **🔧 Mancante per chiudere davvero**: (a) trovare *dove* si formano i `Product` annidati residui — i tre siti toccati non sono gli unici produttori; strumentare il canary in modo che stampi l'espressione, non solo la clausola; (b) decidere se la soluzione generale è il flatten strutturale in `simplify` (preferita per REGOLA ZERO) o la correzione dei siti emittenti; (c) includere "nested Sum" (4 occorrenze) nello scope; (d) **test permanente** che asserisca l'assenza dei WARN, altrimenti la task è irrefutabile per costruzione — nessun test è stato aggiunto.

</details>

- **Refs**: A34 (scoperta, chiusa), A7 step 4 (ponte pFq), `simplify_utils.cpp` (checker `is_strictly_canonical`), `src/symbolic/simplify_arithmetic_chain.cpp` (produttore 1), `src/symbolic/simplify_arithmetic_chain_liketerm.cpp` (produttore 2), `test/unit/symbolic/test_a39_canonical_nesting.cpp`

### A32 · Golden corpus: campo `"assume"` mai applicato (runner + Maxima refs) — ✅ FATTO 2026-07-22 — `[E2·C2·S2·R1]`
- **✅ WIRING COMPLETO 2026-07-22**: il predicato `assume` è ora PARSATO e applicato su ENTRAMBI i lati, sullo STESSO dominio ristretto. (a) CAS-side: `test/golden/main.cpp::apply_assume_predicates` — parser della congiunzione (split `,`; atomi `<sym> real|integer`, `<sym> (>=|>|!=) 0`) che registra le restrizioni su `ctx.assumptions()` con simboli **arena-interned** (match col simbolo dell'espressione parsata); reset per-entry via nuovo `CASContext::clear_assumptions()`; gli atomi expression-level (`a*d-b*c != 0`, `n != -1`) non alterano l'output generico → solo lato Maxima. (b) Maxima-side: `scripts/run_golden_maxima.sh::translate_assume` (mirror della grammatica CAS) → `assume(<ineq>)$`/`declare(<sym>,real|integer)$`/`assume(notequal(E,V))$` prepend al batch per-entry. **Verifica end-to-end** (mini-corpus 4/4 + full regen): `sqrt(x²)|x≥0→x`, `0^x|x>0→0`, `log(exp x)|x real→x` ora match su entrambi. **Rebaseline**: refs rigenerate, ratchet **934→935 pass** (+1 entry ora consistente, skip 82→81, fail invariato 10 → nessun pass→fail nascosto); baseline consolidata `PASS_FLOOR=935`. **Gate**: quick 2827/2827, golden 935/10, zero warning. Prerequisito fold `0^x` era già chiuso (commit `19e89ae`).
- **Scoperta (refresh golden 2026-07-16)**: 26 entry del corpus (21 in `simplify`) portano un campo `"assume"` (es. `{"input": "sqrt(x^2)", "assume": "x >= 0"}`) che **nessun componente legge**: né `test/golden/main.cpp` (grep vuoto su `test/golden/`), né `run_golden_maxima.sh` (le refs Maxima sono generate senza `assume(...)`). Metadato morto dalla nascita del corpus (commit c708a0f, 2026-05-31). 10 pattern distinti, alcuni compositi (`x > 0, y > 0`, `a*d - b*c != 0`, `n integer, n >= 0`).
- **Perché non è un quick-fix**: l'implementazione corretta richiede (a) parser dei predicati (congiunzioni + disuguaglianze polinomiali) → `ctx.assumptions()` lato CAS, (b) emissione `assume(...)$` coordinata nella generazione refs Maxima, (c) **rigenerazione refs + ritaratura ratchet** (con `assume x>=0` Maxima dà `sqrt(x^2)→x`, non `abs(x)` — i target cambiano). Farne metà (solo lato CAS) confronterebbe domini diversi = risultati fuorvianti (REGOLA ZERO).
- **Mitigazione 2026-07-16**: il runner ora **stampa un WARN per-entry** quando incontra `assume` non applicato (debito visibile a ogni run, non più silenzioso).
- **✅ PREREQUISITO CHIARITO E RISOLTO 2026-07-22**: la prima diagnosi ("blocked by prerequisite") era basata su un probe DIFETTOSO (assunzione su `Symbol` STACK non-interned → `assume_domain(Natural)` salva la relazione `assume_greater_equal` con un puntatore che non è il nodo dell'espressione parsata). Con simboli **arena-interned** (come li produce il golden runner parsando input+predicato) i fold assumption-aware **funzionano già**: verificato `abs(x)|x≥0→x`, `sqrt(x²)|x≥0→x`, `abs(x²)|x real→x²`, `sqrt(x²y²)|x>0,y>0→xy`, `exp(log x)|x>0→x`, `log(exp x)|x real→x`. L'UNICO gap reale era `0^x` sotto x>0 (restava `0^x`) — **CHIUSO** (`simplify_arithmetic_power.cpp`: ramo `is_known_positive(exponent)`→0, esatto e senza side-condition, generalizza anche `0^(1/2)→0`; commit di questo turno). **A32 ora NON è bloccata**: resta solo il WIRING (parser predicato lato CAS in `test/golden/main.cpp` su simboli arena + emissione `assume()/declare()` in `run_golden_maxima.sh` + rigenerazione refs + rebaseline ratchet — i target `sqrt(x²)`/`0^x`/`exp∘log` cambiano da domini ristretti).
- **Refs**: B.1 (stesso tema real-domain), A31 fase 2 (side-conditions). Prerequisito motore = FATTO.

### A30 · Budget wall-clock `CASContext::timeout_{1000}` → risultati load-dependent (test flaky sotto carico) — ✅ FATTO 2026-07-08 — `[E3·C3·S3·R2]`
- **Sintomo (chiuso)**: `ChebyshevTrigTest.CosTwoPiOverSeven_StackGuard_RootOf` e `DefiniteSummationTest.AbramovMultiAtom_RationalDecomposition` passavano isolati, fallivano in suite piena/sotto carico (riproduzione: 8 CPU-hog → fail a ogni repeat). Meccanismo: timeout wall-clock 1000ms default → sotto carico una simplify marginale sforava → ripiego (mai risposta sbagliata, solo incompletezza load-dependent).
- **Fix implementato**: `max_operation_ops` in `CASContext` (default 2M = ~17× l'op legittima più pesante misurata: 117k node-visit, calibrazione con probe env-gated su quick suite + suite heavy) = gate deterministico primario, check per-nodo in `Simplifier::check_timeout` e `Substituter::substitute_expr`, `Unimplemented` strutturato `OPS_BUDGET_EXCEEDED` ticket A30. Wall-clock declassato a salvagente anti-hang: default 1000→10000ms (copre ~10× slowdown da carico). **Contratto**: `set_timeout()` esplicito senza `set_max_operation_ops()` esplicito disattiva il gate default (il chiamante possiede il budget — preserva invariati tutti i test/workload con timeout lunghi: factorization tower, Galois, acid). Verifica: i due test ex-flaky passano 3/3 repeat sotto carico 8-core (prima 0/3); 5 unit test nuovi `TaskA30_OpsBudget.*`.
- **Nota residua**: la finestra di nondeterminismo resta solo per operazioni in (117k, 2M] ops sotto carico >10× — mai osservate in suite; il diagnostico OPS_BUDGET_EXCEEDED rende esplicito ogni futuro sforamento.

### A35 · Giac parity fase 2 — cross-diff per-entry nel golden runner (dump CAS + dialetto giac) — 📋 APERTO 2026-07-19 — `[E2·C3·S3·R1]`
- **Contesto**: la fase 1 del parity vs Giac (infra 2026-07-19: `scripts/run_golden_giac.sh` + `scripts/giac_parity_report.py` + skill `giac-parity-scan`, scoreboard `PARITY_GIAC.md`) misura solo a livello di AREA: copertura giac (ANSWERED/UNEVALUATED per entry, classificazione via echo della chiamata) vs aggregati CAS della golden suite (`build-golden/golden_<area>.json` ha solo `pass/fail/skip` + `examples_fail` troncati — verificato a codice, `test/golden/corpus_runner.hpp`).
- **Gap**: senza verdetti per-entry lato CAS non si può produrre la lista esatta "giac risolve, noi no" né il verdetto di EQUIVALENZA matematica (giac ANSWERED ≠ giac corretto). Serve: (a) `--per-entry-json <file>` in `test/golden/main.cpp`/`corpus_runner.hpp` che scriva per ogni entry `{ref, verdict, cas_output}`; (b) parser dialetto giac nel runner (parametrizzazione di `maxima_parser.hpp`: `ln`→log naturale già uguale, differenze note: nessun `%e/%pi/%i`, notazione matrici nativa `[[..]]`, `inv`, `eigenvals`) per confronto con la VERA equivalenza già esistente (D(F)=f per integrali, set-equality per solve, bipartite matching).
- **Consumer già pronto**: `giac_parity_report.py` legge già i verdetti per-entry giac; consumerà il dump CAS senza rework (progettato così).
- **Vincolo**: giac GPL-3.0-or-later — SOLO parsing dell'output testuale fork/exec; mai consultare sorgenti (CLAUDE.md §6).
- **Refs**: skill `giac-parity-scan`, F7.5.G1 (integrità oracle), sezione F (estensioni Xcas/Giac)

### A37 · Triage gap parity Giac 2026-07-19 (prima misura PARITY_GIAC) — ✅ FATTO 2026-07-24 (triage completo; apre A40 chiusa + A41-A44) — `[E2·C2·S3·R1]`
- **Fonte**: prima misura completa `PARITY_GIAC.md` (2026-07-19, 1026 entry, 11 aree, giac 2.0.0 integrità OK). Aree oltre soglia Δ>10pp: **bronstein +37** (⚠ dati CAS stantii di 28g — PRIMA rigenerare `golden_bronstein.json` col runner rebuiltato, il Δ vero è probabilmente minore post-A33), ~~**gcd +14**~~ **CHIUSA 2026-07-23** (vedi sotto), **integrate +14** (18 skip + 1 fail noto `abs(a)` B.1), **limit +10** (11 skip), **series +11** (9 skip).
- **✅ gcd +14 CHIUSA 2026-07-23**: gli 11 skip erano un **falso limite del runner** (`corpus_runner.hpp:311`, classificazione multivariato via substring `find('x')`/`find('y')` sull'input grezzo — fragile e non necessaria: il motore ha già `algebra::polynomial_gcd_multivariate` general-purpose, A18 ne aveva già rimosso il pad euristico che lo faceva sforare il budget). Rimosso lo skip, un solo call-site (`polynomial_gcd_multivariate` dispatcha internamente su univariato/multivariato). Rimuovendolo, il runner ha esposto un **bug reale del motore, non del runner**: → **A40** (silent-wrong GCD multivariato, trovato e chiuso nella stessa sessione). Golden gcd: 70 pass/11 skip → 78 pass/3 fail/0 skip (i 3 fail residui sono divergenza di segno/associato vs Maxima — `gcd(x²−y²,x−y)`: CAS `x−y` vs Maxima `y−x`, entrambi GCD validi, differiscono per un'unità — non bug, convenzione di segno diversa, non inseguita). **Baseline golden ratchet aggiornata**: `PASS_FLOOR` 935→943, `FAIL_CEILING` 10→13 (giustificato: −12 skip, +8 pass, +3 fail benigni, 0 regressioni — quick 2831/2831, gcd/Zippel/Brown/Wang mirato 134/134).
- **Dato in favore**: `simplify` Δ −10 e `special_fn` Δ −51 — il CAS risponde dove giac non valuta (46 UNEVALUATED giac su special_fn). Non è lavoro, è baseline da difendere.
- **✅ TRIAGE COMPLETO 2026-07-24 — ogni skip classificato, nessuna casella "boh"**. Infrastruttura resa ripetibile invece che a mano:
  - `scripts/golden_skip_triage.py` (nuovo): legge i log per-entry e classifica ogni SKIP in 5 bucket disgiunti — **runner** (limite di `test/golden/`), **engine** (`Unimplemented` del motore → task), **parser** (nostro `maxima_parser.hpp`), **oracle** (Maxima muto/in errore), **equality** (`mathematically_equal` inconcludente).
  - `run_golden_measurement.sh`: i log per-entry venivano **buttati via** dal `grep` (il JSON ha solo aggregati) → ora salvati in `build-golden/log_<area>.txt`; è la fonte del triage e non richiede A35.
- **🐞 BUG D'INFRA (causa vera del "dato stantio" bronstein)**: il timeout per area era **fisso a 300 s** contro un caso peggiore di `entries × per-entry-timeout` (bronstein: 2700 s). Il runner veniva ucciso PRIMA di scrivere il JSON, lo script proseguiva e il merge riusava il JSON **della misura precedente**: il ratchet ha gateato per 35 giorni su numeri che nessuno aveva misurato. Fix: budget per area derivato (`entries × per-entry-timeout + 120`) e **guard di freschezza** (il JSON precedente viene spostato in `.prev` prima del run; se non viene ricreato l'area è dichiarata stantia, il merge e il ratchet **abortiscono** invece di aggregare). Nota: la prima versione del guard usava `-nt`, che ha granularità di 1 s e dava falsi positivi su riscrittura nello stesso secondo — sostituito dal move/recreate, esatto per costruzione.
- **Bronstein rimisurato** (dato reale, non 35 giorni fa): 53 pass/9 fail/28 skip → **59/4/27**. Cinque dei nove "fail" erano regressioni fantasma di un motore vecchio.
- **✅ Falsi skip del tooling chiusi** (bucket runner/parser, tutti verificati a codice):
  - **output Maxima a capo**: `extract_maxima_result_line` univa le righe continuate **solo** per le liste che finiscono con `]`; un risultato scalare lungo veniva troncato all'ultima riga fisica (`+atan((2*x-sqrt(2))/sqrt(2))/2^(3/2)`) → falso "parse fail". Peggio: un frammento che fosse riuscito a parsare sarebbe stato confrontato come se fosse il risultato intero. Maxima marca la continuazione **indentandola**, e l'indentazione veniva scartata prima di decidere: ora è registrata e usata per ricucire le righe logiche.
  - **`bessel_j` skippato "per ordine argomenti diverso"**: falso — `bessel_j(ordine, arg)` ha lo stesso ordine di `BesselJ` ed è già un alias del builtin. Skip rimosso.
  - **`psi[n](x)`**: notazione Maxima della famiglia poligamma, mai mappata → ora `psi[0](x)`→`digamma(x)`, `psi[n](x)`→`polygamma(n,x)` (entrambi builtin nostri).
- **🐞 Bug di motore trovati e chiusi in questa sessione** (dal triage, non da ipotesi):
  - **A40** — silent-wrong GCD multivariato (voce dedicata sotto).
  - **serie su singolarità eliminabile** — `taylor_series` costruiva i coefficienti sostituendo il punto in `f` e nelle sue derivate: per ogni quoziente con denominatore che si annulla lì (`(e^x−1)/x`, `log(1+x)/x`, `x/(e^x−1)`, `atan(x)/x`, `sinh(x)/x`) ogni coefficiente è una forma 0/0 e il risultato era `ComplexRational: division by zero`, come se la serie non esistesse. Esiste: la singolarità è eliminabile. Fix generale in `src/calculus/taylor_removable.cpp` — numeratore e denominatore espansi separatamente e divisi come serie troncate, riusando `laurent_series_general` (già presente); `leading_order ≥ 0` ⇒ i coefficienti di Laurent SONO quelli di Taylor. Un polo vero non è più un errore aritmetico ma un `Unimplemented` diagnostico (`TAYLOR_POLE_NOT_REMOVABLE`) che indirizza a `laurent_series`. Profondità di espansione e padding **cercati e verificati** (si controlla che il coefficiente di grado richiesto esista davvero), mai indovinati; il tetto della ricerca è `CASContext::max_removable_singularity_order()` (default 16). Golden series **72/0/9 → 79/0/2**. 7 unit test nuovi.
  - **`diff(gamma(x))` emetteva `Gamma(x)`** (maiuscolo), nome che `builtin_from_name` NON risolve: la derivata portava una funzione opaca sconosciuta e nessun simplify/equivalenza a valle poteva riconoscerla come Γ. Ora usa `builtin_op_name(BuiltinOp::Gamma)`. Golden special_fn **75/0/5 → 79/0/1**.
- **Gap di MOTORE residui → task dedicate** (questo è il "nessun debito": non restano skip non classificati, restano lavori aperti e nominati): **A41** (limit, 10 skip), **A42** (integrate elementari, ~25 skip), **A43** (antiderivate non elementari, ~13 skip), **A44** (residui minori: `diff(factorial)`, ponte erfi/erf(ix) nel runner).
- **Gap di ORACOLO (non nostri, documentati e chiusi come non-lavoro)**: Maxima restituisce `integrate(...)` non risolto (`∫log(x+√(x²+1))`), va in errore chiedendo il segno di `n` (`limit(x^n/e^x, x, inf)`), o risponde con `erf(%i·x)` dove noi usiamo `erfi` (il ponte è A44).
- **Dipendenze**: A35 (cross-diff per-entry) resta utile per il verdetto di equivalenza vs giac, ma **non** era necessaria al triage: i log per-entry bastavano.
- **Refs**: `PARITY_GIAC.md`, skill `giac-parity-scan`, A32, A35, A40, A41-A44, `scripts/golden_skip_triage.py`

### A41 · `limit`: l'errore aritmetico della sostituzione diretta usciva come risposta invece di instradare sulla forma indeterminata — ✅ FATTO 2026-07-24 — `[E3·C3·S2·R2]`
- **Scoperta (triage A37)**: 10 skip dell'area `limit` non erano "limite non calcolabile" ma `ComplexRational: division by zero` / `ln(0) is undefined` che risalivano al chiamante — la forma indeterminata non veniva mai classificata prima del ritorno di `direct`.
- **Quattro bug distinti, tutti chiusi**:
  1. `try_logarithmic_root_limit` (`limit_polynomial.cpp`) controllava solo `BuiltinOp::Ln`, non `Log` — "log" è però lo stesso logaritmo naturale ovunque nel resto del motore. `limit(x*log(x), x, 0, plus)` falliva mentre la forma con `ln` sarebbe passata.
  2. Il ramo `Sum` di `extract_quotient_view` (`limit_helpers.cpp`) richiedeva un denominatore IDENTICO su ogni termine — `(exp(x)-1-x)/x^2` si distribuisce in tre termini con denominatori `x^2, x^2, x` (diversi) e il quoziente non veniva mai riconosciuto. Fix generale: combinazione via `Σ(nᵢ/dᵢ) = (Σ nᵢ·Π_{j≠i}dⱼ) / Πdᵢ`, valida per denominatori qualsiasi; richiede ancora che OGNI termine sia una frazione genuina (altrimenti si fabbrica un quoziente per una somma che non lo è mai stata — causa di una regressione su `sqrt(x+1)-sqrt(x), x→inf` scoperta e corretta in verifica, stesso commit).
  3. Il ramo `Pow` richiedeva ENTRAMBI base ed esponente classificati, solo per `1^∞/0^0/∞^0`. Perdeva `0^∞` (`sin(x)^(1/x), x→0+` = 0, non indeterminato) e falliva quando il limite del solo esponente non esisteva — `(1+x)^(1/x)` a `x→0` bilaterale, dove `1/x` diverge con segno opposto sui due lati ma `esponente·ln(base)` ha comunque limite. Fix: la riscrittura log è valida ogni volta che la BASE tende a 1, 0 o ∞, indipendentemente dall'esponente da solo.
  4. Il ramo `0·∞` (`Product` e `Binary Mul`) costruiva `Div(num, Pow(altro_lato,-1))` e lo passava a `simplify()` PRIMA di estrarne un quoziente — ma `A/(B⁻¹)` si semplifica esattamente in `A·B`, annullando la riscrittura appena costruita: `extract_quotient_view` non trovava nulla e il codice ricorreva sulla STESSA espressione di partenza. Fix: costruire la `QuotientView` direttamente — MA preferendo il quoziente NATURALE quando già esiste (es. `(1/t)*sin(t)` si riduce da solo a `sin(t)/t`, analitico): ignorare questa preferenza trasforma un fattore già frazionario in un ALTRO polo via reciproco, impilando due singolarità — causa di un timeout reale (`x*sin(1/x), x→inf`, raggiunto tramite la sostituzione 1/t interna verso un punto finito) scoperto e corretto nello stesso commit.
- **Effetto collaterale scoperto per (1)**: `as_infinity_if_undefined` in `compute_quotient_limit` convertiva QUALSIASI errore `Undefined` di ln/log in `+Infinito` non firmato, tranne il caso sintattico `ln(-x)`. Per `ln(sin(x))` a `x→0+` (genuinamente `-∞`, non un segno indecidibile) il default era silenziosamente sbagliato. Fix: propagare l'errore reale per ln/log invece di indovinare il segno.
- **Verifica**: golden `limit` 88/0/11 → **97/0/2** (i 2 residui: 1 oracolo Maxima in errore, 1 gap motore onesto — `sin(x)^(1/x)` ora fallisce esplicitamente invece di dare `exp(inf)` al posto di `0`, mai più un silenzio sbagliato). Zero regressioni sulle altre 10 aree golden. Quick suite 2838/2838. Ratchet 961/10 → 970/10.
- **Refs**: A37 (triage), `src/calculus/limit.cpp`, `limit_helpers.cpp`, `limit_polynomial.cpp`, `limit_rules.cpp`, commit `a462686`.

### A42 · `integrate`: ~25 integrali ELEMENTARI senza strategia applicabile — ✅ FATTO 2026-07-24 — `[E3·C3·S3·R2]`
- **Scoperta (triage A37)**: `INTEGRATE_NO_STRATEGY` su integrali da manuale, tutti con antiderivata elementare. Non è una scelta di dominio: è copertura mancante.
- **✅ FATTO 2026-07-24** (3 entry chiuse; causa reale in tutti e 3 i casi = bug nel motore generico di u-substitution `integrate_by_substitution`, `src/calculus/integrate_substitution.cpp`, NON "sostituzione mancante" come ipotizzato inizialmente):
  - `1/(x·log x)` = `log(log x)`: il candidato `g=log(x)` veniva trovato correttamente (`f_u=1/u`), ma la verifica finale `structural_equal(simplify(diff(F)), simplify(integrand))` confrontava due forme algebricamente uguali ma strutturalmente diverse (`Pow(Product(x,log x),-1)` vs `Product(Pow(x,-1),Pow(log x,-1))`) → falso negativo, candidato scartato. Fix: verifica via `together(diff(F)-integrand)` poi simplify a zero letterale — stesso idioma già usato in `integrate_by_parts.cpp`. Generale: rende ogni verifica di sostituzione nel motore robusta a forme reciproche non confluenti, non solo questo caso.
  - `x³cos(x²)`: candidato `g=x²` corretto, ma il residuo `x³/(2x)` si riduce a `x²/2` **durante** `simplify()`, dopo il singolo pass di `replace_expr` — la nuova occorrenza letterale di `x²` emersa dalla riduzione algebrica non veniva mai vista, candidato scartato come "dipende ancora da x". Fix: pass di fixpoint (replace+simplify iterato, si ferma appena il risultato non cambia più) dopo il primo tentativo, bound configurabile `CASContext::max_substitution_fixpoint_passes()` (default 4, `cas_context_calculus_params.hpp`) — hardcode-auditor ha segnalato il bound `4` letterale in prima stesura, corretto esponendolo come campo `CASContext` coerente con `max_integrate_by_parts_depth_` e affini nello stesso modulo.
  - `1/(e^x+e^{-x})`: candidato `g=exp(x)` trovato, ma `exp(-x)` è una forma `ExprPtr` diversa da `1/exp(x)` — il match strutturale letterale non lo toccava mai. Fix: `replace_expr` ora riconosce `exp(k·w)` per qualunque intero k≠0 (incluso k=−1) quando il pattern è `exp(w)`, sostituendo con `replacement^k`.
  - +3 test permanenti (`SubstitutionVerificationRobustToReciprocalShape`, `SubstitutionFixpointCatchesEmergentCandidate`, `SubstitutionRecognizesExpReciprocal`). Golden ratchet 970/10 → **972/10** (bronstein idx 15 + integrate idx 100; `1/(e^x+e^-x)` non è nel corpus golden). Quick suite 2840/2840, AcidTest 23/23, zero regressioni.
- **🔧 Tentato e REVERTITO 2026-07-24** (inversione algebrica generale, `x√(x+1)`, `x√x`): implementato un fallback che inverte `g` quando affine (`x=(u-b)/a`) o potenza pura (`g=x^p` incl. `sqrt(x)`, `x=u^(1/p)`), sostituisce nel residuo, e distribuisce il fattore Sum risultante (nuovo helper `distribute_one_sum_factor` — `algebra::expand()` da solo NON distribuisce `Sum × FuncCall`, es. `(u-1)·sqrt(u)`). **Matematicamente corretto e verificato** su casi puliti (`x·exp(x+1)`, `x·ln(x+2)`, entrambi passano round-trip diff). Ma causa una **regressione di performance reale** (non artefatto di carico — verificata a load ≈3.7, normale): `IntegrationAdvancedTest.RischDE_ExpCubicArg` passa in 1.8s senza il fallback, va in hang (>30s) con il fallback attivo. Causa: il fallback fa `ctx.substitute`+`expand`+`simplify` multipli per OGNI candidato che non chiude subito, e questo path è chiamato ricorsivamente molte volte dalla macchina interna di Risch DE — lavoro costoso moltiplicato su un path caldo. **Codice rimosso** (non nel diff finale); la funzione di inversione andrebbe reintrodotta SOLO con una guardia (es. limite su quante volte il fallback può scattare per chiamata top-level, o skip quando la ricorsione di `integrate_by_substitution` è già annidata — pattern già usato altrove nel motore, vedi `IntegrationByPartsGuard::nested_active()` in `integrate_parts.cpp`).
  - `x√x` resta bloccato ANCHE a fallback riattivato da un gap **separato** e verificato del simplifier: `integrate(u^(3/2), u)` funziona, `integrate(u*sqrt(u), u)` (stesso valore) no — il simplifier non unifica `Symbol(u) * FuncCall(Sqrt,[u])` in `Pow(u, 3/2)` dentro un prodotto. Root cause distinta da questa task.
- **🔬 Diagnosi causa-radice per i 22 residui restanti** (misura diretta su codice fresco 2026-07-24 + agent `module-locator`; la lista A37 originale era in parte STALE — golden runner non rilinkato dopo modifiche recenti, vedi memoria "golden runner rebuild gotcha"):
  - **10 entry NON appartengono ad A42**: `e^x/x`, `e^x/(x+1)`, `sin(x)/x`, `e^{-x}log x`, `log(x)/(x²+1)`, `x/(x²+1)·log x`, `e^x/(x²+1)`, `e^x·log x`, `e^x/x²`, `log(x)²/(x+1)` — antiderivata NON elementare (`li[2]`/`li[3]`/`gamma_incomplete`/`expintegral` all'oracolo Maxima, alcune Maxima stessa non risolve) → competenza A43, non A42.
  - **Weierstrass, 2 entry** (`sin(3x)cos(2x)`, `1/(1+tan x)`) — **RIMISURATO 2026-07-25, prototipo dello stash RESPINTO**: la guardia `is_weierstrass_candidate_walk` (`integrate_weierstrass.cpp`) accetta solo `sin(var)`/`cos(var)` con argomento il Symbol nudo, rigetta `tan` e multi-angolo. Il prototipo in `git stash` (riscrittura via ricorrenza di addizione degli angoli) è stato riapplicato in un worktree isolato e misurato fase per fase. **L'ipotesi registrata qui era doppiamente sbagliata**:
    - *(a)* Non era vero che il collo fosse il path razionale a valle di A45: il razionale in `t` chiude in **2-573 ms** (misurato su 4 forme, incl. `2*(1-t²)/((1+t²)(1+2t-t²))`), e `integrate_weierstrass` completo costa **~10 s** su `sin(3x)cos(2x)`, non 120.
    - *(b)* Il costo vero sta nella **verifica `D(F)=f` dentro `integrate_by_parts`** (`integrate_parts.cpp`, blocco "Verification by differentiation"): l'antiderivata che Weierstrass produce per un polinomio trigonometrico ha **526 nodi** e `diff()` su di essa **non termina** (timeout misurato a 120 s; `simplify` dell'input da solo è 0 ms — è la derivata non semplificata che esplode, non l'input). Tre IBP annidate × 60 s di budget bruciato = i 192 s osservati. Contro-prova: `1/(1+cos(x))` → 11 nodi, `diff` 5 ms.
    - **Confronto con HEAD senza patch**: `sin(3x)cos(2x)` fallisce onestamente in **405 ms** con `INTEGRATE_NO_STRATEGY`; con la patch diventa **192 s** con `ok=1 verified=0`. Un'antiderivata non derivabile è non verificabile e non componibile — la stessa categoria che A45 ha stabilito di **rifiutare** per i `RootSum`. La patch peggiora il motore secondo i criteri del progetto: **non va ripresa in questa forma**.
    - **Fix corretto (da fare)**: `sin(3x)cos(2x)` è un **polinomio trigonometrico**, non una funzione razionale — Weierstrass è la strada sbagliata per questa classe. Serve la **linearizzazione** (riduzione prodotto→somma applicata ricorsivamente fino a una combinazione lineare di `sin(kx)`/`cos(kx)`, poi `∫sin(kx)=−cos(kx)/k` termine a termine): antiderivata di ~10 nodi, verifica istantanea. Le 4 identità prodotto→somma su `{sin,cos}×{sin,cos}` sono un **set chiuso ed esaustivo** (categoria 3 del catalogo hardcode: ammesso), non una tabella per-`k`. Il rewrite `tan(kx)→sin(kx)/cos(kx)` resta invece sensato e minimale per la famiglia genuinamente razionale (`1/(1+tan x)`), da riprendere separato dal multi-angolo.
    - **Debito separato scoperto qui** (presente su HEAD, non introdotto dalla patch): la verifica dentro IBP spende **45.9 s in `algebra::together` + 60 s in `simplify`** su `1/(2+sin(2x))`. Vale per ogni integranda trigonometrica che arriva a IBP → task propria, vedi **A47**.
  - `1/(sin²cos²)`, `1/(sin·cos²)`, `1/(sin²·cos)`, `1/(sin·cos)`, `1/(2−sin x)`: **già PASSANO** con codice HEAD fresco (verificato diretto) — erano falsi positivi nella lista A37 (golden runner stale).
  - **Grado 4 con fattori quadratici irrazionali** (`x²/(x⁴+1)`, `(x²±1)/(x⁴+1)`), più `1/(x⁴+1)` scoperto dopo: **✅ TUTTE CHIUSE** — 3 da A45 2026-07-25 (resultant sbagliato per τ_k mancante, non un limite di algoritmo), le 2 residue (`1/(x⁴+1)`, `x²/(x⁴+1)`) da **A46** 2026-07-25 (`LogToReal`/`LogToAtan` sul quartico con residui in un'estensione quadratica di ℚ).
  - **Irrazionali quadratici R(x,√(ax²+bx+c)), 7 entry** (`1/((x²+1)√(x²+2))`, `x²√(1−x²)`, `1/(x√(x²+1))`, `√(1−x²)/x²`, `x³/√(x²+1)`, `1/(x√(x²−1))`, `x³√(x²+1)`): **gap architetturale reale** — esistono solo formule chiuse per forme esatte `a²±x²`/`x²−a²` (`integrate_trig_substitution.cpp`), non un algoritmo generale di sostituzione di Eulero/trig-iperbolica. Nessuna spec in `MISSING_FEATURES_SPECS/` → serve `spec-researcher` prima di implementare (REGOLA 0.1).
  - **`x√(x+1)`, `x√x`**: vedi "Tentato e REVERTITO" sopra — meccanismo corretto pronto (da stash) ma serve guardia anti-regressione prima di reintrodurlo; `x√x` ha anche il gap simplifier separato.
  - **`cos(log x)`, `sin(log x)`**: nessuna fase tenta `x=e^u` prima di IBP; fallimento onesto dopo ricerca esausta (ordine ~50s misurato, da confermare a carico di sistema normale) prima del `NO_STRATEGY`.
  - **`x²e^{−x}sin x`, `x^n·log x`**: IBP doppia (tabellare, richiede riconoscere il "ritorno" all'integrando originale a meno di costante) e potenza a esponente simbolico, non coperte da `integrate_power`/`integrate_by_parts`.
- **Refs**: A37, A43 (le 10 entry non-elementari), A45 (bug LRT confermato — blocca estensione Weierstrass e famiglia grado-4), B.2 (canonicalizzazione trig, gap diverso), `scripts/golden_skip_triage.py --bucket engine --list`

### A45 · `integrate_rational_lrt` non implementava Lazard-Rioboo-Trager: τ_k mancante + 3 deviazioni dalla spec — ✅ FATTO 2026-07-25 — `[E3·C4·S3·R3]`
- **Spec applicata**: `Symbolic_Integration_I.md` (Bronstein) — `SubResultant(A,B)` righe 1044-1071, `IntRationalLogPart` (LRT) righe 1815-1833, correzione Mulders righe 1811-1813 + nota 1836, presentazione per fattori razionali riga 2378.
- **⚠️ La prima diagnosi (2026-07-24) era in parte SBAGLIATA** e avrebbe fatto ripartire la sessione su pista falsa. Corretta con misura diretta (sympy 1.14 `subresultants()` + Maxima `resultant()` + simulazione fedele riga-per-riga delle due implementazioni C++ reali: **`scripts/a45_prs_simulation.py`**, ripetibile). Errori da non ripetere: (a) `x²/(x⁴+1)` ha sequenza **4,3,2,1,0 — REGOLARE**, non difettosa; (b) nessun silent-wrong era *osservabile* in produzione (i casi rotti davano `INTEGRATE_NO_STRATEGY`); (c) la causa non era "la formula di scaling `g,h` è invalida" ma il **fattore τ_k mancante**.
- **✅ Tre bug distinti chiusi** (commit `6ae886c` fasi 0-2):
  1. **τ_k mancante** (Teorema 1.5.3, riga 1035): `resultant = R_k` (ultimo elemento della PRS di grado 0) vale solo se `deg(R_{k-1}) = 1`; con l'ultimo passo difettoso serve `τ_k·R_k`, altrimenti il resultant esce di grado dimezzato. **Condizione di scatto esatta: `deg(R_{k-1}) > 1`** — non "sequenza difettosa" in generale: `x/(x⁵+x+1)` (gradi 5,4,**2**,1,0) è difettoso a metà catena ma ha τ_k = 1 e usciva giusto. Ecco perché è rimasto invisibile. Misura: 4 casi su 21 sbagliati, tutti nel corpus golden. **Fix architetturale**: la seconda ricorrenza (`subresultant_prs_lrt`/`pseudo_remainder_lrt`, `g`/`h`) è stata **eliminata**; resultant e catena vengono ora da `resultant_generic`, che portando γ tra i passi applica τ_k implicitamente (verificato 21/21 contro l'oracolo). Una sola ricorrenza subresultante nel motore invece di due divergenti — era quella la causa-radice. `resultant_generic` ha guadagnato un `chain_out` opzionale (default `nullptr`, zero impatto sui 6 call-site Galois/torri).
  2. **Consumo non conforme**: `square_free_factorization` al posto di `factor_over_integers` (la molteplicità `i` **è** il grado del subresultante da usare, e la fattorizzazione completa la buttava via); selezione `S_i` per grado (riga 1828) invece dell'euristica di divisibilità; caso `i = deg(D) → S_i = D` (riga 1826); correzione Mulders (righe 1829-1830). La presentazione continua a fattorizzare `Qᵢ` sui razionali, così le forme chiuse log/arctan restano quelle di prima invece di collassare in `RootSum` (consentito dalla spec, riga 2378) — è ciò che ha evitato la regressione di massa temuta in T5.
  3. **`mul_expr_lrt` trattava lo ZERO come identità moltiplicativa** (`0·b` tornava `b`): bug pre-esistente **non previsto dal piano**, scoperto perché il certificato `D(F)=f` è fallito sul caso più semplice. Iniettava un `log` spurio in **ogni** conversione di Rioboo con `R(z)` privo di termine lineare — il caso più comune (`4z²+1`, `8z²+1`…). Via LRT, `∫dx/(x²+1)` dava `√3/2·arctan(x√3/(x+2)) − ¼ln(x²+x+1)`; ora dà `arctan(x)`. Mascherato dal bypass `c/(x²±a²)`, invisibile ai test esistenti. **Lezione: il caso base va nel corpus di verifica anche quando "sicuramente funziona".**
- **Rimosso** anche il nome interno hardcoded `"__z"` del parametro di Rothstein-Trager (divieto hardcode cat. 7) → `ctx.make_fresh_symbol`.
- **✅ Fase 3 (performance) — NON NECESSARIA, misurato.** Il debito originale ("il resultant di `x²/(x⁴+1)` impiega ~8.3s e sfora l'ops-budget") era un **effetto del bug di correttezza**: la ricorrenza sbagliata produceva divisioni non esatte e quindi coefficienti razionali che gonfiavano a ogni passo. Con la ricorrenza corretta: `1/(x⁶+1)` **197ms**, `1/(x⁵+x+1)` 633ms, `1/(x⁸+1)` 406ms — due ordini di grandezza in meno. L'aritmetica esatta `RatPoly` (e quindi `CoeffOps<RatPoly>`) **non serve**: implementarla sarebbe stata ottimizzazione prematura sulla base di una diagnosi sbagliata, quarto caso dello stesso pattern già in memoria (`profiler-misattribution-pattern`). Misura fissata nel test `HigherDegreeDenominatorsStayFast` con bound largo (8s) per non essere flaky sotto carico.
- **✅ Fase 4 (budget)**: `integrate_rational_lrt` passa un deadline a `resultant_generic` (meccanismo già esistente, righe 236-238) — `ctx.hard_deadline()` se impostato, altrimenti il budget per-operazione `ctx.timeout()`. Il controllo avviene fra i passi della catena; un budget configurato scatta *durante*, non si scopre a fine calcolo (divieto hardcode cat. 9).
- **✅ Fase 5 (misura)**: ref Maxima rigenerate per l'area `integrate` (`1/(x⁴+1)` era SKIP per ref mancante, non per fallimento). Golden `integrate` **123→126 pass**, ratchet globale **972/10 → 975 pass / 9 fail**, baseline consolidata `PASS_FLOOR=975`. Quick suite 2847/2847. I 3 pass nuovi sono esattamente le entry difettose sbloccate: `(x²+1)/(x⁴+1)`, `(x²−1)/(x⁴+1)`, `x³/(x⁴+1)`.
- **+7 test permanenti** (`test/unit/algebra/test_lrt_subresultant_a45.cpp`): certificato `D(F)=f` su casi difettosi e regolari, proprietà strutturali della catena (gradi strettamente decrescenti, esistenza del subresultante di grado `i`), forma `RootSum` attesa sui resultant irriducibili, regressione di performance. **Nessun valore atteso hardcoded** — solo proprietà e oracoli interni.
- **Trappole del piano, esito reale**: T1 (debug dall'alto) e T5 (forma dell'output) si sono rivelate decisive ed evitate come previsto; T2 (ordine correttezza→performance) ha evitato di spedire un silent-wrong; **T3, T7, T9 non si sono materializzate** perché la fase 3 non è servita; T8 (limite 500 righe) non ha morso: il file è passato da 378 a 375 righe, la rimozione della PRS locale ha compensato le aggiunte. La trappola vera non era in lista: il bug `mul_expr_lrt`, trovato solo perché il certificato copriva anche il caso banale.
- **Eredita**: **A46** (resa in forma chiusa dei `RootSum` — vedi sotto), aperta con le evidenze raccolte qui. **Sblocca A42** per la sola famiglia razionale grado-4 con fattore quadratico; l'estensione Weierstrass `tan`/multi-angolo va rimisurata ora che la catena è corretta e veloce (l'ipotesi ">120s per k=1" era basata sul codice buggato).
- **Refs**: A42, A30 (guardia ops-budget), A46, `scripts/a45_prs_simulation.py`, `polynomial_resultant_generic.hpp:229`, Mulders [52] (`Symbolic_Integration_I.md:8805`)

### A46 · `integrate` non rende in forma chiusa la parte logaritmica quando il resultant è irriducibile di grado > 2 (`RootSum` non derivabile) — ✅ FATTO 2026-07-25 — `[E2·C3·S3·R2]`
- **Spec applicata**: `Symbolic_Integration_I.md` (Bronstein) — `LogToAtan` righe 2188-2200, `LogToReal` righe 2381-2395, Teorema 2.8.4 (Rioboo) righe 2320-2340. Algoritmo simulato riga-per-riga in **`scripts/a46_logtoreal_simulation.py`** (sympy 1.14, certificato `D(F)=f` simbolico + numerico) PRIMA di scrivere C++ — metodo A45, ripetibile.
- **Confine matematico, misurato non assunto**: la forma chiusa richiede i **residui**, e `solve_polynomial` sul resultant quartico restituisce `RootOf`, non radicali (il ramo Ferrari `solve_degree_four_expr` è commentato in `solve_polynomial.cpp:268-278`). I residui *sono* esprimibili quando vivono in un'**estensione quadratica di ℚ**, cioè quando il **cubico risolvente** del quartico ha una radice razionale: allora il quartico si spezza in due quadratici **reali** e ognuno passa per la forma chiusa di Rioboo grado-2 che il motore già aveva. Fuori da quella classe resta la somma formale `RootSum`, con il rifiuto esplicito in `integrate_rational` (messaggio aggiornato: nomina la classe coperta).
- **✅ Implementato** (nuovi `src/algebra/partial_fractions_rioboo.hpp`, `partial_fractions_logtoreal.cpp`, `partial_fractions_quartic_split.cpp`):
  1. **`LogToAtan` vera ricorsione** (spec 2188-2200) via `poly_extended_gcd` su coefficienti in ℚ(√d): argomenti **polinomiali** (`2·arctan(√2x±1)`), non funzioni razionali. Fermarsi al primo ramo (quello che il motore faceva) dà un'antiderivata con salti e un costo di verifica ordini di grandezza più alto — misurato: certificato `D(F)=f` in timeout.
  2. **Gate di precondizione**: `LogToAtan` vale su K con `√−1 ∉ K` (spec riga 2191) ⇒ serve `disc = 4b−a² > 0` (residui coniugati). Con `disc < 0` i residui sono reali e `B` diventa immaginario: **misurato 673s** su `∫(x²−1)/(x⁴+1)dx` (resultant `8z²−1`, disc = −1/2) prima del gate, **2.8s** dopo. Segno deciso **esattamente**: razionale → confronto in ℚ; non razionale → l'unico produttore è la via quartica, che ha già stabilito con Sturm che R non ha radici reali.
  3. **Conteggio radici reali esatto** (`count_real_roots_rational`): sequenza di Sturm valutata a ±∞ (solo segni dei coefficienti di testa) — nessun float, nessuna tolleranza, nessuna dipendenza da `cas_numeric`.
  4. **Split del quartico verificato**, mai assunto: `e,g=(p±√(p²−4q+4y₀))/2`, `f,h=(y₀±√(y₀²−4s))/2`; realtà dei due radicandi = confronto **razionale** esatto; l'accoppiamento `(e,f)(g,h)` vs `(e,h)(g,f)` è scelto **ri-moltiplicando** e confrontando col quartico monico.
  5. `log(c·u) = log(u) + cost`: argomento del log reso **monico**, altrimenti la pipeline via frazioni parziali si porta dietro contenuti tipo 1/729 in ogni coefficiente.
- **✅ I ~30s NON erano di A46: causa reale diversa, trovata a misura.** `integrate(1/(x⁶+1))` costava ~41s (prima ~30s + `NO_STRATEGY`) ma `integrate_rational_lrt` sullo stesso denominatore costa **0.5s**: il tempo andava tutto in **dispatch dipendente dalla forma**. La stessa integranda scritta `Pow(Q,−1)` (la forma che producono `partial_fractions` e il simplifier) o `Product(N, Pow(D,−1))` **non raggiungeva mai la via razionale** — solo il ramo `Binary(Div)` arriva a `integrate_rational` — e finiva in IBP/sostituzione. Misure: `Pow(x⁴+1,−1)` 40s → **0.36s**; seconda frazione parziale di `1/(x⁶+1)` 157s **e poi FALLIVA** → **1.3s** e chiude. Due innesti: `integrate_power.cpp` (potenza negativa di polinomio → razionale) e `integrate_product_power.cpp` (frazione **propria** in forma Product, il ramo `deg(N) < deg(D)` che il commento preesistente lasciava scoperto). Totale `1/(x⁶+1)`: **41s → 1.3s**.
- **✅ Gate**: golden ratchet **975/9 → 977 pass / 9 fail** (le 2 entry `1/(x⁴+1)` e `x²/(x⁴+1)` da SKIP a PASS, skip 42→40), baseline consolidata `PASS_FLOOR=977`. Quick suite **2853/2853** in 627s (era ~803s: il calo è il costo di dispatch recuperato). Benchmark: tutte le 6 metriche ≤ baseline. Zero warning.
- **+6 test permanenti** (`test/unit/algebra/test_logtoreal_a46.cpp`): Sturm esatto (proprietà, incluso radice doppia), split che si ri-moltiplica all'input, **rifiuto** fuori classe (`z⁴−2`, `z⁴−5z²+1`), forma chiusa senza `RootSum` + certificato, pipeline `integrate`, e **`RationalShapeDoesNotDecideCost`** (stessa integranda in 3 forme sintattiche: `Div`, `Pow^-1`, `Product`). Certificato = **multi-punto esatto** (12 punti razionali): la differenza è razionale di grado noto, quindi annullarsi in più punti del grado la costringe a zero — ridurla a una frazione unica in x sfora >120s sulla forma della pipeline.
- **Aggiornato** `test_lrt_subresultant_a45.cpp`: l'aspettativa "RootSum per `1/(x⁴+1)`" era corretta prima di A46 e ora è falsa → il test fissa il **nuovo confine** su `1/(x⁸+1)` (resultant grado 8, Φ₁₆ irriducibile), non è stato disabilitato.
- **🔧 Residuo dichiarato (fuori classe, `RootSum` + rifiuto diagnostico)**: quartici **con radici reali** (es. resultant di `∫dx/(x⁴−2)`), resultant irriducibili di **grado ≥ 5** (`1/(x⁸+1)`), e cubici irriducibili (`1/(x⁵+x+1)`, radice reale via Cardano + quoziente quadratico: fattibile, non fatto). Chi li vuole chiudere: riattivare/riscrivere il ramo Ferrari di `solve_polynomial` non basta — serve la selezione delle radici reali con segno esatto.

#### Cronologia diagnostica (A46, per non ripetere le piste false)
- **Scoperta (A45, misurata)**: con `R(z)` irriducibile di grado > 2 su ℚ, LRT risponde correttamente con la somma formale sulle radici (spec riga 1831) e il motore emette `RootSum`. Ma come **antiderivata** non è usabile: `diff` non ha regola per `RootSum`, quindi il risultato non è né verificabile (`D(F)=f` non applicabile) né componibile a valle. Maxima risponde in forma chiusa su ℚ(√2): per `∫x²dx/(x⁴+1)` usa `log(x²±√2x+1)` e `atan((2x±√2)/√2)`, perché `x⁴+1` fattorizza su ℚ(√2) — verificato che `256z⁴+1` (il resultant) è invece **irriducibile su ℚ**, quindi la forma chiusa richiede l'estensione.
- **Stato attuale (deciso in A45, non un debito nascosto)**: `Integrator::integrate_rational` (`src/calculus/integrate_rational_part.cpp`) **rifiuta esplicitamente** un risultato contenente `RootSum` con `Unimplemented` e messaggio che nomina il gap. Motivo: restituirlo come forma chiusa sarebbe silenziosamente sbagliato *nella forma* (divieto hardcode cat. 4 — bail-out esplicito, diagnostico, task aperto). `integrate_rational_lrt` continua a emetterlo per chi lo chiama direttamente (`Calculus.RootSumEmission` lo richiede), quindi il RootSum resta disponibile a livello algebrico.
- **⚠️ Pista in parte FALSA (ipotesi di apertura, corretta a misura il 2026-07-25)**: era scritto che i ~30s di `integrate(1/(x⁶+1))` fossero "il dispatcher che *scarta* il risultato valido di LRT e continua a cercare", e che chiudere la resa in forma chiusa li recuperasse. Il numero era vero, la causa no: il dispatcher non scartava nulla — le forme `Pow(Q,−1)` e `Product(N, Pow(D,−1))` **non arrivavano affatto** alla via razionale, perché solo il ramo `Binary(Div)` porta a `integrate_rational`. Le due cose sono indipendenti: la forma chiusa (LogToReal) e il costo (routing) sono stati chiusi da due fix separati. **Lezione: "il dispatcher scarta X" va verificato misurando il path, non dedotto dal fatto che il risultato buono esista.**
- **Impatto atteso (verificato)**: 2 entry golden (`1/(x⁴+1)`, `x²/(x⁴+1)`) → PASS; famiglia razionale grado-4 di A42 chiusa. I casi grado ≥5 restano fuori (vedi Residuo).
- **Refs**: A45 (origine, con le misure), A42 (famiglia grado-4 — ora chiusa), `Symbolic_Integration_I.md` righe 2188-2200 / 2320-2425, `src/algebra/partial_fractions_logtoreal.cpp` + `partial_fractions_quartic_split.cpp` (nuovi), `src/algebra/partial_fractions_lrt.cpp` (`rioboo_conversion`), `src/calculus/integrate_rational_part.cpp` (punto di rifiuto), `src/calculus/integrate_power.cpp` + `integrate_product_power.cpp` (routing per forma), `scripts/a46_logtoreal_simulation.py`

### A47 · Verifica `D(F)=f` dentro IBP: `together`+`simplify` su forme trigonometriche brucia l'intero budget — 📋 APERTO 2026-07-25 — `[E2·C3·S2·R2]`
- **Scoperta (misura A42, 2026-07-25, su HEAD pulito — NON introdotto da patch)**: il blocco "Verification by differentiation" di `integrate_by_parts` (`src/calculus/integrate_parts.cpp`) esegue `diff(ibp_result)` → `algebra::together(delta)` → `simplify`. Su `∫dx/(2+sin(2x))` la misura per fase dà `verify:diff` 281 ms, **`verify:together` 45 880 ms**, **`verify:simplify` 60 118 ms** (= cap del timeout). Le IBP annidate moltiplicano il costo: 3 livelli × budget pieno.
- **Perché conta**: la verifica esiste per una ragione giusta (senza, `∫1/(x·ln x)` riceveva una forma chiusa sbagliata) e **non va rimossa** — ma nella forma attuale è più costosa dell'integrazione stessa e, andando in timeout, non decide nulla: il risultato passa comunque non verificato.
- **❌ La direzione raccomandata all'apertura era SBAGLIATA (corretta 2026-07-25, stessa giornata)**: era scritto di sostituire la riduzione simbolica col **certificato multi-punto** di A46. **Non è valido qui.** In A46 la prova reggeva perché `D(F)−f` era una funzione **razionale di grado noto**: annullarsi in più punti del grado la costringe a zero. Per integrande **trascendenti** l'argomento cade — `sin(πx)` si annulla su tutti gli interi senza essere zero. Usarlo come criterio di *accettazione* sarebbe silent-wrong. L'unico uso sound è come **rifiuto rapido** (se in un punto la differenza è ≠0, non è antiderivata: conclusione certa), ma resta il problema che valutare `sin(3)` non dà un razionale, e la valutazione numerica **non è linkabile** da `cas_calculus` (`cas_numeric` dipende da `cas_calculus` — dipendenza circolare, cfr. memoria T-007).
- **🔬 Dove va davvero il tempo (misurato 2026-07-25 su `WeierstrassSubstitutionTest.IntegralOfOneOverTwoPlusSin`, instrumentando `together`)**: su **un solo** `together` con input di **111 nodi** → `apart_num_den` **5.2 s**, riduzione GCD finale **0.96 s**, output N=280/D=74 nodi. Quindi: (a) il collo è `apart_num_den`, non la riduzione GCD; (b) **non è la dimensione a esplodere** (280 nodi in uscita), è il costo per passo. Scomposizione ulteriore: `normalize_rational_parts` = 101 chiamate per **1.0 s soli** — cioè l'80% del tempo è **fuori** da normalize. Causa: `add_parts`/`multiply_parts`/`divide_parts` costruiscono via `multiply_exprs`/`add_exprs` (`algebra_core.cpp:108-122`) e **ognuna di queste chiama `simplify_expr`** → ~6 `simplify` per ogni combinazione, su un accumulato che cresce.
- **⚠️ Vincolo scoperto sperimentalmente**: rimuovere i `simplify` intermedi (costruzione strutturale + un solo `simplify` finale) **rompe la logica**, non solo la forma: i check `is_zero_expr`/`is_one_expr` in `divide_parts`/`normalize_rational_parts` si appoggiano alla forma semplificata e `apart_num_den` inizia a fallire. Un fix in questa direzione deve prima rendere quei predicati indipendenti dalla forma.
- **Scope reale e rischio**: `together` è usato in tutto il motore (verifica IBP, verifica sostituzione, `mathematically_equal`, QR simbolico, Risch…). Non è una modifica locale a `integrate_parts.cpp` come diceva la stima all'apertura: è **lavoro su un componente core**, merita spec propria e gate completo. Rivedere `[E2·C3·S2·R2]` verso l'alto.
- **Refs**: A42 (misura d'origine), A46 (certificato multi-punto — valido SOLO per differenze razionali), `Together_Polynomial_GCD_Reduction.md` (spec della riduzione GCD, già implementata: `factorization_num_den.cpp:364`), `src/algebra/factorization_num_den.cpp` (`add_parts:105`, `normalize_rational_parts:29`), `src/algebra/algebra_core.cpp:108-122`, `src/calculus/integrate_parts.cpp` (consumatore)

### A52 · `simplify(A − A) ≠ 0` — residuo di A48: monomio a coefficiente unitario + UN solo trascendente — 📋 APERTO 2026-07-27 — `[E2·C3·S3·R2]`
- **Scoperta (A48)**: A48 ha chiuso la famiglia della *raccolta dei coefficienti*, ma resta una classe con causa radice **diversa**. Confine misurato, non supposto:

  | `A` | `simplify(A − A)` |
  |---|---|
  | `(x−2)·e^{2x}` | ❌ |
  | `(x+1)·sin x` | ❌ |
  | `(x²−2)·cos x` | ❌ |
  | `(x²+x−1)·cos x` | ❌ |
  | `(2x−2)·ln x` | ✅ |
  | `(4x−6)·ln x` | ✅ |
  | `(2x²−3x)·cos x` | ✅ |
  | `(x−1)·e^x·sin x` | ✅ — coefficiente unitario ma **due** trascendenti |

- **Caratterizzazione**: fallisce quando il polinomio ha un monomio a **coefficiente unitario** (±1) **e** c'è **un solo** fattore trascendente. Con due trascendenti funziona; con tutti i coefficienti ≠ ±1 funziona.
- **Dove NON e'**: non e' la raccolta dei coefficienti chiusa in A48 (i termini superstiti — `x·e^{2x}` e `−(x·e^{2x})` — stanno al livello del `Sum`, quindi competono al collettore primario dello **Step 4**, non alla raccolta di A48). Non e' `extract_monomial`, che ricorre correttamente su `Neg` (`simplify_arithmetic.cpp:251`). Verificato che il fallback dello Step 4 sul termine opaco **non** e' la causa: la patch corrispondente non ha cambiato nulla ed e' stata revocata (nessuna modifica senza evidenza).
- **Falliva anche PRIMA di A48**: non e' una regressione, e' una parte non ancora isolata.
- **Stato**: tre tentativi di diagnosi falliti → fermato per **protocollo anti-loop** invece di patchare alla cieca. Il prossimo passo e' strumentare con `std::cerr` dentro `simplify_sum_terms` per vedere la lista `flat_terms` reale allo Step 4 su `(x−2)·e^{2x} − (x−2)·e^{2x}` — la domanda aperta e' *quando* la distribuzione produce i due termini superstiti, dato che `simplify` da solo mantiene `A` fattorizzato.
- **Refs**: A48 (scoperta), A39, `src/symbolic/simplify_arithmetic_chain_sum.cpp` (Step 4), `test/unit/symbolic/test_a48_sum_fixpoint.cpp` (confine documentato a codice)

### A53 · `calculus::integrate` senza budget d'operazione + condizioni di dominio dei rami morti — ✅ FATTO 2026-07-28 — `[E2·C3·S3·R2]`
- **Scoperta (A51)**, con misura completa già in mano — vedi A51 «CAUSA RADICE 2».
- **Il difetto**: le ops si contano solo in `Simplifier`/`Substituter`, e ogni `ctx.simplify()` dell'integratore è top-level, quindi azzera contatore e timer. Nessun budget limita il **totale** di un'integrazione: misurato, la stessa integranda consuma per intero qualunque cap (30 s → 30 s, 300 s → oltre 300 s) e il RISULTATO cambia col budget concesso invece che con l'input.
- **Il fix è pronto ma bloccato da un secondo difetto**: `CASContext::OperationScope` (RAII rientrante, già in `symbolic.hpp` e usato da `CASContext::substitute`) applicato all'ingresso di `calculus::integrate` risolve il budget. Ma aprire l'operazione ferma anche l'azzeramento di `side_conditions_` fatto dalle simplify top-level, e **le condizioni emesse dai rami scartati sopravvivono nel risultato**: `∫e^{-x²}` esce con `x>0` che il completamento del quadrato non richiede (repro: `IntegrateMeijerGTest.GaussianIntegral_ErfClosedForm_DiffVerified`).
- **Due lavori, in quest'ordine**:
  1. **Rollback delle side-conditions sui rami morti** (famiglia **A31**): una strategia che fallisce non deve lasciare condizioni. Serve uno snapshot/ripristino attorno a ogni tentativo dell'integratore, non un `clear()` finale — che è ciò che oggi maschera il problema per caso.
  2. **Calibrazione del budget di integrate**: il default A30 di 2M ops è per-`simplify`, mai calibrato come totale d'integrazione. Misurare con `--ops-report` / `ops_high_water()` il costo delle integrazioni legittime più care (il quartico di A46 e la gaussiana MeijerG sono i candidati) e derivare il parametro da lì — mai un numero a intuito.
- **Nota di metodo**: `poly_extended_gcd` andava in ricorsione infinita non appena il budget si restringeva. Corretto in A51 (guardia `deg(r) < deg(b)`), ma è il segnale che stringere il budget **espone** difetti latenti a valle: la fase 2 va fatta misurando, non alzando o abbassando numeri.

#### ✅ Fase 1 — rollback delle side-conditions dei rami morti (2026-07-28)
- **Sintomo riprodotto prima del fix**: con `OperationScope` aperto su `integrate`, `∫e^{-x²}` usciva con `Positive(x)` (`IntegrateMeijerGTest.GaussianIntegral_ErfClosedForm_DiffVerified` rosso).
- **Ramo emittente trovato a MISURA, non per deduzione** (backtrace strumentato su `SideConditionSet::add`; il breakpoint lldb condizionale sull'enum non ha mai valutato la condizione):
  `integrate_once → integrate_product → integrate_by_parts → integrate → integrate_function → integrate → integrate_meijerg_fallback`, cioè il fallback Mellin dentro una sotto-integrazione di by-parts poi **scartata** — mentre il risultato finale viene dal completamento del quadrato, esatto su tutto ℝ.
- **Meccanismo**: `CASContext::SideConditionRollback` (RAII, `commit()` esplicito). Il mark è una **copia** del set, non la sua dimensione: `add` rimuove la condizione più debole quando ne entra una che la subsume (§3.4), quindi un ripristino per troncamento lascerebbe dentro la condizione sbagliata (test dedicato).
- **Applicato per costruzione, non per annotazione**: i ~60 call-site prova-e-scarta sarebbero stati violabili per omissione (lezione A51). I 18 metodi pubblici di `Integrator` diventano wrapper in `src/calculus/integrate_condition_guard.cpp` sopra le rispettive `_impl`; le strategie libere (dirac, u-sub, Risch, Weierstrass, non-elementari, Meijer, by-parts) sono avvolte al call-site. Tre modi di dire «rifiutato» — `Result` in errore, `ok(false)`, `ok(nullopt)` — trattati tutti (`accept_if_true` / `accept_if_matched`), perché trattarne uno solo lascia passare gli altri due.
- **Il rollback NON tocca la cache**: le condizioni restano attribuite alla loro entry e sono ri-emesse a ogni hit (A31 §4.2) — il difetto opposto, e altrettanto silenzioso, sarebbe un ramo morto che avvelena la cache per tutti.

#### ✅ Fase 2 — budget d'integrazione calibrato sulla distribuzione (2026-07-28)
- **`max_integration_ops` = 500'000**, campo di `CASContextParams`, applicato da `OperationScope(ctx, trace, ops_cap)` come `min` col budget dell'operazione corrente; gate spento (0) ⇒ nessun tetto imposto (contratto A30: il chiamante possiede il budget).
- **Derivazione dalla misura** (`--ops-report --max-ops 0`, aree integrate+bronstein, 2026-07-28): legittima più cara `bronstein[23]` 310'184 ops, poi `integrate[26]` 308'940, poi 132'806; prima patologica `bronstein[73]` 700'911, fino a `integrate[109]` 1'764'313 — tutte troncate, mai decise. **Fra 310k e 700k non cade nessuna entry decisa**: soglia nel vuoto (media geometrica ~466k), margine 1.61× / 1.40×. Stesso criterio del cap 60 s di A51.
- **Perché non bastava il default 2M**: a 13-29k ops/s misurati vale 70-150 s, cioè più del cap wall-clock — il taglio sarebbe sempre arrivato dal SIGALRM dipendente dal carico.
- **Effetto misurato sul gate**: la ripartizione delle non decise passa da **28 skip + 15 over** a **39 skip + 4 over**. Undici entry patologiche ora sono fermate dal gate ops *deterministico* invece che dal SIGALRM. La **somma** (43) resta invariante, come previsto da A51.
- **Strumento aggiunto**: `--max-ops` nel golden runner e in `run_golden_measurement.sh` — serve ai run di sola calibrazione; nei run di gate NON va usato (la baseline si misura al budget di default).
- **Confine dichiarato**: le ops si contano solo in `Simplifier`/`Substituter`, quindi il budget non morde dove il costo sta altrove (Risch, algebra dei polinomi) — misurato: `sin(log x)·cos(log x)/x³` gira per minuti con tetto 20'000 ops. Estendere il conteggio a quelle fasi è lavoro proprio, pinnato dal test `BudgetOnlyBindsWhereOpsAreCounted` perché non venga scambiato per copertura totale.
- **Verifica**: 10 test permanenti (`test/unit/symbolic/test_a53_condition_rollback.cpp` 5, `test/unit/calculus/test_a53_integration_budget.cpp` 5). Prove di chiusura: sintomo Gaussiano rimisurato rosso→verde con il test complementare (rotta Mellin diretta) che **deve** dichiarare `Positive` e resta verde; quick suite 2894/2894; golden **979 / 4 / 43** identico alla baseline; benchmark 6/6 `status=ok` (−8% … −31% vs baseline, a macchina scarica).
- **Refs**: A51 (scoperta e misure), A31 (side-conditions), A30 (contratto budget), `src/calculus/integrate.cpp`, `src/calculus/integrate_condition_guard.cpp`, `include/cas/symbolic.hpp`, `include/cas/cas_context_simplifier_params.hpp`

### A51 · Golden ratchet inaffidabile: il gate misurava la macchina, non il codice — ✅ FATTO 2026-07-27 — `[E2·C3·S4·R1]`
- **Scoperta (gate finale A43)**: due misure golden complete con lo **stesso identico binario** e lo stesso corpus danno risultati diversi:
  - run A: `982 pass / 8 fail / 36 skip`
  - run B: `982 pass / 9 fail / 35 skip`
  L'entry che oscilla è **`integrate(1/(2 - sin(x)), x)`**, della famiglia Weierstrass/trig (le altre 4 della stessa famiglia sono FAIL stabili).
- **Meccanismo**: nel runner (`test/golden/main.cpp`) **SKIP** = `mathematically_equal` ritorna *errore* ("inconclusive equality"), **FAIL** = ritorna `false`. Sotto carico il confronto sfora il budget e diventa inconcludente; a macchina scarica completa e risponde `false`.
- **⚠️ La prima diagnosi (2026-07-27) era SBAGLIATA**: attribuiva il difetto alla famiglia **A30** (budget wall-clock load-dependent). A30 era già chiusa e il gate ops deterministico funzionava. La causa vera è un'altra ed è stata trovata a codice, non per deduzione — vedi sotto.
- **Perché è grave**: `FAIL_CEILING` è 8 e l'entry oscilla **esattamente sul confine**. Il gate quindi passa o fallisce a seconda del carico della macchina, non del codice — un gate che non discrimina il codice è peggio di nessun gate, ed è la stessa classe di difetto d'infrastruttura del ratchet su dati stantii (35 giorni di misure mai fatte).
- **Conseguenza operativa**: la baseline **NON è stata consolidata** a 982 (sarebbe stata una consolidazione su una misura non riproducibile). Resta `PASS_FLOOR=979 / FAIL_CEILING=8` finché A51 non chiude.
- **Escluso che sia A43**: le due run hanno lo stesso binario. Inoltre le due modifiche di A43 a `mathematically_equal` non possono produrre l'effetto — `nonelementary_normalize` su input trigonometrico restituisce lo stesso puntatore (il blocco non scatta), e lo short-circuit `structural_equal` può solo decidere `true`, mai trasformare un errore in un `false`.
- **✅ CAUSA RADICE 2026-07-27 — `mathematically_equal` apriva un'operazione senza inizializzarne il budget.**
  - Alzava `operation_active_` ma **non** azzerava `ops_count_` né faceva ripartire `operation_started_at_`. Le `simplify` interne vedevano l'operazione già aperta (`owns_operation == false`) e non lo facevano al posto suo. Quindi `Simplifier::check_timeout` misurava `elapsed` dall'**ultima** operazione top-level del contesto — o **dall'epoch**, se non ce n'era mai stata una, e allora `elapsed` vale l'**uptime della macchina** e il timeout scatta al primo controllo wall-clock.
  - **Firma inconfondibile del bug, misurata**: su contesto appena costruito il confronto delle due antiderivate tornava `Symbolic operation timed out` in **63 ms**, con un budget di 10 s. Un timeout impossibile.
  - **Perché proprio il golden runner**: `main.cpp:254` usa **UN solo `CASContext` per tutte le entry dell'area**, quindi il budget di ogni confronto era quello residuo lasciato dalle entry precedenti → il verdetto dipendeva dalla storia del run e dal carico, non dall'input.
  - **Dei 4 siti che aprono un'operazione** (`CASContext::simplify`, `CASContext::substitute`, `algebra::polynomial_gcd`, `mathematically_equal`) **solo l'ultimo sbagliava**. Il pattern era duplicato: incapsulato in `CASContext::begin_operation_budget`/`end_operation_budget` e convertiti tutti e quattro, così l'invariante non è più violabile per omissione.
- **Due difetti latenti corretti nello stesso giro** (`test/golden/corpus_runner.hpp`, ramo gcd): `set_timeout(5000)` **spegneva il gate ops deterministico per tutto il resto del run** (contratto A30: wall-clock esplicito senza budget ops esplicito cede il controllo al chiamante, e il contesto è condiviso); il successivo "restore default" a 1000 ms **non era il default** (10000 ms dopo A30), stringendo di 10× il budget di ogni entry successiva.
- **Budget del runner ora DICHIARATO** (`main.cpp`), non ereditato: wall-clock pari al budget per-entry già dichiarato dallo script (nessun numero nuovo inventato; l'anti-hang vero resta il SIGALRM per-entry) e gate ops reso **esplicito al suo stesso valore di default**, così nessun `set_timeout` successivo può più spegnerlo.
- **🔎 La prima chiusura era PREMATURA — tre difetti trovati verificandola** (2026-07-27, sessione successiva). Il fix sopra è corretto e necessario, ma non chiudeva la task:
  1. **Bug DENTRO il fix** (`main.cpp`): le due righe erano nell'ordine sbagliato. `set_timeout` azzera `max_operation_ops_` finché è implicito (contratto A30), quindi `set_max_operation_ops(ctx.max_operation_ops())` scritto **dopo** leggeva `0` e rendeva esplicito proprio lo zero → **gate ops spento**, con un commento che dichiarava il contrario. Ordine invertito, e il perché è ora scritto al sito.
  2. **`over_budget` non sommato nel merge** (`run_golden_measurement.sh`): `bronstein` e `integrate` dichiarano la stessa area, e il merge sommava solo `pass/fail/skip` → le entry troncate del secondo file sparivano (totale 1022 invece di 1026).
  3. **Il difetto vero non era ancora stato toccato** — vedi sotto.
- **🔬 CAUSA RADICE 2 — `calculus::integrate` non ha alcun budget** (diagnosticata qui, **fix scorporato in A53**). Le ops si contano in due soli punti (`Simplifier`, `Substituter`) e **ogni operazione top-level azzera contatore e timer**: le migliaia di `ctx.simplify()` che l'integratore esegue sono ognuna top-level, quindi ripartono da zero e **nessun budget limita il totale**. L'unico freno è il SIGALRM esterno.
  - **Misurato**: la stessa entry consuma per intero qualunque cap le si dia — 30 s con cap 30 s, oltre 300 s con cap 300 s. E cap diversi cambiano **entry diverse** (10 s tocca `[76]`/`[77]`, 30 s tocca `[69]`/`[37]`): il verdetto è funzione del tempo concesso, non dell'integranda. Due run identiche non lo avrebbero mai rivelato.
  - **Il gate ops non ha mai deciso nulla**: misurato `ops=406 469` contro un tetto di `2 000 000` nel momento in cui il wall-clock taglia. Il commento di A30 («10 s coprono ~10× il rallentamento di qualunque operazione che sta nel budget ops») è **falso alla misura**: 2M ops richiedono ~150 s.
  - **Perché non è chiuso qui**: `CASContext::OperationScope` (RAII rientrante) risolve il budget ed è stato provato sull'ingresso di `integrate`, ma aprire l'operazione cambia anche la semantica **A31**: le simplify interne smettono di azzerare `side_conditions_`, e le condizioni dei rami **scartati** sopravvivono nel risultato — `∫e^{-x²}` tornava con una condizione `x>0` che il completamento del quadrato non richiede (`IntegrateMeijerGTest.GaussianIntegral_ErfClosedForm_DiffVerified`). Chiudere A51 portandosi dietro quel cambio sarebbe stato barattare un debito con due. → **A53**.
- **✅ Bug di terminazione trovato per strada e corretto** (`poly_extended_gcd`, `polynomial_internal_xgcd.cpp`): la ricorsione di Euclide terminava solo se il resto diventava zero, senza verificare `deg(r) < deg(b)`. Con coefficienti simbolici la riduzione del leader passa da `simplify`: se quella non riesce, il grado apparente non cala e **la ricorsione non finisce** — misurato come stack-overflow ASan (~250 frame) appena il budget delle simplify interne si è ristretto. Un crash non è un modo accettabile di fallire: ora è un `Result` diagnostico. La funzione è stata estratta in un file proprio (`polynomial_internal.cpp` era a 498 righe, senza spazio per la guardia).
- **✅ CAUSA RADICE 3 — il runner confondeva «risposta sbagliata» con «non ho finito».** Un'entry troncata registrava comunque un verdetto (`false` da un confronto interrotto, `NO_STRATEGY` da una strategia troncata) e finiva fra i PASS/FAIL. Misurato su 1026 entry: **16 toccavano il cap**, fra cui tre FAIL (`bronstein[69][70][73]`) e un PASS (`integrate[94]`) con margine **1.0×** — esito deciso sul confine.
  - **Fix**: nuova categoria `over_budget` nel runner (riga `OVER` nel log, campo nel JSON, colonna nel sommario). L'entry troncata perde il verdetto invece di inquinarlo.
  - **Strumentazione permanente**: `CASContext::ops_high_water()` + flag `--ops-report` (ops **e** ms per entry). Senza questi dati la soglia sarebbe un numero preso a intuito e le entry al confine restano invisibili finché non oscillano.
- **Cap per-entry scelto sulla distribuzione, non a comodo**: 99% delle entry decise sotto **5.4 s**, la più lenta **30.8 s**, le patologiche consumano tutto. Fra 31 s e 72 s non cade nessuna entry → cap **60 s** nel vuoto. Il margine 3× **non è raggiungibile** su questo corpus: `integrate[108]`/`[109]` (sin/cos di log) costano ~72 s, vicino a qualunque cap — ridurre quel costo è B.2/A47, non A51.
- **Il gate poggia su una grandezza INVARIANTE**: la ripartizione fra `skip` e `over_budget` dipende dal cap (le due entry da 72 s si spostano fra 60 s e 90 s), la **somma** no. Il ratchet traccia quindi `UNDECIDED_CEILING = skip + over_budget` accanto a `PASS_FLOOR`/`FAIL_CEILING`. Misura identica ai due cap: **979 pass / 4 fail / 43 non decise**.
- **Verifica**: 4 test permanenti in `test/unit/symbolic/test_a51_operation_budget.cpp`. Prove di chiusura, tutte entry-per-entry sui log (non sugli aggregati, che possono coincidere per compensazione):
  1. **riproducibilità** — due misure complete: 11/11 aree identiche;
  2. **invarianza rispetto al budget** — misure a cap 60 s e 90 s: `979 / 4 / 28 skip / 15 over` identici fin nella ripartizione. È la prova che due run identiche non danno;
  3. **A/B contro il pre-fix** (worktree via `git stash`, stessi refs Maxima): differenza isolata a 3 entry di `bronstein`, tutte spiegate.
- **Confine dichiarato**: A51 chiude il **non-determinismo**, non il verdetto. `integrate(1/(2−sin x))` e la famiglia trigonometrica restano non decise perché l'integratore produce per esse forme Weierstrass mostruose (`∫sin³x·cos²x` esce come albero con `where{}` annidati invece di `cos⁵/5 − cos³/3`) — costo intrinseco, famiglia **B.2/A47**. A51 garantisce che questo costo non arrivi al gate travestito da verdetto matematico.
- **Refs**: A30 (gate ops, contratto set_timeout — il suo commento sul rapporto ops/tempo è smentito dalla misura), B.2/A47 (canonicalizzazione trig: è il costo che rende quelle entry non decidibili), A43 (scoperta), `include/cas/symbolic.hpp`, `src/symbolic/context_core.cpp`, `src/calculus/integrate.cpp`, `test/golden/main.cpp`, `scripts/check_golden_ratchet.sh`

### A48 · `simplify` non raggiunge il punto fisso: `A − A ≠ 0` quando `A = (polinomio)·(trascendente)` — ✅ FATTO 2026-07-27 (famiglia della raccolta coefficienti; residuo isolato in **A52**) — `[E3·C3·S4·R2]`
- **Scoperta (A43 inc. 4)**: `mathematically_equal` rifiutava coppie di antiderivate corrette. Restringendo, il difetto non è nel confronto ma nel simplifier, e il repro è di **due righe**:

  | `A` | `simplify(A − A)` |
  |---|---|
  | `(2x−2)` | `0` ✅ |
  | `(2x−2)·x` | `0` ✅ |
  | `(2x−2)·exp(2x)` | `(−2 − 2x + 2x + 2)·exp(2x)` ❌ |
  | `(2x+2)·exp(2x)` | idem ❌ (non è il `Neg`) |
  | `(2x−2)·sin(x)` | idem ❌ (non è `exp`) |
  | `(3x−2)·exp(2x)` | idem ❌ (non è il coefficiente 2) |

- **Caratterizzazione**: basta che un `Sum` sia moltiplicato per una `FuncCall` trascendente. La sottrazione viene distribuita sul fattore comune (giusto), ma il `Sum` risultante **non viene ri-semplificato**: i termini opposti restano affiancati. Le due `A` sono lo **stesso albero** (`structural_equal` = TRUE), quindi non è un problema di forme diverse — è un punto fisso mancato. AST: `Product([Sum([Neg(2), Product([2,x])]), FuncCall(exp,[Product([2,x])])])`.
- **Impatto**: ogni decisione di `mathematically_equal` la cui differenza ha questa forma. È la famiglia di **A39** (`Product`/`Sum` annidati), che chiudeva il caso `flatten` attraverso `Neg` ma non questo.
- **Mitigazione già in atto (NON è il fix)**: `algebraic_equal.cpp` ora prova `structural_equal(cross_l, cross_r)` prima della normalizzazione — decisione sound e gemella del test già presente sui lati non incrociati, ma copre solo il caso in cui i due prodotti incrociati sono identici. Il difetto resta.
- **✅ CAUSA RADICE TROVATA 2026-07-27 — erano DUE difetti, non uno.** Il sospetto iniziale ("il `Sum` non rientra nella raccolta") era corretto solo a metà: correggerlo da solo lasciava `(-(2x) + 2x)·e^{2x}`, misurato.
  1. **`try_merge_symbolic_like_terms` costruiva il `Sum` dei coefficienti grezzo** (`arena.make<Sum>(coeff_terms)` dopo splice+sort, nessuna raccolta). Il commento al sito chiamante vieta — a ragione — di ri-semplificare il *prodotto* fuso (riaprirebbe il ciclo distribuzione/fattorizzazione), ma quel divieto **non riguarda il `Sum` dei coefficienti**, che deve essere in forma raccolta come qualunque altro `Sum`. Aggiunta `collect_coefficient_terms`: stessa raccolta dello Step 4 (chiave = lista fattori canonica, valore = coefficiente razionale), in locale, senza costruire alcun `Product` — quindi non può innescare il ciclo che il divieto protegge. Se tutto cancella, **entrambi** i termini spariscono (lasciare uno `0` letterale lo porterebbe fino al `Sum` finale: dopo lo Step 5 non c'è un'altra raccolta).
  2. **`decompose_term` non ricorreva sul ramo `Neg`**: spingeva l'operando come fattore opaco, quindi `-(2·x)` dava coeff=−1/fattori=[`Product(2,x)`] mentre `2·x` dava coeff=2/fattori=[`x`] — due chiavi diverse per due termini opposti, e nessun `-(c·x) + c·x` si annullava mai.
- **Regressione del fix stesso, colta dal test scritto insieme al fix**: dopo la raccolta `coeff_sum` può collassare a un solo termine, che può essere un `Product` (es. `2a` da `(a+b)·sin x + (a−b)·sin x`). Annidarlo dava `Product{Product{2,a}, sin x}` — non canonico, canary A34 acceso, idempotenza rotta. Prima non poteva accadere perché `coeff_sum` era **sempre** un `Sum`. Ora i fattori vengono splicati, e il caso coefficiente `= 1` non aggiunge alcun fattore.
- **Verifica**: 4 test permanenti in `test/unit/symbolic/test_a48_sum_fixpoint.cpp` — la tabella completa del repro (11 forme, incluse quelle che passavano già, così una regressione si distingue da un falso successo), i **casi negativi** (differenze genuine che non devono collassare), l'idempotenza (inclusa la guardia residuo `x^3 + x` che non va fattorizzata) e il caso A43 che ha fatto emergere il difetto.
- **Refs**: A39, A43 (scoperta), `src/symbolic/simplify_arithmetic_chain_liketerm.cpp`

### A49 · `integrate`: costo esponenziale nel budget di ricorsione su polo lineare TRASLATO — ✅ FATTO 2026-07-28 — `[E2·C3·S3·R2]`
- **Scoperta (A43 inc. 4)**: `∫e^{2x}/(x−1)³` non termina (>200 s). Non è l'ordine del polo e **non è A43**.
- **Misure**:
  - `∫e^x/x³` 0.52 s · `∫e^x/x⁴` 0.58 s (polo in 0, qualunque ordine) — ma `∫e^x/(x−1)²` **3.3 s** contro `∫e^x/x²` **0.58 s**: il costo esplode con la **traslazione**, non con l'ordine.
  - Sweep su `max_integration_depth` per `∫e^{2x}/(x−1)³`: depth 2 → 0.56 s (`NO_STRATEGY`), depth 3 → **1.99 s con la risposta corretta e completa**, depth 4 → 4.42 s, depth 16 (default) → non termina. Il tempo cresce col budget mentre la risposta è già completa a 3: è esplorazione **sprecata**.
  - **Controllo decisivo**: con il fallback A43 disattivato l'integranda non termina lo stesso (>200 s). Il costo è tutto nella catena ricorsiva **pre-Risch** (passi 1-2 di `Integrator::integrate`), che esplora a fondo prima di fallire.
- **Conseguenza pratica**: il caso era escluso da `test_a43_nonelementary_integrate.cpp` (`HigherOrderPolesReduceToEi`); **rientra con questa chiusura**, insieme a `exp(x)/(x+5)^2`.

#### ✅ CHIUSA 2026-07-28 — la causa non era nessuna delle tre ipotesi misurate per prime
- **Rimisura post-A53 (il quadro era cambiato)**: il budget d'integrazione ha convertito l'hang in un rifiuto deterministico, quindi il difetto aveva finalmente un numero. `e^x/x²` 56'167 ops · 1.8 s · risolta; `e^x/(x−1)²` **316'681 ops · 15.0 s** risolta; `e^{2x}/(x−1)³` e `e^x/(x−1)³` al tetto 500'000 · 35 s → `NO_STRATEGY`. La sola traslazione del polo costava **5.6×**, a parità di ordine.
- **Tre ipotesi formulate e SMENTITE dai contatori** (nessuna patch scritta su di esse):
  1. *esplosione dei passi IBP* — misurato: 18 chiamate in entrambi i casi, e il caso peggiore (`(x−1)³`) ne fa **9**, cioè meno;
  2. *crescita delle espressioni* — misurato con conteggio nodi per passo: 6 nodi contro 8, costanti su tutti i livelli;
  3. *espansione ripetuta del binomio* — misurato: `expand` 1278→1406, `parse_polynomial` 1261→1389, cioè **+10%** contro 5.6× di costo.
- **Causa radice (profilo a campionamento)**: il costo non è nel numero di livelli ma **nel costo di ciascun livello**. Ogni passo IBP rilancia l'INTERA pipeline sul sotto-integrale — `integrate_risch` compreso: **56% dei campioni**, con ricorsione profonda 13. Con costante di traslazione più grande (`x+5`) il caso non terminava affatto, e **né il tetto ops né il SIGALRM per-entry lo fermavano**: il lavoro sta in codice che non incrementa il contatore né attraversa un punto di interruzione (è il confine dichiarato in A53).
- **Fix — un teorema, non una forma riconosciuta a occhio**: per **Liouville**, `∫e^{P(x)}·R(x)` con `R` dotato di polo genuino **non ha primitiva elementare**; la catena IBP non può quindi chiuderla ed è lavoro garantito sprecato. Il guard in `integrate_product_impl` (accanto al gemello BUG-HANG-001 già presente) rifiuta quel dispatch e delega alla famiglia non-elementare A43, che quella forma chiusa la produce (`Ei`). Riconosce solo i poli VERI (`Pow(Q,−k)`, `Div(·,Q)`): un `Sum` che *contiene* un polo (`x + 1/x`) non è un fattore-polo e resta intatto, come `∫x²e^x` e `∫x·ln x`.
- **Effetto misurato**: `e^x/x²` 56'167 → **2'097 ops** (1.8 s → 0.08 s); `e^x/(x−1)²` 316'681 → **6'235** (15.0 s → 0.29 s); `e^x/(x+5)²` non terminante (>300 s) → **6'297** (0.27 s); `e^{2x}/(x−1)³` 500'094 al tetto → **12'627** (35 s → 0.57 s). Controllo senza polo `x²·e^x` invariato (1'121 ops).
- **Verifica**: 3 test permanenti (`test/unit/calculus/test_a49_exp_pole_no_ibp.cpp`) — la soglia è sulle **ops**, non sul tempo, perché un test a wall-clock misurerebbe la macchina (lezione A51); più i due casi riabilitati in A43, che pretendono forma chiusa **e** `D(F)=f`. Quick suite 2898/2898; golden **979/4/43** invariato; benchmark senza regressioni.
- **Refs**: A43 (scoperta), A53 (il budget che ha reso il difetto misurabile), A2 (hard-timeout), A30 (budget ops-count), `src/calculus/integrate_product_power.cpp`

### A50 · Verifica simbolica `D(F)=f` bloccata su tre identità mancanti — 📋 APERTO 2026-07-26 — `[E2·C3·S2·R1]`
- **Scoperta (A43 inc. 4)**: tre famiglie di antiderivate **esatte** (certificate mpmath a 30 cifre, errore relativo < 1e-20, `scripts/a43_special_fn_check.py` §"generalizzazioni emesse dal motore") che il motore deriva correttamente ma non riesce a **provare** uguali all'integranda:
  1. **Gaussiana con `|A|` non quadrato perfetto** — `∫e^{2x²} = ½√(π/2)·erfi(√2·x)`: la verifica richiede di ridurre `√(π/A)·√A`.
  2. **Trigonometrica con fase non nulla** — `∫sin(x+1)/x = cos1·Si(x) + sin1·Ci(x)`: richiede le formule di addizione (`sin(x+1) = sin x cos 1 + cos x sin 1`).
  3. **`∫x^s/ln x = li(x^{s+1})` con `s ≥ 1`** — richiede `ln(x^{s+1}) = (s+1)·ln x`, identità condizionata al dominio (territorio **A31**).
- **Stato in A43**: i test pretendono la **forma chiusa** su questi casi e `D(F)=f` dove passa; una regressione della formula resta quindi un fallimento, non un silenzio. Nessun risultato silenziosamente sbagliato.
- **Refs**: A43 (scoperta), A31 (condizioni di dominio, per la 3), A47 (linearizzazione trigonometrica, vicino alla 2)

### A43 · `integrate`: antiderivate NON elementari (Ei, Si, Ci, li, erf, Li₂) — ✅ FATTO 2026-07-26 — `[E3·C4·S3·R2]`
- **✅ CHIUSA 2026-07-26 — tutti e 4 gli incrementi della spec §8 attuati.** 15 test permanenti (`test_a43_nonelementary_builtins.cpp` 7 + `test_a43_nonelementary_integrate.cpp` 8).
  - **Inc. 1-2** (commit `e9b7e74`): gli 8 builtin `Ei/Si/Ci/Shi/Chi/li/dilog/erfi` con round-trip nome↔enum e alias degli oracoli, le 8 derivate §3 con regola catena, e la precedenza LPO sopra il tier esponenziale (`Ei` domina come generatore della famiglia; `erfi` chiude, riducendosi a `erf`).
  - **Inc. 3**: `src/algebra/nonelementary_normalize.cpp` — le identità §4 (`li→Ei(ln)`, `Shi/Chi→` combinazioni di `Ei`, `erfi→−i·erf(i·)`) applicate ai due lati di `mathematically_equal`, accanto a `hyperbolic_normalize`/`factorial_gamma_normalize` e **non** come canonicalizzazione globale (churn di massa, stessa valutazione di A44). `Si`/`Ci` deliberatamente non toccati: le loro identità verso `Ei` introdurrebbero un `i` spurio in un risultato reale. Test con casi negativi espliciti, così la normalizzazione non può collassare coppie genuinamente diverse.
  - **Inc. 3, residuo ereditato da A44 CHIUSO**: il ponte `erfi` in `test/golden/maxima_parser.hpp` — lo SKIP su `erf(%i*x)` è sostituito dalla riscrittura `erf(±i·u) → (±i·erfi(u))` (parentesizzata: la sostituzione può cadere dentro un quoziente).
  - **Inc. 4**: `src/calculus/integrate_nonelementary{,_special}.cpp` + `.hpp`, montati in `integrate_core.cpp` fra Weierstrass e Meijer G. Famiglie: `∫e^{L}·R` con **riduzione** ricorsiva per parti sui poli (non tabella), gaussiana generale per completamento del quadrato (`erf`/`erfi` secondo il segno di `A`), `∫trig(ax+b)/(x−r)` via formule di addizione, `∫x^s/ln x → li(x^{s+1})`, `∫ln(αx+β)/(x−r) → Li₂`, `∫e^{L}·ln(αx+β)` per parti verso `Ei`. Tutte e 13 le righe della tabella §5 escono nella forma dichiarata e superano `D(F)=f`.
- **Tre decisioni prese su misura, non per intuito** (le prime due hanno smentito un'ipotesi iniziale):
  1. **Il fallback gira solo sul frame più esterno** (`depth_ == 1`). Lasciandolo disponibile a profondità ≥ 2 la catena per-parti *pre*-Risch "riesce" su `∫e^x/x` dopo sette passi che alzano il polo a `x⁻⁸`, e riavvolge la forma `Ei` dentro quell'espansione: uguale a `Ei(x)`, inutilizzabile. Misurato, non temuto.
  2. **`make_rational` locale che normalizza a `IntegerLit`** quando il denominatore è 1: il `make_rational` condiviso dell'integratore no, e la costante sopravviveva come `RationalLit(n,1)` dentro `exp(...)` — `∫e^x/(x+1)` usciva `exp(-1/1)·Ei(x+1)`, non confrontabile strutturalmente. Normalizzazione tenuta locale ad A43 invece di toccare l'helper condiviso.
  3. **Salto della decomposizione in fratti semplici** quando la parte razionale è già un singolo termine decomposto: chiamarla comunque su `(x−r)^{-k}` la fa passare per espansione e rifattorizzazione. (Non è ciò che causava il caso lento — vedi **A49** — ma resta lavoro sprecato eliminato.)
- **Tre difetti PRE-ESISTENTI trovati e nominati** (nessuno introdotto da A43, tutti con repro minimo): **A48** (`simplify(A−A) ≠ 0` per `A = polinomio·trascendente`), **A49** (costo esponenziale nel budget su polo traslato), **A50** (tre identità mancanti che bloccano `D(F)=f` su formule esatte). Su A48 è stata aggiunta in `algebraic_equal.cpp` la mitigazione sound (`structural_equal` sui prodotti incrociati, gemella del test già presente sui lati non incrociati) — dichiarata come mitigazione, non come fix.
- **Confini dichiarati** (spec §6, aggiornata con le misure): denominatori che non si spezzano su Q → nessun match, il controllo prosegue a Meijer G e all'errore preesistente; integrande la cui *sotto*-integrale cade nella famiglia ma il livello esterno no (`∫x·ln x·e^x`) non coperte, conseguenza dichiarata della decisione 1.
- **Banco numerico esteso e MESSO NEL GATE**: `scripts/a43_special_fn_check.py` copre ora anche le 14 generalizzazioni emesse dal motore (mpmath 30 cifre, errore relativo < 1e-20) ed è registrato come test CTest (`a43_nonelementary_numeric_certificate`), quindi la CI lo esegue. Motivo: dove `D(F)=f` non chiude (A50) il test C++ può solo confrontare con una stringa scritta a mano — se la formula fosse sbagliata, implementazione e attesa lo sarebbero insieme e il test passerebbe. Il banco è l'oracolo indipendente. CMake **sonda** gli interpreti cercando chi importa `mpmath` (misurato: `find_package` sceglieva un Python 3.14 che non ce l'aveva) e, se nessuno lo fa, emette un warning esplicito invece di registrare nulla — un certificato assente dev'essere visibile, non passare per verde.
- **Gate misurati**: quick suite **2877/2877**; benchmark **6/6 sotto baseline** (nessun costo aggiunto sui path caldi, che era il rischio reale toccando `mathematically_equal`); golden **982 pass** (da 979, **+3**) con gli 8 FAIL preesistenti (4 canonicalizzazione trig, 1 `abs(a)`, 3 convenzione di segno gcd — nessuno introdotto qui); certificato numerico CTest verde; anti-monolito 0 violazioni; zero warning.
- **Baseline golden NON consolidata di proposito**: la misura di consolidamento ha dato 9 FAIL invece di 8 con lo **stesso binario** — una entry oscilla SKIP↔FAIL secondo il carico. Aperta come **A51**; consolidare su una misura non riproducibile sarebbe stato lo stesso errore del ratchet su dati stantii. ✅ Risolto con A51 (2026-07-27): baseline ora `PASS_FLOOR=979 / FAIL_CEILING=4 / UNDECIDED_CEILING=43`, consolidata su misure invarianti rispetto al budget.
- ⬇️ diagnosi originale, conservata per contesto:
- **Scoperta (triage A37)**: ~13 skip la cui antiderivata **non esiste in forma elementare** — l'`Unimplemented` è corretto, ma giac/Maxima rispondono con le funzioni speciali e noi no: `∫e^x/x` (Ei), `∫sin(x)/x` (Si), `∫e^x/(x+1)`, `∫e^x/(x²+1)`, `∫e^x/x²`, `∫e^{−x}log x`, `∫e^x log x`, `∫log(x)/(x²+1)`, `∫log(x+1)/x` (−Li₂(−x)), `∫log(x)²/(x+1)`, `∫e^{x²}` (erfi).
- **✅ Prerequisito CHIUSO 2026-07-25 — spec redatta e censimento fatto**: [`Nonelementary_Antiderivatives.md`](.APROJECT_REFERENCES/MISSING_FEATURES_SPECS/Nonelementary_Antiderivatives.md) (la regola 0.1 bloccava la task: nessuna spec esisteva).
  - **Censimento a codice**: già builtin `Erf`, `Erfc`, `Gamma`, `GammaIncomplete(+Lower)`, `Digamma`, `Polygamma`, `Beta`, `Pochhammer`, `LambertW`, `Hypergeometric0F1/1F1/2F1`, `MeijerG`, `Elliptic*`, `Bessel*`. **Assenti tutti gli 8** che servono: `Ei`, `Si`, `Ci`, `li`, `Li₂`, `erfi`, `Shi`, `Chi`.
  - **Ogni formula verificata numericamente PRIMA di entrare nella spec** — banco riproducibile `scripts/a43_special_fn_check.py` (exit code usabile in CI): 8 derivate con `mpmath.diff`, 4 identità di riduzione, 9 primitive con `mpmath.quad` su intervalli definiti. **Il banco ha smentito una riga della prima stesura**: `∫e^{−x}ln x` è `−e^{−x}ln x **+** Ei(−x)`, non `−`; il segno sbagliato sarebbe finito in codice.
  - **Decisioni di design fissate nella spec**: (a) le identità (`li(x)=Ei(ln x)`, `erfi(x)=−i·erf(ix)`, `Shi/Chi` da `Ei`) vanno in `mathematically_equal` accanto a `hyperbolic_normalize`/`factorial_gamma_normalize`, **non** come canonicalizzazione globale (churn di massa — stessa valutazione di A44); (b) il fallback va **dopo Risch**, che è la procedura di decisione (stesso vincolo di `Meijer_G_Slater.md` §8); (c) `∫e^u·R(u)` va chiuso con la **riduzione** a `Ei` + termini elementari, non con le righe della tabella; (d) `Li_n` per `n>2` e gli argomenti oltre il taglio sono **fuori scope dichiarato**.
  - **Piano in 4 incrementi indipendenti** (§8 della spec): enum+round-trip → derivate → identità (chiude il ponte `erfi` ereditato da A44) → integrazione per famiglia, con gate completo per incremento.
- **Nota**: `∫e^{x²}` è già confrontabile a meno del ponte `erfi(x) ↔ −i·erf(i·x)`. **Ereditato da A44 il 2026-07-25** dopo verifica: `erfi` **non esiste a codice** (nessun `BuiltinOp::Erfi`), quindi non è un residuo minore di normalizzazione — il ponte nel runner (`maxima_parser.hpp:174-176`) va aggiunto *dopo* il builtin e la regola d'integrazione, che appartengono a questo tavolo.
- **Refs**: A37, A7 (Meijer-G/§5.9 gamma incompleta), Bronstein cap. 5

### A44 · Residui minori dal triage A37 — ✅ FATTO 2026-07-25 — `[E1·C1·S1·R1]`
- **✅ `diff(factorial(x))` CHIUSO 2026-07-25**: valeva `Differentiation is not implemented for function 'factorial'`. Implementata `d/du u! = Γ(u+1)·ψ(u+1)` in `differentiate_rules.cpp`, accanto al ramo `Gamma` e con le stesse ortografie canoniche (`gamma`/`polygamma` — un nome non canonico non si risolve via `builtin_from_name` e la derivata porterebbe una funzione opaca, cfr. fix A37). **Identità verificata numericamente prima di implementare** (mpmath, 30 cifre, punti `{0.5, 1.3, 2.7, 4.2, −0.4}` incluso argomento negativo).
  - **Due ortografie, una sola regola**: `x!` è `UnaryOp::Factorial`, `factorial(x)` è `FuncCall` — path distinti, e il postfisso falliva con un `Unimplemented` separato. Il ramo `Unary` ora **riscrive** a `FuncCall` e delega, invece di duplicare la formula: unica fonte di verità per l'identità.
  - **Secondo bug, trovato dal golden e non dai test unit**: con la sola regola di derivazione l'entry passava da SKIP a **FAIL**. Causa: CAS emette `(1/x + ψ(x))·Γ(x+1)`, Maxima `(1/x + ψ(x))·x!` — stessa funzione (entrambi applicano già la ricorrenza `ψ(x+1) = 1/x + ψ(x)`), ma `mathematically_equal` non sapeva che `u! = Γ(u+1)`. Aggiunto `algebra::factorial_gamma_normalize` (in `hyperbolic_normalize.cpp`, stesso file e stesso ruolo del rewrite sech/csch già presente) applicato a **entrambi i lati** in `mathematically_equal`, come per gli iperbolici. Γ scelto come rappresentante perché ha dominio più ampio e porta già le regole di derivazione/riflessione. Nessun caso speciale per i letterali: `factorial(5)` → `gamma(6)` e il constant folding esistente lo riduce.
  - +4 test permanenti (`test_differentiate_smoke.cpp`): `FactorialFuncCallDerivative`, `FactorialPostfixMatchesFuncCall` (le due ortografie devono coincidere), `FactorialChainRule` (`d/dx (x²)!`), `FactorialAndGammaAreRecognizedEqual` (4 coppie equivalenti **+ un caso negativo**: `factorial(x)` vs `gamma(x+2)` deve restare diverso, altrimenti la normalizzazione starebbe collassando shift genuini).
  - **Gate**: golden **977 → 978 pass / 9 fail** (skip 40→39), baseline consolidata `PASS_FLOOR=978`. Quick suite 2856/2856 in 596s. Benchmark 6/6 ≤ baseline. Zero warning.
- **✅ Ponte `erfi` CHIUSO in A43 il 2026-07-26**: builtin `Erfi` + identità `erfi(x) = −i·erf(i·x)` in `nonelementary_normalize` + riscrittura `erf(±i·u) → (±i·erfi(u))` in `maxima_parser.hpp` al posto dello SKIP. A44 non ha più residui.
- **➡️ Ponte `erfi` SPOSTATO in A43 il 2026-07-25** (non era un residuo minore): verificato a codice che **`erfi` non esiste** — nessun `BuiltinOp::Erfi`, nessuna occorrenza in `src/`. Il ponte nel normalizzatore del runner (`maxima_parser.hpp:174-176`, che oggi SKIPpa gli `erf` ad argomento immaginario) da solo non farebbe passare l'entry: il motore non sa comunque integrare `∫e^{x²}`. Serve prima `erfi` come builtin + la regola d'integrazione, cioè il tavolo delle funzioni speciali che **A43** possiede.
- **Refs**: A37, A43 (ponte erfi), `src/calculus/differentiate_rules.cpp`, `test/golden/maxima_parser.hpp`

### A40 · Silent-wrong GCD multivariato: Zippel-Prony Fp-monic scarta il coefficiente direttore — ✅ FATTO 2026-07-23 — `[E3·C3·S2·R2]`
- **Scoperta (A37, rimozione skip gcd runner)**: `gcd(x*y²−x, x*y−x)` (= `x(y−1)(y+1)` vs `x(y−1)`, GCD vero = `x*(y−1)`) restituiva silenziosamente **`x`** — un divisore proprio, non il GCD massimale. Confermato con 5 istanze dal corpus golden (`multivar_x_factor`, `multivar_xy_common`, ecc., pattern `a*(fattore lineare)²−a*(fattore lineare)`).
- **Root cause**: `gcd_zippel_prony` (`polynomial_gcd_zippel_prony.cpp`, Zippel EUROSAM 1979 + Prony-Vandermonde) specializza `P,Q` a punti Fp e calcola `gcd` univariato in `x₁` via `univariate_gcd_fp_dense` — **forzato MONICO** (convenzione standard su campo). Quando il coefficiente direttore del GCD vero in `x₁` è un polinomio NON costante nelle altre variabili (qui `y−1`), ogni sample monico collassa alla stessa costante 1, e Prony interpola correttamente — ma dal dato SBAGLIATO — il coefficiente direttore costante 1, sottostimando il fattore condiviso. `certify_divides` (esistente) certifica solo che il candidato DIVIDE `P,Q`, non che sia MASSIMALE — un divisore proprio come `x` passa comunque. `gcd_brown_modular` (algoritmo diverso, nessuno step di normalizzazione monica per-sample) è immune e dà il risultato corretto.
- **Fix**: certificato di MASSIMALITÀ indipendente (`is_maximal_gcd_candidate`, nuovo) — dopo `certify_divides`, calcola i cofattori `P/g`, `Q/g` (via `exact_quotient` esistente) e verifica `gcd(P/g, Q/g) = 1` con `gcd_multivariate_eval_interp` (algoritmo Z-esatto, senza lo step Fp-monico, quindi non soggetto allo stesso bias). Se non massimale → `Unimplemented` strutturato (`ZIPPEL_PRONY_NOT_MAXIMAL`), che fa cadere il dispatcher (`gcd_zippel_sparse`) sul fallback esistente `gcd_brown_modular` (già corretto per questa classe — nessuna nuova infrastruttura, solo instradamento sul path sano). Applicato sia al path single-prime sia al fallback CRT (stesso bias strutturale, deterministico su ogni primo). Generale — non hard-code per il caso di test, copre qualunque `gcd` multivariato con GCD a coefficiente direttore non costante.
- **Verifica**: 4 nuovi unit test (`test_polynomial_gcd_multivariate_smoke.cpp`, famiglia dei 5 fail golden) verdi; gcd/Zippel/Brown/Wang mirato 134/134 (0 regressioni); quick suite 2831/2831; golden gcd 70→78 pass (vedi A37).
- **Refs**: A37 (scoperta), A18 (bound Mignotte, prerequisito che aveva già sbloccato il call multivariato nel runner), Geddes-Czapor-Labahn §7.6 (leading-coefficient problem in sparse GCD — la fix corretta "da manuale" sarebbe ricostruire il coefficiente direttore via `gcd(LC_{x1}(P), LC_{x1}(Q))` e scalare i sample Prony di conseguenza; il certificato di massimalità qui implementato è la via SOUND equivalente che riusa infrastruttura esistente invece di riscrivere la fase 2 di Zippel).

### A38 · Wiring `solve_risch_de_parametric_field` nel path integrate() reale — ✅ FATTO 2026-07-20 (torri annidate risolte end-to-end) — `[E3·C4·S3·R2]`
- **✅ TORRI ANNIDATE RISOLTE 2026-07-20 (terzo giro, HC-A26 fase 5)**: il residuo di completezza era, come previsto, in `solve_risch_de_parametric_field` sul caso PRIMITIVO. Due gap distinti, entrambi chiusi: (1) **cancellazione dei poli** (nuovo `src/calculus/risch_param_pole_cancel.cpp`) — nel caso primitivo Teorema 5.1.1 dà `k⟨t⟩=k[t]`, quindi `Σ c_i·g_i` deve essere polinomiale in `t` anche quando le singole `g_i` hanno poli: `reduce_parametric_forcing_poles` trova il sottospazio `{c : Σ c_i·g_i ∈ k[t]}` via null space e riduce la famiglia (il vecchio `D=lcm(den)` assumeva che la soluzione ereditasse i poli → `f_new=f−D'/D` non polinomiale → bail spurio); (2) **degree bound** (Bronstein §6.3 `RdeBoundDegreePrim`, `d_a=0`): ramo `df==0` log da `N=dg_max` a `N=dg_max+1` (la soluzione può avere grado uno più della forzante — `q=x·log x`). Ora `∫(1/x+1/(x·ln x))·ln(ln x)dx = ln(ln x)²/2 + ln(x)·ln(ln x) − ln(x)` si risolve end-to-end (verificato per derivazione). 263 test Risch/Integrate/Parametric PASS, quick 2807/2807, ratchet 934/10 invariato. Residuo **HC-A26-RDEBOUND-CANCEL-GAP** CHIUSO 2026-07-20 (NON era un gap): per `d_a=0` (a=1 dopo il clearing) `N=dg_max+1` è provato COMPLETO — sostituzione `q=z·h`⇒`Dh=c/z`, l'integrazione primitiva alza il grado in t di ≤1, e nessuna soluzione di grado `dg_max+2` esiste (richiederebbe `∫η=log(u)∈k`). Il "further increment §6.3.3" è il ramo generale `d_a>0`, che questa funzione non attraversa. Prova in-linea al codice + ledger.

- **Scoperta (audit 2026-07-19)**: A1 (df>0 NoCancel1) e A26 (validazione tower solver) sono entrambe ✅, ma `solve_risch_de_parametric_field` ha **zero caller esterni** (grep: solo auto-ricorsione in `risch_rde_parametric_field.cpp:197` + harness di test). La macchina parametrica RDE è costruita, sound e testata via harness — ma NESSUNA task aperta possedeva il wiring nel path `integrate()` reale: caduto tra le maglie della chiusura di A1/A26 (entrambe scopavano "wiring = passo successivo" senza aprirlo).
- **Fatto (2026-07-20)**: letta spec `Symbolic_Integration_I.md` Cap.7 §7.2 (Limited Integration Problem). Nuovo `limited_integrate_field` (`src/calculus/risch_limited_integrate.cpp`) risolve `f = D(v) + Σc_i·w_i` via il solver parametrico esistente (§7.1, A1/A26) + vincolo `c_0=1` (eq. 7.36) — sound-by-construction, back-substitution esatta. Wired in `integrate_log_polynomial_part` (§5.10, `integrate_risch_hermite.cpp`): ogni livello k≥1 della ricorsione poli-in-log ora tenta limited integration nel campo inferiore invece del vecchio `b_k=∫rhs` cieco (che assumeva implicitamente il coefficiente top `b_{n+1}=0`, non generale). Invariante forma-generatore corretto in tutto il file (fix A27-style: `eta_gen` invece di `du_over_u` per le correzioni, round-trip `to/from_field_generators` attorno al fallback `integrate()`). 242 test regressione Risch/Integrate + 9 nuovi test A38 PASS, quick suite 2806/2806 PASS, golden ratchet 934/10 invariato.
- **✅ HC-A38-01 CHIUSA 2026-07-20 (secondo giro)**: la ricorsione non limitata sui coefficienti con generatore lower-tower era **strutturale**, non algebrica. Bronstein §5.2/§5.7: la ricorsione deve eliminare un monomio (ricorrere su `k = C(t_1..t_{n-1})`) o abbassare `deg_t(p)` — il fallback per-livello faceva invece un **root-restart su `integrate()` generico**, che ricostruisce la torre da capo (nessuna misura decresce → loop). Fix: fallback dispatchato sul campo inferiore (`integrate_risch_poly_and_rational_part(..., lower_field, ...)` per `k ⊋ Q(x)`; integratore generico solo per `k = Q(x)`, base case non rientrante). Guard `coeff_blocks_poly_quotient` + `has_var_transcendental` **rimossi** (bail-out Cat-4 non più necessario). Il caso `∫(1/x+1/(x·ln x))·ln(ln x)dx` passa da loop infinito a terminazione in ~4.7s; `∫e^{-x}·ln(x)dx` blindato contro la vecchia forma silenziosamente sbagliata. 243 test Risch/Integrate PASS, quick 2807/2807, ratchet 934/10 invariato.
- **Residuo di COMPLETEZZA (non più di terminazione)**: la torre annidata termina ma riporta `Unimplemented` invece dell'antiderivata esistente (`ln(ln x)²/2 + ln(x)·ln(ln x) − ln(x)`). Il limite è nel solver §7.2: `limited_integrate_field` non risolve una forzante con **denominatore nel generatore** — è il base case `Q(x)` polynomial-only già tracciato da **HC-A26-PRIMITIVE-PARAMQ-RATIONAL** (nessuna voce di ledger nuova: stesso debito, un livello più in alto).
- **Valore**: sblocca il valore utente di A1+A26 per torri a un livello (raggiungibile da `integrate()` reale, non solo dai test) — classe `∫` con RDE parametrica che Giac/Maxima gestiscono. Le torri annidate ora terminano pulite; la loro chiusura passa da HC-A26-PRIMITIVE-PARAMQ-RATIONAL.
- **Refs**: A1 e A26 (residui wiring, chiusi da questo lavoro per single-level), HC-A38-01 (✅ chiusa), HC-A26-PRIMITIVE-PARAMQ-RATIONAL (gap di completezza residuo), test_risch_parametric_tower.cpp, test_risch_limited_integrate.cpp

### A36 · `guard_test_timeout.py`: split dei segmenti ignora il contesto quote → falso deny — ✅ FATTO 2026-07-19 — `[E1·C1·S1·R1]`
- **Repro**: `pgrep -lf "test_quick|debt_gate|cas_foundation_tests"` → DENY "REGOLA TIMEOUT TEST" benché nessun binario di test venga eseguito. Causa: `re.split(r"&&|\|\||;|\||\n", cmd)` (`.claude/hooks/guard_test_timeout.py:30`) spezzava ANCHE i `|` dentro stringhe quotate, e il frammento `cas_foundation_tests"` diventava primo token di un segmento fittizio.
- **Fix 2026-07-19**: `split_segments()` quote-aware (state machine su `'`/`"`/escape; separatori `&& || ; | \n` splittano solo fuori quote). Verifica 6/6: repro passa, violazione secca DENY, timeout+filtro passa, pipeline con violazione DENY, `test_quick.sh` passa, `ctest` nudo DENY.
- **Mitigazione collaterale**: `guard_gate_lock.sh` ha early-exit per comandi di sola ispezione (pgrep/ps/grep/tail/...) — i comandi usati per controllare se un gate gira non vengono mai negati. `guard_anti_loop.sh` (stessa classe potenziale): non fixato — al peggio conta un pgrep come "heavy run" nella history (warn spurio, mai deny di comando legittimo); accettato.
- **Impatto**: solo tooling agent (nessun codice motore).

---

## B — RESEARCH / WON'T-DO-NOW (aperta-permanente by-design)

Fuori scope deliberato (vedi `PLAN_HP_PRIME` §STATUS PERMANENTI). Parità HP Prime ≥95% **non** li richiede. Confermati assenti a codice = coerente con la scelta. **Non** sono lavoro dimenticato.

| Voce | Motivazione | Fallback attuale a codice | Ledger |
|---|---|---|---|
| Schönhage-Strassen NTT BigInt mul | SS NTT = mesi effort, GMP 30 anni tuning | Karatsuba + Toom-3 + Burnikel-Ziegler (tutti presenti) | HPP-F1.1-MUL |
| Collins CAD McCallum multivar | complessità doppia esponenziale | solo disequazioni 1-var via Sturm (presente) | F6.A / Fase 8 |
| Risch structure theorem full (Bronstein cap 9) | 600pp, multi-mese math expert | Liouville+Hermite+Trager+log-deriv (presenti) | — |
| Hypergeometric ₚFq recognition completo | tabelle Wilf-Zeilberger vaste | Gauss ₂F₁, Saalschütz, casi noti | — |
| Multi-sheet Riemann surface | AST single-valued incompatibile | branch principale (presente) | — |

### B.1 · Golden `simplify`-fail = soundness deliberata (NON fixare alla cieca)
Audit golden 2026-06-28: i 5 fail `simplify` vs Maxima (`exp(log(x))→x`, `log(x^n)→n·log(x)`, `log(1/x)→−log(x)`, `abs(x^2)→x^2`, `0^x→0`) **non sono bug**: valgono solo sotto ipotesi `x>0` / dominio reale. Maxima sovra-semplifica assumendo dominio reale; il CAS è **complex-aware conservativo** (A14 soundness, `is_known_nonnegative` senza regola even-power perché `x=i⇒x²=−1`). Allinearsi a Maxima = introdurre falsi-positivi (REGOLA ZERO). Il fix corretto sarebbe un **default real-domain configurabile** (decisione architetturale cross-cutting per parità HP Prime, fuori scope sessione singola), non hack per-regola. **Golden aggregate 2026-06-28: ~97.7%** (corpus integrate basic.jsonl = 140 entry, 116/6/18 fresh; ⚠ `build-golden/report.json` *merged view* è stale/inaffidabile — fidarsi del per-area output, non del totale 230≠140).

### B.2 · Golden `integrate`/`diff`-fail = gap canonicalizzazione trig (risposte già corrette) — 🔨 PARZIALE 2026-06-28 — `[E3·C4·S2·R3]`
- **FATTO 2026-06-28** (commit `3b15be1`): aggiunto building-block **sound** `trig_exponential_zero_diff` (`algebraic_equal_trig_exp.cpp`) — forma canonica Laurent in `z=e^{ix}` per **polinomi trig** (e razionali via cross-multiplication), wired in `mathematically_equal` dopo `weierstrass_zero_diff`. Prova product-to-sum freq-diverse + Fourier (`cos(x)cos(2x)=(cos x+cos3x)/2`, `sin³x cos²x` linearizzato) dove il path `t=tan(x/2)` cede. SOUND (solo identità esatte, bail su non-trig-poly → mai falso-positivo; A14 verde). +4 test, suite quick 2489 verde, zero regressione. Anti-monolith: split `algebraic_equal_subset_risch.cpp` (file era 504 LOC).
- **✅ SUB-GAP abs/half-angle CHIUSO 2026-06-28** (commit `ec651d5`): esteso `trig_exponential_zero_diff` da polinomi a **RAZIONALI trig** — la forma esponenziale ora modella `N(z)/D(z)` (Laurent num/den), gestendo **potenze intere negative** e **trig al denominatore** via cross-mult esatta di mappe Laurent. `N/D≡0 ⟺ N≡0` (D≢0), sound perché un Laurent che si annulla su un arco del cerchio unitario è il poly nullo. **Diagnosi corretta**: l'`abs` NON era il problema — `diff` già applica `d/dx ln|u|=u'/u` e droppa l'abs; il blocker reale erano le **potenze negative di half-angle** (`cot(x/2)`, `cos(x/2)⁻²`) nel derivato. **Golden flip verificato (sound)**: integrate `116→118` (famiglia `1/sin x`), diff `78→79` (`sin/cos`). +3 test rational-equiv + 1 rational-soundness-reject (`TrigIdentitiesTest` 21/21), A14 verde, file 247 LOC.
- **✅ FLIP `∫x²/(x²−1)` 2026-06-28** (commit `7ee0935`): root cause era un **gap in `diff`**, non equivalenza — `differentiate_visitor.cpp` faceva bail `Unimplemented` su `ComplexLit` (`"Complex literals differentiation not implemented"`). Ma `ComplexLit` è una **costante numerica** → `d/dx≡0` come `IntegerLit`/`Constant`. L'integratore razionale emette `∫x²/(x²−1)=x−i·arctan(−i·x)` (con `ComplexLit(−i)`, non `Symbol`); `antiderivative_equivalent` non riusciva a derivarla. Fix: `ComplexLit → 0`. Ora `diff(x−i·arctan(−i·x))=1+1/(x²−1)` matcha Maxima. **Golden integrate 118→119**. Sound (costante→0, nessuna ipotesi di dominio). +2 test, quick suite 2495 verde, zero regressione.
- **✅ FLIP `∫√(x²−1)` 2026-06-29** (commit `c27f1ee` + fix simplify): erano DUE bug. (1) **`unknown(x)`**: NON era un bug d'integrazione — l'antiderivata corretta è `½x√(x²−1)−½·acosh(x)`, ma `simplify_node` (`simplify_functions.cpp`) ricostruiva ogni `FuncCall` da `node.func_id`; `acosh/asinh/atanh` NON sono enumerati in `BuiltinOp` (→ `Unknown`), e `builtin_name(Unknown)="unknown"` → il nome veniva corrotto in `unknown(x)`. Fix: ricostruzione name-preserving quando `func_id==Unknown` (generale, ogni funzione non-enumerata). (2) **Equivalenza radicale**: nuovo building-block sound `radical_zero_diff` (`algebraic_equal_radical.cpp`) — estensione singola radice `Q(x)(√p)`, rappresenta il diff come `(A+B·s)/(C+D·s)`, `A,B,C,D∈Q[x]`, `s²→p`; `diff≡0 ⟺ A≡0 ∧ B≡0` (sound incondizionato). Wired in `mathematically_equal` **prima** del dispatch RootOf (un `√quadratica` è RootOf di `t²−p`, altrimenti short-circuit a false), con `operation_active_` azzerato temporaneamente (il `polynomial_normal_form`→`expand` non è idempotente sotto operazione attiva → coordinate non ridotte → falso negativo). **Golden integrate 119→120**. +5 test, quick suite verde, diff golden 79/1 invariato.
- **🔧 RESIDUO (real-domain `abs`, NON forzare — vd. B.1)**: restano 2 fail integrate **inverse-hyp/algebrici a dominio reale** — `∫1/sqrt(x²+1)` (CAS `ln|x+√(x²+1)|` vs Maxima `asinh x`: bridge `asinh→ln` già in `hyperbolic_normalize`, blocca solo l'**`abs`** — `x+√(x²+1)>0` vale solo su ℝ), `∫1/sqrt(a²−x²)` (`arcsin(x/a)` vs `arcsin(x/|a|)`, `abs` su parametro, vale solo `a>0`); e `diff(x/sin x)` = **polinomio×trig misto** (`x` Symbol nudo × trig → bail sound). I casi `abs` sono la tensione real-vs-complex di B.1: fix corretto = **default real-domain configurabile** (cross-cutting), non hack per-caso.
- **🔬 INDAGATO 2026-06-28, NON gating (no fix)**: il `diff` timeout su `arctan(tan(x/2)/√3)` **non blocca alcun golden** — `∫1/(2+cos x)` già PASSA (confronto antiderivate diretto, non dipende da quel `diff`). Root cause accertata (diag): `integrate` ritorna l'antiderivata **non-canonica** (`materialize_expr` senza simplify → scaffolding `1·…`, termine spurio `−(0·x)` dalla back-sub Weierstrass); `diff` su quel wrapper sfora il budget simplify 1000ms (`diff(raw F)` ~1334ms→timeout vs `diff(simplify(F))` ~1044ms OK). **Fix tentato e REVERTITO**: simplify dell'output di `integrate` (con fallback su errore) → **0 flip golden + 3 regressioni unit** (`XTimesAsinhX`: il `diff` della forma asinh semplificata va a sua volta in timeout; `IntegratesClassicalTrigSubstitutionRadicals`, `IntegralOfErfTimesExp`). Conclusione: il timeout è **debito perf R3 di `diff`/`simplify`** su `arctan(g)` con `g` a potenze negative, non risolvibile via canonicalizzazione dell'output senza regredire altre famiglie. Eventuale fix corretto = riduzione mirata del quoziente `u'/(1+u²)` in `diff` (multi-sessione), oppure cleanup *leggero* (prune 0-termini/flatten `1·`) localizzato in `integrate_weierstrass` — ma **valore nullo** (nessun golden gated). Tutti C4·R3, sessioni a sé.
Audit 2026-06-28: i fail `integrate` (~6 su basic.jsonl: `1/sin(x)`, `1/(1+sin x)`, `1/(2+cos x)`, `sin³cos²`, `1/(sin·cos²)`, `1/(sin·cos)` …) e `diff` (2: `x/sin(x)`, `sin/cos`) **non sono bug di correttezza**: le antiderivate CAS sono **verificate corrette** dai unit test via `D(F)==integrand` (`test_integrate_inverse_trig.cpp::expect_integral_correct` passa). Falliscono nel golden perché `antiderivative_equivalent` (`test/golden/integrate_equiv.hpp`) confronta `D(cas)` vs `D(maxima)` e **`simplify` non riesce a provare `D(cas)−D(maxima)=0`** per forme trig equivalenti (Weierstrass `tan(x/2)=cos(x/2)⁻¹·sin(x/2)`, half-angle `cos(2x)`). Es. `∫1/sin(x)`: CAS `ln|tan(x/2)|` (forma da manuale) = Maxima `½log((cos x−1)/(cos x+1))`.
- **Root cause reale**: il simplifier manca di una **forma canonica per razionali trigonometrici** (power-reduction/double-angle folding orientato, es. `½cos(2x)+½ ↔ cos²x`, Weierstrass-collapse). Migliorarlo flipperebbe questi entry E rafforzerebbe `mathematically_equal`/diff/integrate-verify in generale. Research-grade, hot-path simplifier (R3).
- **NON fare ora alla cieca**: rischio regressione su hot-path; e modificare l'oracolo `antiderivative_equivalent` per aggirare (es. confronto `D(cas)==integrand`) è sound ma marginale (testato 2026-06-28: +1 entry, bloccato dallo stesso limite simplify) e tocca un benchmark → cautela REGOLA 0.2 (revertito). **Il vero lavoro è la canonicalizzazione trig nel simplifier.** **Refs**: F7.5 trig, golden integrate/diff corpus

---

## C — FATTO E VERIFICATO (non rifare)

Confermato a codice 2026-06-26. Elencato per evitare ri-lavoro (i vecchi tracker davano molti di questi come aperti).

**Foundation/BigInt**: Toom-3 (`bigint_mul_toom3.cpp`), Burnikel-Ziegler (`bigint_div_burnikel_ziegler.cpp`), Lehmer GCD, Miller-Rabin basi fisse. *(commit 70a1e52, 4131398)*

**Algebra univar**: Half-GCD (`polynomial_half_gcd.cpp`), Berlekamp, sparse interpolation, cyclotomic detection >724 *(commit dd27a51, T-022)*.

**Algebra multivar/ext**: Wang EEZ + Kronecker fallback, Zippel sparse GCD CRT+Farey *(commit ad023bf, T-006)*, **CRT modular resultant** (`polynomial_resultant_crt.cpp`, Collins 1971 — *era erroneamente dato aperto come T-012*), FGLM + F5 Groebner, tower 2-level + nested squarefree.

**LinAlg**: **Householder QR simbolico razionalizzato** (`matrix_qr.cpp` — *PLAN_HP_PRIME lo dava aperta-perm/MGS, è stale: codice ha risolto l'AST-explosion via reflector `I−2vvᵀ/N` senza √ al denominatore*), Cholesky LDLᵀ, fresh-symbol pervasivo, adaptive G7/K15 quadrature *(commit 5268e90)*.

**Calculus**: Kovacic Case 1 (Laurent √r poli≥4, *commit 709a437, T-008*) + Case 2; Risch trial-constants rimossi (`integrate_risch.cpp:69`), Rothstein-Trager generalizzato, Hermite con D arbitrario; Zeilberger/Gosper/Petkovšek higher-order *(commit 8440ba5, 3addb54, T-027)*; ∫xᵏ/√(c−dx²) *(commit 830f80e, T-055)*, ∫1/√(Ax²+Bx+C) *(6c5ae48)*, inverse-trig IBP, ∫log(x+√(x²+a)) *(ec9e065)*; **limit signed-infinity per poli non-polinomiali a punto finito** (`tan(x)/(x−π/2)→−∞` via sign del reciproco, `try_signed_pole_via_reciprocal`, *commit fc537e5*).

**Special functions (F7.5.E1 — Γ/B/ζ/erf identities)**: ✅ **DONE** (verificato golden 2026-06-28: area `special_fn` = **75/0/5 = 100%**, spec target era ≥82%). ζ pari/dispari via **Bernoulli** (no tabella, REGOLA ZERO, `simplify_special_fn.cpp:380-413`), Γ half-integer + functional-eq + poli→ComplexInfinity, Beta→Γ, Digamma/Polygamma/Pochhammer, erf/erfc. *La spec `Special_Fn_Identities.md` era erroneamente citata da A7 (Meijer-G) — sono task diversi.*

**Complex/branch-cut**: BC-1..BC-3 + BC-1b corrections, ln(a+bi) esatto *(commit 89eae9e, T-017)*, cyclotomic↔exp equal *(4597332, T-025)*, extended-real arithmetic *(b145cb9)*, UnwindingNumber builtin.

**Numerica**: fsolve tolerance ctx-param (`fsolve_tolerance_bits`, T-019), solve_inequality via Sturm, RootOf isolation bounds, Bessel identities *(ade20ff)*, statistics package (`src/statistics/`).

**Infra**: **Phase-7 & Phase-8/Task-25 anti-monolith DONE (HC-F8-PENDING-25)** — tutti i 28 file ex >500 LOC ora <500 *(commit 7aa5f2e + T-046..050, T-029..051, A29 split)*; flaky cos(7π/16) fixed via co-function *(d03ff6d, T-004)*; rapidcheck property tests; golden aggregate **94.5%** (F7.5); **A2 hard-timeout RDE solver interrompibile** *(commit 388f6f5)*; **untracked top-level `_deps/` cruft + gitlink rotto rimossi, `_deps/` gitignored** — il dep tree reale è `build/_deps` da FetchContent *(2026-06-28)*; `ledger_index.py` 3-tier classifier + `doctor` *(03928f7)*.


---

## D — Grafo dipendenze (task aperti)

```
A1 (Risch RP-2 Hermite) ──> A7 (Slater/Meijer G fallback, raccomandato)
A9 (tower ≥3) ──> A4 (Smith Q[x]) [parziale], A10 (eigen/Jordan RootOf)
A11 (Gruntz dinamico) ──> A13 (residue) [indipendenti ma stesso dominio limit]
A2 (hard-timeout) — indipendente, ALTA priorità (S5 hang)
A16..A24 (debiti minori) — indipendenti, inframmezzabili
A35 (per-entry cross-diff) — utile al verdetto di equivalenza vs giac; NON era
                             prerequisito del triage A37 (chiuso 2026-07-24 coi log per-entry)
A37 (triage) ──> A41 (limit, CHIUSA) · A42 (integrate elementari, CHIUSA) · A43 (non elementari) · A44 (minori)
A45 (LRT: tau_k + conformita' Bronstein, CHIUSA) ──> A46 (LogToReal / RootSum in forma chiusa)
     A45 ha sbloccato la famiglia grado-4 di A42 con fattore quadratico (+3 pass golden)
A42 Weierstrass tan/multi-angolo — RIMISURATO 2026-07-25: patch dello stash RESPINTA
     (405ms/NO_STRATEGY diventa 192s/verified=0; antiderivata a 526 nodi indifferenziabile).
     Il fix e' la LINEARIZZAZIONE dei polinomi trigonometrici, non Weierstrass ──> A47
A47 (verifica D(F)=f in IBP: together 46s + simplify 60s) — debito su HEAD, emerso dalla
     misura A42; sbloccarlo abbassa il costo di ogni integranda trigonometrica che passa da IBP
A46 (LogToReal + routing per forma, CHIUSA) ──> ultime 2 entry grado-4 di A42 (chiuse, +2 pass
     golden → 977) e i ~30s di 1/(x⁶+1) (41s → 1.3s, causa = dispatch per forma, NON il RootSum).
     Residuo fuori classe: quartici con radici reali, resultant irriducibili di grado ≥5, cubici.
A43 (Ei/Si/Li₂/erfi) ──> A44 (ponte erfi nel runner) [stesso tavolo di funzioni speciali]
A39 (canonicalizzazione Product/MeijerG) — indipendente, minore, qualità non-gating
```

## E — Ordine raccomandato

> **Avanzamento 2026-07-26**: **A43 CHIUSA** — tutti e 4 gli incrementi della spec (builtin+derivate, identità §4, ponte `erfi` che chiude il residuo di A44, integrazione §5). 15 test permanenti; le 13 righe della tabella §5 escono nella forma dichiarata e superano `D(F)=f`. Tre decisioni prese su misura e non per intuito: il fallback gira solo sul frame più esterno (a profondità ≥2 la catena per-parti pre-Risch alzava il polo a `x⁻⁸` prima di riavvolgere la forma `Ei`), `make_rational` locale che normalizza a `IntegerLit` (altrimenti `exp(-1/1)` sopravvive e non è confrontabile), salto della PFD sui termini già decomposti. **Tre difetti PRE-ESISTENTI trovati, con repro minimo, e aperti come task**: **A48** (`simplify(A−A) ≠ 0` quando `A = polinomio·trascendente` — famiglia A39, impatto largo su `mathematically_equal`), **A49** (costo esponenziale nel budget di ricorsione su polo lineare traslato; controllo col fallback disattivato: il blocco resta, è pre-Risch), **A50** (tre identità mancanti — radicali sotto `√`, formule di addizione, `ln(xⁿ)=n·ln x` — che impediscono di *provare* `D(F)=f` su antiderivate esatte, certificate mpmath). Ordine consigliato ora: (1) **A48**, che è il più trasversale e il più economico da isolare (repro di due righe); (2) **A47** (debito su HEAD, abbassa il costo di ogni integranda trigonometrica via IBP); (3) **A50**, che condivide il tavolo con A47 e A31; (4) **A49**; (5) **A35** se serve il verdetto per-entry vs giac.

> **Avanzamento 2026-07-24 (2)**: **A41 CHIUSA** — 4 bug distinti in `compute_recursive`/`compute_quotient_limit` (guardia `Ln`-only invece di `Ln|Log`, `Sum` di quozienti a denominatore non identico mai riconosciuto, ramo `Pow` che perdeva `0^∞` e richiedeva l'esponente classificato, ramo `0·∞` che si auto-annullava passando per `simplify()`), più un effetto collaterale (`as_infinity_if_undefined` indovinava `+∞` invece di propagare l'errore per `ln`/`log`). Golden `limit` 88/0/11 → 97/0/2, zero regressioni sulle altre 10 aree, ratchet 961/10 → 970/10. Due regressioni introdotte durante il lavoro (una su `sqrt(x+1)-sqrt(x)`, una timeout su `x*sin(1/x)`) trovate e chiuse in verifica prima del commit. Residue: **A42** (~25 integrali elementari), **A43** (non elementari), **A44** (minori), **A39** (qualità non-gating).
>
> **Avanzamento 2026-07-24**: **A37 CHIUSA** — triage completo, tutti gli skip della golden suite classificati in 5 bucket con `scripts/golden_skip_triage.py` (nuovo, ripetibile). Trovati e chiusi: bug d'infra del ratchet (timeout per area fisso a 300 s → JSON mai riscritto → 35 giorni di gate su dati stantii, ora budget derivato + guard di freschezza che aborta il merge), 3 falsi skip del tooling (righe Maxima continuate, `bessel_j`, `psi[n]`), 2 bug di motore (serie su singolarità eliminabile; `diff(gamma)` con nome non canonico) oltre ad **A40**. I gap di motore residui sono ora task nominate: **A41** (limit, forme indeterminate non classificate — CHIUSA, vedi sopra), **A42** (~25 integrali elementari senza strategia), **A43** (antiderivate non elementari Ei/Si/Li₂), **A44** (minori). Ordine consigliato: (1) **A42** — misurare prima quale fase del dispatcher rifiuta, non 25 fix; (2) **A35** se serve il verdetto di equivalenza per-entry vs giac; (3) **A39**/A44 intercalabili.
>
> **Avanzamento 2026-07-22**: **A34 CHIUSA** (checker/sorter allineati su base-order, canary falso-allarme fixato) — follow-up scoperto e aperto come **A39** (Product annidati con MeijerG non appiattiti, S1 qualità non-gating, no fix alla cieca). **A32 CHIUSA** (wiring completo predicato `assume` CAS+Maxima, ratchet rialzato a `PASS_FLOOR=935`). Task aperte residue in ordine consigliato: (1) **A37** — triage cheap-win, nessuna infra nuova: rigenerare `golden_bronstein.json` stantio (28g) col runner rebuiltato per il Δ vero post-A33, verificare se gli 11 skip `gcd` (`corpus_runner.hpp:311`) sono limite di runner o di motore; (2) **A35** — se il triage A37 mostra che serve equivalenza per-entry reale (oltre i cheap-win), investire nel `--per-entry-json` + parser dialetto giac; (3) **A39** — quality/non-gating, intercalabile quando capita un buco S1; (4) A13/A18 residui — by-design, bassa priorità, riaprire solo con driver reale.
>
> **Avanzamento 2026-07-17**: **A31 FASE 2 FATTA** (flag `conditional_domain_rules` + regole R1-R5 con emissione condizioni, fix bug cache-hit fase 1, `simplify_tracked()`, golden runner consuma il flag per le entry con `assume` — golden simplify 110/6 → 116/0, ratchet in miglioramento). Residuo A31 = fase 3 (integrate/solve, congiunzioni) su driver reale. A32 resta aperta (parser predicati + refs Maxima con assume).
>
> **Avanzamento 2026-07-16**: **A33 CHIUSA** (silent-wrong ∫ razionali-in-exp, residuo iperesponenziale §5.9 — 8/8 test nuovi, 285/285 gate mirato, golden integrate 121/1, ratchet golden VERDE a 928/16 senza ritocchi). **A32 APERTA** (campo `assume` del corpus golden mai applicato; WARN visibile nel runner). Infra: manifest Maxima robusto ai bump di revision Homebrew (contenuto normalizzato, verificato 308/310 identici al bump _5→_6); benchmark.sh → mediana-di-5 + warning load (A/B worktree ha certificato: il "drift +41-77%" era ambiente di misura, NON codice — vedi `test/benchmarks/BASELINE_NOTES.md`; residuo software reale: simplify +21%/arena +14% da A30+A31, deliberato); test_quick.sh: 4 test >100s (49% della suite) spostati in SLOW_OK, quick atteso ~600s — `WeierstrassDirectProbe` (196s isolato, probe senza asserzioni) = candidato indagine perf su `weierstrass_zero_diff`.
>
> **Avanzamento 2026-07-15**: **A6 CHIUSA** (Brick 1-4 fatti; `galois_group` copre deg 8..10 end-to-end; HC-F8-PENDING-09 chiusa). **A31 fase 1 IMPLEMENTATA** (side-conditions in `CASContext`, 2 siti verificati, 13 test, quick suite 2715/2715, benchmark eseguito) — fase 2 residua (censimento completo altri siti + regole B.1 opt-in). Prossima linea attiva = **A7** (Meijer-G, spec `Meijer_G_Slater.md` pronta e verificata, in coda per decisione utente). Minori intercalabili: A22 (Padé Q(π,e,√)), A18-residuo (osservabilità `false`→`Unimplemented`, R2/R3). A5/A13/A11/A26 residui = by-design, riaprire solo con driver reale. Residuo perf noto A6: passo Stauduhar deep deg-9/10 (>90 s) — soffitto documentato, non correttezza.
>
> ⬇️ avanzamento 2026-06-28 (storico): A2 ✅ (388f6f5) · A10 ✅ chiuso con probe (156b18a) · A16 chiarito = deferral ledgered post-parità (non E1) · A4/A12/A14/A15/A17/A24 = già FATTO/stale. Quick-win esauriti.
>
> ⬇️ ordine storico (pre-sessione 2026-06-28), conservato per contesto:

1. ~~**A2**~~ ✅ + **A3** (perf-hang C4, hard)
2. Debiti rapidi E1: ~~A16~~ (deferral) · ~~A17/A24~~ (done) · A18 (ledger-deferred) · **A23** (structured Unimplemented, S1, broad)
3. **A5** (Frobenius, residuo by-design) · **A13** (residue, residuo by-design) · ~~A4~~ (done)
4. **A7** (Slater/Meijer) ← **prossima research, entry-point** · A1 (Risch RP-2) BLOCCATO
5. **A6** (Stauduhar) · **A9** (tower≥3) · ~~A10~~ ✅ · A11 (FROZEN) / A12 (done) / A14 (done)
6. **A15** (done) · **A8** (Kovacic n=12) · **A19-A22** — bassa priorità



---

## F — ESTENSIONI ARCHITETTURALI XCAS / GIAC (lungo termine)

> Macro-aree per il raggiungimento della parità applicativa completa con XCAS/Giac.
> Ogni task richiede la redazione e approvazione di una spec formale prima dell'implementazione (REGOLA 0.1).

| ID | Area | Titolo / Descrizione Algoritmica | Codici | Dipendenze | Stato |
|---|---|---|---|---|---|
| XCAS-EXT-01 | Scripting | **Interprete e Control Flow**: execution engine su AST per blocchi `if/then/else`, cicli `for`/`while`, scope locale/globale e definizioni `proc(args)`. | `[E4·C3·S3·R1]` | Lexer/Parser | Pianificato |
| XCAS-EXT-02 | Calculus | **Trasformate Fourier & Z**: integratore di kernel trascendente ($e^{-i\omega t}$ / $z^{-n}$), tabelle identità, proprietà di convoluzione e traslazione. | `[E3·C3·S3·R1]` | Calculus core | Pianificato |
| XCAS-EXT-03 | Tensor | **Calcolo Tensoriale & Geometria Diff**: oggetti n-dimensionali, indici covarianti/controvarianti, contrazione di Einstein, simboli di Christoffel e operatori vettoriali ($\nabla \times, \nabla \cdot$). | `[E4·C4·S3·R1]` | LinAlg/Symbolic | Pianificato |
| XCAS-EXT-04 | Optimization | **Ottimizzazione & Simplesso**: algoritmo del Simplesso esatto (su Rational) e numerico, solutore moltiplicatori di Lagrange per minimi/massimi vincolati. | `[E3·C3·S2·R1]` | LinAlg esatta | Pianificato |
| XCAS-EXT-05 | Graphics | **Motore Plotting Adattivo**: campionamento adattivo di funzioni cartesiane/parametriche/polari 2D e superfici 3D, generatore di maglie (mesh) agnostico da UI. | `[E4·C3·S2·R1]` | Evaluator numerico | Pianificato |
| XCAS-EXT-06 | Geometry | **Geometria Dinamica & Vincoli**: primitive algebriche (rette, coniche), intersezioni esatte e risolutore di vincoli (incidenza, tangenza) via Basi di Gröbner. | `[E5·C4·S2·R1]` | Gröbner/Resultanti | Pianificato |

---

## Vincoli invariabili (CLAUDE.md)

BigInt only (no int64/double nel core) · Structural sharing · AstArena bump · LPO orientation
· `Result<T>` (no throw/catch) · 500 LOC/file (hard 550) · fresh-symbol pervasivo · REGOLA ZERO
(no via facile, no hardcode non-ledgered) · REGOLA 0.1 (spec read first) · REGOLA 0.2 (no test disable)
· timeout test espliciti (`scripts/test_quick.sh`).
