# F0.6 Anti-monolith whitelist — files that currently exceed the 500-line limit.
# Each entry must have a MANDATORY refactor ticket in CAS_TASKS.md / HARDCODE_LEDGER.md.
# Format: one path relative to repo root per line, comments with #.
# No new entries without a corresponding split task filed.

# src/calculus/limit_mrv.cpp — SPLIT DONE (F7.0-A1.3): now 181 LOC.
#   Split into limit_mrv_set/_compare/_leading/_exp.cpp + limit_mrv_internal.hpp.

# src/algebra/polynomial_gcd_multivariate.cpp — SPLIT DONE (F7.0-A1.2): now 430 LOC.
#   Split into _helpers/_interp/_sparse/_linear.cpp + _internal.hpp.

# src/algebra/factorization_polynomials.cpp — file removed/restructured previously.

# src/calculus/integrate_risch.cpp — SPLIT DONE (F7.0-A1.1): now 338 LOC.
#   Split into integrate_risch_hermite/_rde.cpp + _internal.hpp.

# src/symbolic/simplify_arithmetic.cpp — DEBT CLOSED (W9.3): now 403 LOC (≤500). No whitelist needed.

# src/symbolic/assumptions.cpp — SPLIT DONE (W9.3, 2026-06-18): assumptions_core.cpp
# (mutators/consistency) + assumptions_predicates.cpp (queries/provers), both <500 LOC.

# src/foundation/bigint.cpp — DEAD ORPHAN REMOVED (W9.3): never compiled (not in
# CMakeLists, not #included); superseded by wired bigint_arithmetic/conversions/logic/...
# File deleted, so no whitelist entry needed.

# src/ast/ast.cpp — DEBT CLOSED (W9.3): now 274 LOC (≤500). No whitelist needed.

# src/symbolic/simplify_exp_log.cpp — SPLIT DONE (W9.3): sqrt branch + helpers
# extracted to simplify_sqrt.cpp + simplify_sqrt_helpers.cpp; now ≤500 LOC.

# src/symbolic/simplify_arithmetic_chain.cpp — SPLIT DONE (F1.6-B1): now 440 LOC.

# include/cas/ast.hpp — DEBT CLOSED (W9.3): now 19 LOC (≤500). No whitelist needed.

# src/symbolic/simplify_trig.cpp — SPLIT DONE (F1-DEBT1): arc-trig extracted to
#   simplify_trig_inverse.cpp; simplify_trig.cpp now 455 lines (≤500). No whitelist needed.

# include/cas/symbolic.hpp — DEBT CLOSED (SPLIT-SYMBOLIC-HPP-F2.5).
# Algorithm parameters extracted to cas_context_params.hpp; now 328 lines (≤500).
# No whitelist entry needed.

# Pre-existing violations (split tasks filed in CAS_TASKS.md under SPLIT-F3.x):

# src/calculus/ode_classifier.cpp — SPLIT DONE (F7.0-A1.4): now 172 LOC.
#   Split into ode_classifier_first_order/_higher_order.cpp + _internal.hpp.


# ── SPLIT DONE 2026-06-22 (anti-monolith batch T-029…T-051) ──────────────────
# The following files were split below 500 LOC and their whitelist entries
# removed: context_core, algebraic_tower_bridge, differentiate, builtin_rewrite,
# term_order, algebraic_number_bridge, solve_polynomial, limit,
# orthogonal_polynomials, residue_theorem, algebraic_tower_primitive_internal.hpp,
# polynomial_gcd_fp_recursive, polynomial_gcd_brown_modular, polynomial_groebner_fglm,
# cas_context_params.hpp, ode_solver_frobenius, summation_abramov.

# A29 (SPLIT-RISCH-RDE) — SPLIT DONE 2026-07-07: risch_rde_bronstein.cpp →
#   + risch_rde_parametric_field.cpp; risch_parametric_rational.cpp →
#   + risch_parametric_limited.cpp (shared helpers in risch_parametric_internal.hpp).
#   All four TUs now ≤500 LOC. No whitelist entries needed.

